// Оболочка карточки ресурса — РЕ-ЭКСПОРТ общей реализации.
//
// Здесь лежала своя копия на 524 строки, и держала её РАЗНОСТЬ РЕЕСТРОВ, а не
// собственное поведение: общая оболочка резолвит спеку ПО МАРШРУТУ в общем
// реестре, а спека снимка жила только в доменном — карточка снимка осталась бы
// без спеки, то есть без страницы. Разность снята переносом спек (#1466), и
// вместе с ней ушло основание копии.
//
// Различия были ОТСТАВАНИЕМ, а не своим, и обе стороны измеримы: у копии не было
// адаптера, снимающего результат перехода (`void navigate`) — переход уезжал в
// расширение промисом под видом void, и отказ становился необработанным
// отклонением, которого уже никто не увидит; не было и продолжения курсора у
// вкладки связанных ресурсов, поэтому она судила о наборе по первой странице.
//
// Своего у копии ровно один проп — `extraTabs`, и этот модуль не передавал его
// НИ РАЗУ: доменные вкладки приходят реестром расширений (`@/registerExtensions`),
// а страница монтирует оболочку только как `spec` и `mode`.
export * from "@shared/components/organisms/ResourceShell";
