// Сборка тела запроса и маски правки — единственная реализация в
// `@shared/lib/update-mask`. Здесь была её копия ВМЕСТЕ с диалогом формы,
// который не рендерился ни в одном модуле (ноль вхождений `<ResourceFormDialog`):
// снят как мёртвый вместе со своей копией помощников (#405).
//
// Файл остаётся на прежнем месте, потому что по нему адресуются `InlineResource*Form`
// и пробы модулей: переезд импортов — отдельный предмет, а не попутный.
export * from "@shared/lib/update-mask";
