// Подписи опций RefSelect — единственная реализация лежит в @shared.
//
// Здесь была своя копия этих двух функций, и копия отстала: общая версия уже
// читала подсеть по именам ствола (`ipv4_cidr_primary`/`ipv4_cidr_blocks`) и
// помечала зону/регион, закрытые для размещения, а локальная — всё ещё по
// снятым `v4_cidr_blocks`/`v6_cidr_blocks`. Реэкспорт убирает не расхождение,
// а саму возможность разойтись: реализация одна.

export * from "@shared/components/organisms/form/RefSelect/refOptionLabel";
