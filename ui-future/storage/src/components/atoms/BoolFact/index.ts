// BoolFact живёт в @shared в ЕДИНСТВЕННОМ экземпляре — здесь только реэкспорт.
//
// Своя копия завелась бы третьей (shared + compute) и разошлась бы молча: у
// реэкспорта тела нет, поэтому он следует за источником сам. Правило 6 канона
// консоли («булево свойство называется СЛЕДСТВИЕМ, а не «Да»») держится ОДНОЙ
// реализацией, а не тремя одинаковыми на вид.
export * from "@shared/components/atoms/BoolFact";
