// Реестр ресурсов блочного хранения — РЕ-ЭКСПОРТ общего.
//
// Здесь лежала своя копия на 704 строки. Она была не «своим», а ПОСЛЕДНИМ
// держателем разности: спека снимка жила только тут, и общая оболочка карточки
// (`ResourceShell`) резолвит спеку ПО МАРШРУТУ в общем реестре — карточка снимка
// осталась бы без спеки, то есть без страницы. Из-за этой разности из одного
// элемента домен был обязан держать и свой реестр, и свою оболочку сразу.
//
// СВЕДЕНО НЕ «взять общее». По трём спекам из шести богаче был домен: у общего
// они стояли целями ссылок — без полей формы и без глаголов, — и замена в ту
// сторону сняла бы у арендатора создание тома, снимка и правку образа МОЛЧА.
// Полные объявления перенесены отсюда в общий реестр (#1466), и уже после этого
// здесь осталась ссылка.
//
// Наблюдаемое, ради чего это делалось: `RefNameLink` резолвит спеку в ОБЩЕМ
// реестре, поэтому на карточке образа колонка «Источник» ссылалась на том, но
// вырождалась в плоский идентификатор на снимке — одно поле читалось двумя
// разными способами.
export * from "@shared/lib/resource-registry";
