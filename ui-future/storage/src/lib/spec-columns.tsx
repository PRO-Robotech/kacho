// Колонки таблицы из `ResourceSpec.columns` — ОДНА реализация на всё дерево,
// в `@shared/lib/spec-columns`. Здесь только реэкспорт.
//
// Здесь лежала её копия — байт-в-байт такая же, как ещё у трёх модулей, и все
// четыре отстали от общей. Отставание было не косметическим:
//
//   · ветки `bool` не было вовсе — булево уезжало в умолчание и печаталось
//     служебным словом `true` вместо факта, названного следствием (правило 5
//     канона консоли);
//   · ширины колонки не было — колонка без ширины забирает весь остаток себе,
//     и короткие значения расходятся на треть экрана каждое;
//   · набор ссылок «кем используется» сворачивался в «первый + ещё N» —
//     число, из которого не узнать ни одного ресурса.
//
// И расхождение было НАБЛЮДАЕМЫМ внутри одного модуля: списки storage рисует
// `ResourceListPage` из `@shared` — то есть уже общими колонками, — а вкладка
// связанного ресурса на карточке звала эту копию. Один предмет, два поведения
// (правило 9 канона).
export * from "@shared/lib/spec-columns";
