// Какие ресурсы монтирует ЭТОТ модуль — одно объявление на всё приложение.
//
// Реестр спек стал общим (#1466): в нём лежит вся платформа, а модуль показывает
// шесть ресурсов из тридцати. Пока перечень был выписан внутри страницы, «что
// монтирует storage» знала только она, и всякий, кому это нужно, выводил ответ
// заново — из реестра. Такой вывод был верен ровно до тех пор, пока реестр был
// доменным, и стал ложным в тот час, когда реестр переехал: dev-прокси начал
// требовать правил для /compute, /nlb и /vpc, к которым это приложение не
// обращается ни одним запросом.
//
// Поэтому перечень объявлен, а не выведен: состав экрана — решение продукта, и
// у него обязан быть автор. Выводится из него ДАЛЬШЕ (домены прокси, цели
// ссылок), но сам он ниоткуда не выводится.

/** Ресурсы блочного хранения, которыми арендатор управляет в своём проекте. */
export const MODULE_CRUD_SPEC_IDS = ["volumes", "snapshots", "images"] as const;

/** Справочник классов диска: заводит его администратор облака, область — кластер. */
export const MODULE_CATALOG_SPEC_ID = "disk-types";

/** Всё, что модуль монтирует страницей. */
export const MODULE_SPEC_IDS: readonly string[] = [...MODULE_CRUD_SPEC_IDS, MODULE_CATALOG_SPEC_ID];
