// Чтение списка ресурса — ОДНА реализация на всё дерево, в
// `@shared/lib/use-resource-list`. Здесь только реэкспорт.
//
// Здесь лежала её копия, отставшая на целое свойство: она брала РОВНО ОДНУ
// страницу и курсор ответа не читала вовсе, поэтому всё, что за курсором,
// оставалось невидимым. Расхождение было наблюдаемо внутри одного модуля:
// список storage рисует `ResourceListPage` из `@shared` — он страницы
// накапливает и умеет «Показать ещё», — а вкладка связанного ресурса на
// карточке звала эту копию и о продолжении не знала. Один предмет, два
// поведения (правило 9 канона консоли).
//
// Вместе с копией снят и её спутник `fetchAllPages`: общий хук дочитывает
// курсор сам (`opts.loadAllPages`), то есть ТЕМ ЖЕ запросом, а не вторым
// путём чтения — два пути разошлись бы молча. Проба, закреплявшая
// `fetchAllPages`, снята ВМЕСТЕ со своим предметом: она была скопирована из
// модуля реестра и проверяла функцию, которую storage не звал ниоткуда.
export * from "@shared/lib/use-resource-list";
