node_modules/
# Без завершающего слеша — иначе правило ловит только КАТАЛОГ, а символическая
# ссылка с тем же именем проходит мимо и попадает в индекс по `git add -A`
# (случилось 2026-08-12: ссылка на абсолютный путь рабочей машины уехала в
# коммит; на чужой машине она битая, а гейты обхода дерева читают её как файл).
node_modules
dist/
*.tsbuildinfo
dev-*.log
*.log
.DS_Store

# AI-оснастка (источник истины — kacho-workspace, локально синкается) — не коммитим
.claude/
CLAUDE.md
AGENTS.md
.codex/
.agents/

# Редактор JSON кладётся в public при сборке (scripts/copy-monaco.mjs) — произведённое
*/public/monaco/
