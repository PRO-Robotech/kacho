// Повторное подтверждение личности — РЕ-ЭКСПОРТ общей реализации.
//
// Здесь лежала копия на 368 строк, отличавшаяся от соседней ровно алиасом
// импорта. Обе вели к одному файлу shared, то есть различия по существу не
// было вовсе — было два места, которые разошлись бы при первой же правке.
export * from "@shared/components/molecules/auth/StepUpModal";
