import type { FC } from "react";

/**
 * Рисунок пустого списка наборов префиксов.
 *
 * ЧТО НАРИСОВАНО И ПОЧЕМУ ИМЕННО ЭТО
 *
 * Стопка записей одной формы, собранных общей скобой слева. Набор префиксов —
 * не пространство и не отрезок на шкале: это ИМЕНОВАННЫЙ ПЕРЕЧЕНЬ, ценность
 * которого в том, что на него ссылаются по имени, а правят в одном месте.
 * Поэтому рисунок вертикальный: у перечня есть длина, но нет ни границы, как у
 * сети, ни положения на оси, как у подсети.
 *
 * Записи нарочно одинаковы по форме и по ширине — в отличие от подсетей, где
 * ширина отрезка несёт размер диапазона. Здесь префиксы равноправны: набор не
 * упорядочивает их и не придаёт вес ни одному.
 *
 * Каждая запись — точка, длинный штрих адреса и короткий, более тихий штрих
 * длины префикса: ритм «адрес / маска» узнаётся без подписей, которых на рисунке
 * быть не должно. Адрес взят тоном заметнее маски намеренно — на размере, в
 * котором рисунок показывают (208 пикселей в ширину), самый тихий тон исчезает,
 * и записи читались бы пустыми, то есть неотличимо от той, которой ещё нет.
 * Скоба слева — то, что делает перечень одним предметом: её единственная работа
 * сказать, что записи связаны именем, а не лежат рядом случайно.
 *
 * Верхняя запись намечена пунктиром — место под первый префикс. Цветное пятно
 * ровно одно, тон первичный: приглашение, а не предупреждение.
 *
 * Встроенный SVG, а не файл: экран показывается и в модуле, поднятом отдельно
 * от каркаса, где путей к ассетам каркаса нет. Цвета — только токены, иначе на
 * светлой теме рисунок читался бы выключенным.
 *
 * Движения нет: анимация на пустом экране читается как продолжающаяся загрузка,
 * то есть обещает строки, которых не будет.
 */

// Занятые записи набора. Шаг 21 — высота записи 16 плюс просвет 5; перечень, а
// не цикл по индексу: координаты видны глазом и правка одной не двигает прочие.
const ENTRIES = [
  { y: 55, cy: 63 },
  { y: 76, cy: 84 },
  { y: 97, cy: 105 },
];

export const CidrGroupArt: FC = () => (
  // Смысл несёт текст под рисунком, поэтому для чтения с экрана рисунок скрыт.
  <svg
    viewBox="0 0 208 136"
    width="208"
    height="136"
    fill="none"
    strokeLinecap="round"
    aria-hidden="true"
    focusable="false"
    style={{ maxWidth: "100%" }}
  >
    {/* Опора: без неё стопка висит в пустоте и рисунок читается обрезанным. */}
    <line x1="10" y1="118" x2="198" y2="118" stroke="var(--kc-border)" strokeWidth="1.25" />

    {/* Имя набора и число записей — два штриха разной длины. Имя здесь несущее:
        именно по нему набор подставляют в правила. */}
    <line x1="40" y1="20" x2="84" y2="20" stroke="var(--kc-text-tertiary)" strokeWidth="1.25" />
    <line x1="92" y1="20" x2="108" y2="20" stroke="var(--kc-text-tertiary)" strokeWidth="1.25" />

    {/* Скоба набора: она и делает перечень одним предметом. */}
    <line x1="28" y1="34" x2="28" y2="113" stroke="var(--kc-border)" strokeWidth="1.25" />

    {/* Занятые записи: точка, адрес, длина префикса — одна форма на всех. */}
    {ENTRIES.map((entry) => (
      <g key={entry.y}>
        <line
          x1="28"
          y1={entry.cy}
          x2="40"
          y2={entry.cy}
          stroke="var(--kc-border-secondary)"
          strokeWidth="1.25"
        />
        <rect x="40" y={entry.y} width="128" height="16" rx="5" stroke="var(--kc-border)" strokeWidth="1.25" />
        <circle cx="58" cy={entry.cy} r="2.5" stroke="var(--kc-text-tertiary)" strokeWidth="1.25" />
        <line
          x1="68"
          y1={entry.cy}
          x2="124"
          y2={entry.cy}
          stroke="var(--kc-text-tertiary)"
          strokeWidth="1.25"
        />
        <line
          x1="132"
          y1={entry.cy}
          x2="152"
          y2={entry.cy}
          stroke="var(--kc-border-secondary)"
          strokeWidth="1.25"
        />
      </g>
    ))}

    {/* Отвод верхней записи остаётся в общем тоне: скоба уже держит её место,
        цветом помечена сама запись, которой ещё нет. */}
    <line x1="28" y1="42" x2="40" y2="42" stroke="var(--kc-border-secondary)" strokeWidth="1.25" />

    {/* Намеченная запись — единственное цветное пятно рисунка. Знак стоит там,
        где у прочих записей точка: форма записи сохранена, содержимого нет. */}
    <rect
      x="40"
      y="34"
      width="128"
      height="16"
      rx="5"
      fill="var(--kc-primary-bg)"
      stroke="var(--kc-primary)"
      strokeWidth="1.25"
      strokeDasharray="4 4"
    />
    <line x1="52" y1="42" x2="64" y2="42" stroke="var(--kc-primary)" strokeWidth="1.25" />
    <line x1="58" y1="36" x2="58" y2="48" stroke="var(--kc-primary)" strokeWidth="1.25" />
  </svg>
);
