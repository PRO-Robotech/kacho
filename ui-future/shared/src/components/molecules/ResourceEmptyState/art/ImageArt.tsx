import type { FC } from "react";

/**
 * Рисунок пустого списка образов — в общем ключе рисунков продукта.
 *
 * ЧТО НАРИСОВАНО. Слепок диска как стопка слоёв и стрелка разворачивания в
 * готовый экземпляр справа. Верхний слой намечен пунктиром: это место под
 * образ, которого ещё нет.
 *
 * ПОЧЕМУ ИМЕННО ЭТО. Образ отличается от тома не размером, а РОЛЬЮ: он не
 * подключается, он ТИРАЖИРУЕТСЯ — из одного слепка разворачивают сколько угодно
 * томов. Поэтому здесь слои и стрелка наружу, тогда как у тома — цилиндр и
 * линия подключения: два ресурса не вправе получить одну схему, иначе рисунок
 * перестаёт что-либо утверждать о предмете.
 *
 * Слои нарисованы плоскими: слой образа — это разница, а не ёмкость, и
 * толщина у него ничего не означает. Намеченный слой поэтому тоже без боков.
 *
 * Цвет берётся ТОЛЬКО из токенов: зашитый шестнадцатеричный цвет не меняется с
 * темой, и на светлом фоне рисунок читался бы выключенным. Цветное пятно ровно
 * одно — намеченный верхний слой; тон первичный (приглашение), а не
 * предупреждающий.
 *
 * Встроенный SVG, а не файл: экран показывается и в модуле, поднятом отдельно
 * от каркаса, где путей к ассетам каркаса нет.
 *
 * Движения нет вовсе: анимация на пустом экране читается как продолжающаяся
 * загрузка, то есть обещает строки, которых не будет.
 */

// Слой слепка — ромб в изометрии: левая вершина, верх, правая вершина, низ.
// Полуширина 34 и полувысота 9 держат слои разнесёнными на 22 без наложения.
const layer = (cy: number): string => `M34 ${cy} L68 ${cy - 9} L102 ${cy} L68 ${cy + 9} Z`;

export const ImageArt: FC = () => (
  // Смысл несёт текст под рисунком; озвучивать «стопка слоёв» нечего.
  <svg
    viewBox="0 0 208 136"
    width="208"
    height="136"
    fill="none"
    strokeLinecap="round"
    aria-hidden="true"
    focusable="false"
    style={{ maxWidth: "100%" }}
  >
    {/* Опора: без неё стопка висит в пустоте и рисунок читается обрезанным. */}
    <line x1="10" y1="118" x2="198" y2="118" stroke="var(--kc-border)" strokeWidth="1.25" />

    {/* Существующие слои слепка. */}
    {[92, 70, 48].map((cy) => (
      <path key={cy} d={layer(cy)} stroke="var(--kc-border)" strokeWidth="1.25" />
    ))}

    {/* Боковых рёбер у стопки НЕТ намеренно: ребро через все слои превращает
        рисунок в ящик, а слои — в его грани. Принадлежность одному слепку несут
        общая ось и равный шаг, как в любой послойной схеме. */}

    {/* Намеченный верхний слой — единственное цветное пятно рисунка. */}
    <path
      d={layer(26)}
      fill="var(--kc-primary-bg)"
      stroke="var(--kc-primary)"
      strokeWidth="1.25"
      strokeDasharray="4 4"
    />

    {/* Разворачивание: стрелка от слепка к экземпляру. */}
    <line x1="110" y1="70" x2="138" y2="70" stroke="var(--kc-text-tertiary)" strokeWidth="1.25" />
    <line x1="138" y1="70" x2="132" y2="66" stroke="var(--kc-text-tertiary)" strokeWidth="1.25" />
    <line x1="138" y1="70" x2="132" y2="74" stroke="var(--kc-text-tertiary)" strokeWidth="1.25" />

    {/* Экземпляр, развёрнутый из слепка: у него, в отличие от слоя, есть
        толщина — это уже ёмкость, а не разница. */}
    <path d="M144 62 L166 55 L188 62 L166 69 Z" stroke="var(--kc-border-secondary)" strokeWidth="1.25" />
    <line x1="144" y1="62" x2="144" y2="76" stroke="var(--kc-border-secondary)" strokeWidth="1.25" />
    <line x1="188" y1="62" x2="188" y2="76" stroke="var(--kc-border-secondary)" strokeWidth="1.25" />
    <path d="M144 76 L166 83 L188 76" stroke="var(--kc-border-secondary)" strokeWidth="1.25" />
  </svg>
);
