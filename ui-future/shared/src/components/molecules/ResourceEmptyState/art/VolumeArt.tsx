import type { FC } from "react";

/**
 * Рисунок пустого списка томов — в общем ключе рисунков продукта.
 *
 * ЧТО НАРИСОВАНО. Стопка блоков цилиндром — том как блочное устройство, — и
 * линия подключения к машине справа. Верхний блок намечен пунктиром: это место
 * под том, которого ещё нет.
 *
 * ПОЧЕМУ ИМЕННО ЭТО. Том в Kachō задаётся не файлами, а ЁМКОСТЬЮ и
 * ПОДКЛЮЧЕНИЕМ: набор блоков, который присоединяют к машине. Отсюда две части
 * рисунка, и вторая обязательна — том без линии подключения читался бы как
 * файловое хранилище, то есть обещал бы не тот предмет.
 *
 * Машина справа нарисована глухой рамкой без содержимого: владелец привязки —
 * storage, и рисунок не должен выглядеть так, будто отсюда заводят машину.
 *
 * Цвет берётся ТОЛЬКО из токенов: зашитый шестнадцатеричный цвет не меняется с
 * темой, и на светлом фоне рисунок читался бы выключенным. Цветное пятно ровно
 * одно — намеченный верхний блок; тон первичный (приглашение), а не
 * предупреждающий.
 *
 * Встроенный SVG, а не файл: экран показывается и в модуле, поднятом отдельно
 * от каркаса, где путей к ассетам каркаса нет.
 *
 * Движения нет вовсе: анимация на пустом экране читается как продолжающаяся
 * загрузка, то есть обещает строки, которых не будет.
 */
export const VolumeArt: FC = () => (
  // Смысл несёт текст под рисунком; озвучивать «стопка блоков» нечего.
  <svg
    viewBox="0 0 208 136"
    width="208"
    height="136"
    fill="none"
    strokeLinecap="round"
    aria-hidden="true"
    focusable="false"
    style={{ maxWidth: "100%" }}
  >
    {/* Опора: без неё стопка висит в пустоте и рисунок читается обрезанным. */}
    <line x1="10" y1="118" x2="198" y2="118" stroke="var(--kc-border)" strokeWidth="1.25" />

    {/* Существующие блоки: бока и донце стопки. */}
    <line x1="38" y1="44" x2="38" y2="88" stroke="var(--kc-border)" strokeWidth="1.25" />
    <line x1="98" y1="44" x2="98" y2="88" stroke="var(--kc-border)" strokeWidth="1.25" />
    <path d="M38 88 A 30 10 0 0 0 98 88" stroke="var(--kc-border)" strokeWidth="1.25" />
    <ellipse cx="68" cy="44" rx="30" ry="10" stroke="var(--kc-border)" strokeWidth="1.25" />

    {/* Границы блоков — только передние дуги: стопка читается по слоям, а не по
        сплошной трубе. */}
    {[62, 75].map((y) => (
      <path
        key={y}
        d={`M38 ${y} A 30 10 0 0 0 98 ${y}`}
        stroke="var(--kc-border-secondary)"
        strokeWidth="1.25"
      />
    ))}

    {/* Намеченный верхний блок — единственное цветное пятно рисунка. Он стоит
        НАД стопкой, потому что том добавляют сверху, а не врезают в середину. */}
    <ellipse
      cx="68"
      cy="30"
      rx="30"
      ry="10"
      fill="var(--kc-primary-bg)"
      stroke="var(--kc-primary)"
      strokeWidth="1.25"
      strokeDasharray="4 4"
    />
    <line x1="38" y1="30" x2="38" y2="44" stroke="var(--kc-primary)" strokeWidth="1.25" strokeDasharray="4 4" />
    <line x1="98" y1="30" x2="98" y2="44" stroke="var(--kc-primary)" strokeWidth="1.25" strokeDasharray="4 4" />

    {/* Подключение: точка привязки на томе и линия к машине. */}
    <circle cx="98" cy="70" r="2.5" stroke="var(--kc-text-tertiary)" strokeWidth="1.25" />
    <line x1="102" y1="70" x2="146" y2="70" stroke="var(--kc-border-secondary)" strokeWidth="1.25" />

    {/* Машина-потребитель: рамка с парой штрихов. Её устройство здесь не
        расписано — предмет экрана том, а не то, к чему он присоединён. */}
    <rect x="146" y="52" width="36" height="36" rx="6" stroke="var(--kc-border)" strokeWidth="1.25" />
    {[64, 76].map((y) => (
      <line key={y} x1="154" y1={y} x2="174" y2={y} stroke="var(--kc-text-tertiary)" strokeWidth="1.25" />
    ))}
  </svg>
);
