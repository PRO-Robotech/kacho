import type { FC } from "react";

/**
 * Рисунок пустого списка целевых групп.
 *
 * ЧТО НАРИСОВАНО. Скоба слева охватывает три цели в столбец; у существующих
 * целей справа стоит отметка проверки состояния, у средней — пустой кружок
 * вместо отметки, потому что самой цели ещё нет. Точка на конце скобы — место,
 * которым группа цепляется к балансировщику.
 *
 * ПОЧЕМУ ИМЕННО ЭТО. Предмет ресурса — не распределение трафика (это соседний
 * рисунок, там вход, узел и веер), а ДВА других свойства: цели названы ВМЕСТЕ,
 * одним именем, и у каждой есть состояние, которое кто-то проверяет. Отсюда
 * скоба вместо стрелки и отметка проверки вместо потока.
 *
 * Цветное пятно ровно одно — намеченная цель, тон первичный: это приглашение
 * завести первую группу, а не предупреждение.
 *
 * Встроенный SVG, а не файл: экран показывается и в модуле, поднятом отдельно
 * от каркаса, где путей к ассетам каркаса нет. Цвета — только токены, иначе на
 * светлой теме рисунок читался бы выключенным.
 *
 * Движения нет: анимация на пустом экране читается как продолжающаяся загрузка,
 * то есть обещает строки, которых не будет.
 */
export const TargetGroupArt: FC = () => (
  // Смысл несёт текст под рисунком; озвучивать схему нечего.
  <svg
    viewBox="0 0 208 136"
    width="208"
    height="136"
    fill="none"
    strokeLinecap="round"
    aria-hidden="true"
    focusable="false"
    style={{ maxWidth: "100%" }}
  >
    {/* Опора: без неё рисунок висит в пустоте и читается обрезанным. */}
    <line x1="10" y1="118" x2="198" y2="118" stroke="var(--kc-border)" strokeWidth="1.25" />

    {/* Скоба: вертикаль с двумя плечами и носиком посередине. Она и есть
        «группа» — общее имя, под которым цели адресуются разом. */}
    <line x1="48" y1="26" x2="48" y2="102" stroke="var(--kc-border)" strokeWidth="1.25" />
    <line x1="48" y1="26" x2="56" y2="26" stroke="var(--kc-border)" strokeWidth="1.25" />
    <line x1="48" y1="102" x2="56" y2="102" stroke="var(--kc-border)" strokeWidth="1.25" />
    <line x1="48" y1="64" x2="38" y2="64" stroke="var(--kc-border)" strokeWidth="1.25" />

    {/* Точка привязки: сюда группу берёт балансировщик. */}
    <circle cx="33" cy="64" r="3" stroke="var(--kc-text-tertiary)" strokeWidth="1.25" />

    {/* Две существующие цели: маркер адресата и короткий штрих содержимого. */}
    {[26, 82].map((y) => (
      <g key={y}>
        <rect x="64" y={y} width="86" height="20" rx="5" stroke="var(--kc-border)" strokeWidth="1.25" />
        <circle cx="76" cy={y + 10} r="2.5" stroke="var(--kc-text-tertiary)" strokeWidth="1.25" />
        <line
          x1="86"
          y1={y + 10}
          x2="120"
          y2={y + 10}
          stroke="var(--kc-border-secondary)"
          strokeWidth="1.25"
        />
        {/* Отметка проверки состояния — галочка справа от цели. */}
        <line
          x1="160"
          y1={y + 10}
          x2="164"
          y2={y + 15}
          stroke="var(--kc-text-tertiary)"
          strokeWidth="1.25"
        />
        <line
          x1="164"
          y1={y + 15}
          x2="172"
          y2={y + 5}
          stroke="var(--kc-text-tertiary)"
          strokeWidth="1.25"
        />
      </g>
    ))}

    {/* Намеченная цель — предмет экрана: место под первую целевую группу. */}
    <rect
      x="64"
      y="54"
      width="86"
      height="20"
      rx="5"
      fill="var(--kc-primary-bg)"
      stroke="var(--kc-primary)"
      strokeWidth="1.25"
      strokeDasharray="4 4"
    />

    {/* Место под отметку проверки: у несуществующей цели состояния ещё нет, и
        показывать галочку значило бы утверждать проверку, которой не было. */}
    <circle cx="166" cy="64" r="5" stroke="var(--kc-border-secondary)" strokeWidth="1.25" />
  </svg>
);
