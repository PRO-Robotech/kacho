import type { FC } from "react";

/**
 * Рисунок пустого списка реестров образов.
 *
 * ЧТО НАРИСОВАНО. Полка репозиториев в столбец слева; от среднего репозитория
 * отходит стойка с метками версий — тегами (бирка со скошенным носом и
 * отверстием). Верхняя запись полки намечена пунктиром: место под первый реестр.
 *
 * ПОЧЕМУ ИМЕННО ЭТО. Предмет ресурса — ХРАНИЛИЩЕ с двумя осями: сколько
 * репозиториев на полке и сколько версий у каждого. Рисунок показывает обе оси
 * сразу, поэтому не путается ни с балансировщиком (там вход, узел и веер), ни с
 * целевой группой (там скоба и отметка проверки состояния).
 *
 * ПОЧЕМУ ТЕГ — БИРКА, А НЕ ЕЩЁ ОДНА СТРОКА. Первая редакция рисовала версии теми
 * же скруглёнными строками, что и репозитории, и два разных предмета выглядели
 * одинаково: правый столбец читался как второй список репозиториев. Скошенный
 * нос и отверстие отличают метку от записи полки без единой подписи.
 *
 * Цветное пятно ровно одно — намеченная верхняя запись, тон первичный: это
 * приглашение завести первый реестр, а не предупреждение.
 *
 * Встроенный SVG, а не файл: экран показывается и в модуле, поднятом отдельно
 * от каркаса, где путей к ассетам каркаса нет. Цвета — только токены, иначе на
 * светлой теме рисунок читался бы выключенным.
 *
 * Движения нет: анимация на пустом экране читается как продолжающаяся загрузка,
 * то есть обещает строки, которых не будет.
 */

// Контур бирки: скошенный нос слева, дальше прямоугольник. Только прямые
// отрезки — тот же чертёжный ключ, что у остальных рисунков.
const tagOutline = (y: number) =>
  `M 132 ${y + 7} L 141 ${y} L 182 ${y} L 182 ${y + 14} L 141 ${y + 14} Z`;

export const RegistryArt: FC = () => (
  // Смысл несёт текст под рисунком; озвучивать схему нечего.
  <svg
    viewBox="0 0 208 136"
    width="208"
    height="136"
    fill="none"
    strokeLinecap="round"
    aria-hidden="true"
    focusable="false"
    style={{ maxWidth: "100%" }}
  >
    {/* Опора: без неё рисунок висит в пустоте и читается обрезанным. */}
    <line x1="10" y1="118" x2="198" y2="118" stroke="var(--kc-border)" strokeWidth="1.25" />

    {/* Намеченная верхняя запись полки — предмет экрана: место под первый
        реестр. Она стоит первой, потому что новое кладут сверху. */}
    <rect
      x="22"
      y="24"
      width="90"
      height="18"
      rx="5"
      fill="var(--kc-primary-bg)"
      stroke="var(--kc-primary)"
      strokeWidth="1.25"
      strokeDasharray="4 4"
    />

    {/* Существующие репозитории: квадратный маркер образа и штрих имени. */}
    {[52, 80].map((y) => (
      <g key={y}>
        <rect x="22" y={y} width="90" height="18" rx="5" stroke="var(--kc-border)" strokeWidth="1.25" />
        <rect
          x="30"
          y={y + 5}
          width="8"
          height="8"
          rx="2"
          stroke="var(--kc-text-tertiary)"
          strokeWidth="1.25"
        />
        <line x1="46" y1={y + 9} x2="78" y2={y + 9} stroke="var(--kc-border-secondary)" strokeWidth="1.25" />
      </g>
    ))}

    {/* Отвод от среднего репозитория и стойка, на которой висят его версии:
        теги принадлежат ОДНОМУ репозиторию, а не полке целиком. */}
    <line x1="112" y1="61" x2="124" y2="61" stroke="var(--kc-border-secondary)" strokeWidth="1.25" />
    <line x1="124" y1="61" x2="124" y2="97" stroke="var(--kc-border-secondary)" strokeWidth="1.25" />

    {/* Метки версий: бирка с отверстием и штрихом значения. */}
    {[54, 72, 90].map((y) => (
      <g key={y}>
        <line x1="124" y1={y + 7} x2="132" y2={y + 7} stroke="var(--kc-border-secondary)" strokeWidth="1.25" />
        <path d={tagOutline(y)} stroke="var(--kc-border)" strokeWidth="1.25" />
        <circle cx="147" cy={y + 7} r="2" stroke="var(--kc-text-tertiary)" strokeWidth="1.25" />
        <line
          x1="156"
          y1={y + 7}
          x2="174"
          y2={y + 7}
          stroke="var(--kc-border-secondary)"
          strokeWidth="1.25"
        />
      </g>
    ))}
  </svg>
);
