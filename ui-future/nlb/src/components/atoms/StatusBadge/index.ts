// Единственная реализация живёт в `shared/`. Копия в модуле была форком и
// ОТСТАЛА: в ней не было тона для `MIGRATING`, поэтому переезжающий том падал
// в запасной `muted` — тем же тоном, что «остановлен» и «освобождён», — и
// живой процесс выглядел неактивным (#405).
export * from "@shared/components/atoms/StatusBadge";
