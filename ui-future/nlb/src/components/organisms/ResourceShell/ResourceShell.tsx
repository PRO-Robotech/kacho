// Оболочка карточки ресурса — ОДНА на всю консоль, в `shared/`.
//
// Здесь стояла копия, разошедшаяся с общей на 426 строк, и различие было
// отставанием целиком: продолжение курсора на связанных вкладках, фасетный
// отбор, сужение ребёнка адресом и серверным полем, охрана неполного адреса
// карточки, действия на уровне ИМЕНИ ресурса вместо шапки страницы,
// копирование у строк имени и описания, общая вкладка JSON. Правка общей до
// раздела не доезжала.
//
// Единственное, что было своим, — проп `extraTabs` ради вкладки «Целевые
// группы» балансировщика. Общая оболочка умеет ровно это через реестр
// расширений (`ext.extraTabs`), поэтому проп не переезжал, а исчез: вкладку
// подаёт `@/lib/nlb-detail-extensions`.
export * from "@shared/components/organisms/ResourceShell";
