// Барьер карточки ресурса — ВЕСЬ общий модуль, а не один файл из него.
//
// Прежде здесь стоял `export * from "./DetailShell"`, то есть наружу выходила
// ровно оболочка. Соседние части того же языка карточки — поверхность-секция
// (`DetailSurface`), строка свойства (`PropertyRows` вместе с признаком
// `property-row-context`), вкладка `JsonTab` и шапка страницы `PageHead` — за
// барьером оставались, и модуль, не имея к ним доступа, рисовал «Обзор» своей
// таблицей antd. Узкий барьер и есть то, чем форк держался: пока общего нечем
// позвать, своё выглядит единственным выходом.
export * from "@shared/components/organisms/DetailShell";
