// Единственная реализация живёт в `shared/`. Копия в модуле была форком и
// отставала от неё: правка, севшая в одну копию, молча минует остальные,
// и пользователь читает разницу как «другое место продукта» (#405).
//
// Что копия НЕ умела и что приезжает вместе со сведением (#560): объявленное
// действие-глагол `spec.rowVerbs` — ресурс, у которого глагол был единственным
// действием, не получал ни столбца, ни пункта, и отказа при этом не было;
// закрытый список «перемещать нечем» — общий, поэтому заглушка перемещения
// больше не предлагается там, где у края такого глагола нет; адрес мутации
// берётся у admin-плоскости, если она есть.
export * from "@shared/components/molecules/RowActionsMenu/RowActionsMenu";
