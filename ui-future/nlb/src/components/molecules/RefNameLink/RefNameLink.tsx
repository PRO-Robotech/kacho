// Ссылка на чужой ресурс по имени — РЕ-ЭКСПОРТ общей реализации.
//
// Здесь лежала своя копия, отставшая от общей на два изменения: она рисовала
// ссылку сама (`Link` + `ResourceIcon`), тогда как общая делает это через
// `ResourceLink`, и не знала признака `copy`, которым строка свойств просит
// ссылку не рисовать собственный значок копирования.
//
// Из-за копии в одной таблице обзора жили два поведения: у «Имени» и
// «Идентификатора» отзывался общий значок справа, у ссылок — свой, другого вида
// и размера. Правка общего компонента до этого модуля не доезжала.
export * from "@shared/components/molecules/RefNameLink";
