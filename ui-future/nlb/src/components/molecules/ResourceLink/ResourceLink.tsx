// Ссылка на ресурс — РЕ-ЭКСПОРТ единственной реализации.
//
// Здесь лежала копия, отставшая от общей на четыре изменения, и каждое из них
// человек видит:
//   • копия не знала признака «я в строке свойств» (`property-row-context`),
//     поэтому в обзоре карточки рядом с общей кнопкой копирования строки
//     вставала вторая, своя, другого вида и размера — в одной таблице жили два
//     поведения (канон §4: «копирование — одна кнопка на строку»);
//   • обрезка имени не наступала никогда: стояло одно `textOverflow` без
//     `nowrap` и `minWidth: 0`, то есть правило, не делавшее ничего;
//   • у каталога размещения (`idIsTheName`) значение несёт как раз хвост
//     идентификатора, и копия резала его на двенадцатом символе — все зоны
//     региона выглядели одинаково;
//   • копия не умела адресовать ресурсы IAM (`/iam/<route>/<id>`).
//
// Копий не заводить: правка общего до модуля не доезжает молча (канон §9).
export * from "@shared/components/molecules/ResourceLink";
