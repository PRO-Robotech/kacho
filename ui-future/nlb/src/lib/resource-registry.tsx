// Реестр ресурсов — ОДИН на всю консоль, в `@shared/lib/resource-registry`.
//
// Здесь стояла копия на девять записей. Состав уже сходился — все девять общий
// реестр нёс, — а вот СОДЕРЖАНИЕ расходилось: у общего балансировщика не было
// схемы VIP, резолвнутого адреса, зон без анонса, привязки сессий, защиты от
// удаления и вкладки обработчиков, а у обработчика — порта на цели и группы
// целей по умолчанию. Различие содержания и держало форк: пользователь видел
// РАЗНЫЕ формы одного ресурса (маршрут `/nlb/*` рисует этот модуль, а
// `/vpc/*` — общий реестр), и правка в одном месте до другого не доезжала.
//
// Содержание сведено к тому, что богаче (#1471), и копия стала прослойкой.
// Записи, которые модуль показывает, перечисляет `NlbPage`; ссылочные цели
// (`addresses`, `subnets`, `compute-instances`, …) резолвит `RefSelect` по
// идентификатору — обе стороны спрашивают один и тот же реестр (#408).
export * from "@shared/lib/resource-registry";
