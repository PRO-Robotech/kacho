// Единственная реализация живёт в `shared/`. Копия в модуле была форком:
// правка, севшая в одну копию, молча минует остальные, и пользователь читает
// разницу как «другое место продукта» (#405).
//
// Здесь форк был не по содержанию, а ПО ИДЕНТИЧНОСТИ: файлы совпадали байт в
// байт с точностью до алиаса импорта, но `createContext` в двух модулях даёт
// два РАЗНЫХ объекта контекста. Пока потребитель тоже был свой, это не
// проявлялось; #405 свёл просмотр JSON к прослойке на общий компонент — и
// поставщик страницы (`NlbPage`) перестал удовлетворять потребителя внутри
// него. `useThemeMode` бросал, бросок доходил до границы федеративного модуля,
// и пользователь получал не пустую вкладку, а «Раздел «Load Balancer»
// недоступен. Модуль не загрузился» (#479, наблюдалось на стенде 2026-08-16).
//
// Класс: компонент свели, контекст оставили форком. Диффом это не видно — обе
// стороны выглядят исправными по отдельности, а ломается их стык.
export * from "@shared/lib/theme-context";
