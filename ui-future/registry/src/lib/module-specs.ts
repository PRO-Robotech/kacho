// Какие ресурсы монтирует ЭТОТ модуль — одно объявление на всё приложение.
//
// Реестр спек стал общим и для раздела registry (#409): в нём лежит вся
// платформа, а модуль показывает три ресурса из тридцати с лишним. Пока перечень
// был выписан внутри страницы, «что монтирует registry» знала только она, и
// всякий, кому это нужно, выводил ответ заново — из реестра. Такой вывод был
// верен ровно до тех пор, пока реестр был доменным, и стал ложным в тот час,
// когда реестр переехал: dev-прокси начал требовать правил для /compute, /nlb,
// /storage и /vpc, к которым это приложение не обращается ни одним запросом.
//
// Тот же порядок и та же причина, что у storage (#1466): перечень ОБЪЯВЛЕН, а не
// выведен, потому что состав экрана — решение продукта, и у него обязан быть
// автор. Выводится из него ДАЛЬШЕ (домены прокси, цели ссылок), но сам он
// ниоткуда не выводится.

/**
 * Ресурсы Container Registry, которые раздел монтирует страницей.
 *
 * Реестр — единственный, которым арендатор управляет; репозиторий появляется при
 * первом `docker push`, тег пишет он же. Все три при этом маршрутизируются:
 * список, карточка, дочерние вкладки.
 */
export const MODULE_SPEC_IDS: readonly string[] = ["registries", "repositories", "tags"];
