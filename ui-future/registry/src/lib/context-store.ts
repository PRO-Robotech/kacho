// Контекст (account/project) — ОДИН модуль на всё дерево.
//
// Состояние живёт в модуле, поэтому копия означает второе, никем не заполненное
// хранилище: приложение гидратировало бы своё, а общие организмы (RefSelect,
// RefNameLink) читали бы пустое и теряли scope ссылок и выпадающих списков.
export * from "@shared/lib/context-store";
