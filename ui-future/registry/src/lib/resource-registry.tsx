// Реестр ресурсов — ОДИН на всю консоль, в `@shared/lib/resource-registry`.
//
// Здесь лежала копия на четыре записи, и состав у неё был СВОЙ: `registries`,
// `repositories` и `tags` не значились в общем реестре вовсе. Держало форк
// именно это — спеку по идентификатору резолвят ОБЩИЕ оболочка карточки
// (`ResourceShell`), подборщик ссылок (`RefSelect`) и ссылка на чужой ресурс
// (`RefNameLink`), поэтому прямая замена оставила бы раздел без ресурсов, а
// домен был обязан держать и свой реестр, и свою оболочку сразу.
//
// Наблюдаемое, ради чего это делалось: ссылка на реестр из соседнего раздела
// вырождалась в плоский идентификатор — общий `RefNameLink` спеки `registries`
// не находил, — то есть одно поле читалось двумя разными способами, смотря
// откуда на него смотрят.
//
// Три записи перенесены в общий реестр (#409), четвёртая (`regions`,
// ссылочная цель каталога geo) там уже была и богаче: у общей есть админская
// плоскость и фильтр открытости к размещению, которых у копии не было.
export * from "@shared/lib/resource-registry";
