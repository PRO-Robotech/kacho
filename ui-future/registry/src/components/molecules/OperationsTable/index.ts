// Единственная реализация живёт в `shared/`. Копия в модуле была бы форком:
// правка, севшая в одну копию, молча минует остальные, и пользователь читает
// разницу как «другое место продукта» (#405).
export * from "@shared/components/molecules/OperationsTable";
