// Единственная реализация живёт в `shared/`. Копия в модуле была форком и
// отставала от неё: правка, севшая в одну копию, молча минует остальные,
// и пользователь читает разницу как «другое место продукта» (#405).
//
// Проброшен ВЕСЬ барель оболочки, а не один её файл. Прежде здесь стоял
// `@shared/…/DetailShell/DetailShell` — то есть до модуля доезжала оболочка, но
// НЕ то, из чего собирается её содержимое: поверхность-секция, строки свойств с
// их признаком места (`property-row-context`) и вкладка JSON. Модуль поэтому
// собирал обзор сам — таблицей `Descriptions` antd, у которой рамка на каждой
// ячейке и нет столбца копирования, — и объявленное строкой `copy` не рисовалось
// вовсе. Один импорт-шов решал, каким языком говорит карточка.
export * from "@shared/components/organisms/DetailShell";
