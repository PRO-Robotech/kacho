// TokensLayout — каркас части «Токены и ключи» раздела администрирования
// (`/system/tokens/{service-account-keys,user-tokens}`): МЕСТО ДЛЯ СТРАНИЦЫ,
// и только.
//
// ─────────────────────────────────────────────────────────────────────────────
// ПОЧЕМУ ЗДЕСЬ НЕТ НИ ЗАГОЛОВКА, НИ ВКЛАДОК
//
// Части раздела приходят от самого модуля (`system/navigation.ts`) и рисуются
// колонкой слева — тем же способом, что у любого другого раздела консоли. Своя
// полоса вкладок поверх содержимого была бы ТРЕТЬЕЙ конструкцией навигации:
// рейл модулей, колонка разделов и ещё ряд, перечисляющий ровно то же самое.
//
// Соседняя часть того же раздела (`AdminLayout`) устроена так же и по той же
// причине. Пока здесь стояла своя оболочка, две части ОДНОГО раздела строили
// пункты по-разному — столбцом слева и рядом сверху, — и пользователь читал
// разницу как «другое место продукта». Это и есть предмет решения владельца
// #447; проба браузером `findings.spec.ts` меряет ПОЛОЖЕНИЕ пунктов у обеих
// частей и падает, пока они расходятся.
//
// Заголовок называет предмет ОТКРЫТОЙ СТРАНИЦЫ, а не имя части: имя части уже
// сказано подсвеченным пунктом колонки и хлебными крошками.
//
// Поясняющий абзац снят вместе с прежним рядом: он пересказывал название
// раздела, а его единственный факт — секрет показывается один раз — сказан
// там, где он нужен: в окне выпуска (`OneTimeSecretModal`).

import { Outlet } from "react-router";

export function TokensLayout() {
  return <Outlet />;
}

export default TokensLayout;
