// Подписи части «Токены и ключи» — ЕДИНСТВЕННЫЙ источник этого модуля.
//
// ПОЧЕМУ ОТДЕЛЬНЫЙ ФАЙЛ, А НЕ КАНОН ПОДПИСЕЙ. В `shared/lib/entity-names` их
// нет намеренно — они не именуют сущность, а называют раздел и его пункты; там
// прямо сказано, что такие подписи «принадлежат своему месту и в двух местах не
// встречаются». Второе оказалось неверным для этого модуля: «Токены и ключи»
// стояло ТРЕМЯ литералами (пункт меню модуля, имя в границе отказа, шапка
// рейла), «Ключи сервисных аккаунтов» и «Токены пользователей» — двумя каждое
// (пункт меню и пункт рейла). Правка подписи доезжала до одного места из
// двух-трёх, а пользователь читал разницу как разные предметы — ровно тот
// класс, ради которого канон подписей и заведён.
//
// ПОЧЕМУ НЕ РЯДОМ С РЕЙЛОМ. `navigation.ts` уезжает к каркасу отдельным
// федеративным выходом и грузится ДО модуля; импорт из файла с компонентом
// потянул бы за собой оболочку раздела и весь её граф в чужой кусок сборки.
// Отсюда простой модуль без React.
//
// КЛЮЧ ПУНКТА — СЕГМЕНТ АДРЕСА: он связывает две поверхности, обязанные
// совпасть, — пункт меню модуля (`navigation.ts`) и пункт рейла раздела
// (`TokensLayout`). Иконка пункта здесь НЕ живёт: она принадлежит меню, а не
// подписи, и в рейле её нет.

/** Адрес части; адреса её пунктов производятся отсюда. */
export const TOKENS_BASE_PATH = "/system/tokens";

/**
 * Подпись самой части: заголовок рейла, пункт меню модуля и имя, которым
 * называет себя экран отказа модуля.
 */
export const TOKENS_SECTION_LABEL = "Токены и ключи";

export interface TokensSection {
  /** Сегмент адреса — он же идентификатор пункта. */
  segment: string;
  /** Полный адрес страницы пункта. */
  path: string;
  label: string;
}

/** Пункты части — в том порядке, в каком их показывают меню и рейл. */
export const TOKENS_SECTIONS: readonly TokensSection[] = [
  {
    segment: "service-account-keys",
    path: `${TOKENS_BASE_PATH}/service-account-keys`,
    label: "Ключи сервисных аккаунтов",
  },
  {
    segment: "user-tokens",
    path: `${TOKENS_BASE_PATH}/user-tokens`,
    label: "Токены пользователей",
  },
];

/** Адрес, на который часть открывается по умолчанию, — её первый пункт. */
export const TOKENS_LANDING_PATH = TOKENS_SECTIONS[0].path;
