// Реестр ресурсов — ОДИН на всю консоль, в `@shared/lib/resource-registry`.
//
// Здесь стояла копия на восемь записей. СОСТАВ уже сходился — все восемь общий
// реестр несёт (разность ключей пуста), — а расходилось СОДЕРЖАНИЕ, и в обе
// стороны сразу:
//
//   · у общего каталога типов машин не было колонки с моделью ускорителя,
//     которую показывала копия, — два типа с одинаковым числом GPU выглядели
//     одинаково в разделе, который рисует общий реестр (#406, колонка
//     перенесена в общий вместе с гейтом по контракту);
//   · у копии, наоборот, ссылочные записи были ВЫРОЖДЕНЫ до пары колонок без
//     полей, проверки и приведения тела: образ — 2 колонки против 8, сетевой
//     интерфейс — 2 против 10, том — 2 против 9, зона — 1 против 5. Они жили
//     здесь только как цели `RefSelect`, но реестр — один на модуль, и та же
//     бедная запись отвечала за всё, что модуль про них показывает.
//
// Различие содержания и держало форк: пользователь видел РАЗНЫЕ формы одного
// ресурса в зависимости от того, каким разделом он в него пришёл, и правка в
// одном месте до другого не доезжала.
//
// Записи, которые показывает сам раздел, перечисляет `InstancesPage`; ссылочные
// цели (`volumes`, `network-interfaces`, `images`, `zones`, `machine-types`)
// резолвит `RefSelect` по идентификатору — обе стороны спрашивают теперь один и
// тот же реестр.
export * from "@shared/lib/resource-registry";
