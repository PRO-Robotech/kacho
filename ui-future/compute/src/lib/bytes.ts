// Размер в байтах — ОДНА реализация на всё дерево, в @shared/lib/bytes.
//
// Здесь лежала её копия — байт-в-байт такая же, как ещё у двух модулей, и ни
// одна из трёх не была покрыта пробой. Для переписи форков такая копия невидима
// СРАЗУ ПО ОБЕИМ осям: символа общий модуль не объявлял, файла по этому адресу
// у него не было. Расхождение трёх копий не показал бы ни гейт, ни поиск по
// имени — заметить его можно было бы только сравнив файлы вручную, а повода
// сравнивать не возникает.
//
// Имя оставлено прежним намеренно: файл существует ровно затем, чтобы
// вызывающий не заметил переезда реализации.
export * from "@shared/lib/bytes";
