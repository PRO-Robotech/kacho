// Столбцы таблицы по объявлению ресурса — РЕ-ЭКСПОРТ общей реализации.
//
// Здесь лежала копия на 275 строк против 344 в общей, различий 185, и набор
// экспортируемых имён совпадал полностью — то есть своего у модуля не было
// вовсе, было отставание.
//
// Отставание содержательное, а не косметическое: у ссылки в столбце имени
// пропал проп `icon`, поэтому ссылки compute отличались от ссылок всех прочих
// разделов отсутствием значка типа — ровно тот случай, ради которого форки и
// сводятся: правка общего до модуля не доезжает, и в одной таблице живут два
// поведения.
export * from "@shared/lib/spec-columns";
