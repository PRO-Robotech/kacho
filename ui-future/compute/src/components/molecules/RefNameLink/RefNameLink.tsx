// Ссылка на чужой ресурс по имени — РЕ-ЭКСПОРТ общей реализации.
//
// Здесь лежала своя копия, отставшая от общей: она рисовала ссылку сама
// (`Link` + `ResourceIcon`) вместо `ResourceLink` и не знала признака `copy`,
// которым строка свойств просит ссылку не рисовать собственный значок
// копирования. Замер расхождения на момент сведения — 78 строк.
//
// Из-за копий в одной таблице обзора жили два поведения: у «Имени» и
// «Идентификатора» отзывался общий значок справа, у ссылок — свой, другого вида
// и размера. Правка общего компонента до этих модулей не доезжала.
export * from "@shared/components/molecules/RefNameLink";
