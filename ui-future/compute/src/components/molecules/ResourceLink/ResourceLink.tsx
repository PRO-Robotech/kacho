// Ссылка на ресурс — РЕ-ЭКСПОРТ общей реализации.
//
// Здесь лежала своя копия, отставшая от общей на 68 строк, и отставшая ПО
// СУЩЕСТВУ, а не по форме:
//
//   • она не знала `useInPropertyRow` — то есть механизма, которым различие
//     «строка свойств против ячейки таблицы» держится во всём продукте. В
//     строке свойств её значок копирования рисовался вторым, рядом с общей
//     кнопкой столбца;
//   • не знала `resourceServicePrefix`, поэтому ссылки на ресурсы IAM вели не
//     туда;
//   • не несла `minWidth: 0` и `whiteSpace: nowrap` — без них обрезка
//     многоточием не наступает никогда, сколь бы узкой ни была колонка;
//   • не знала решения о постоянной видимости значка копирования.
//
// Правка общего компонента до этого модуля не доезжала вовсе.
export * from "@shared/components/molecules/ResourceLink";
