// Оболочка карточки ресурса — РЕ-ЭКСПОРТ общей реализации.
//
// Здесь лежала своя копия (539 строк против 736 общих, 457 различающихся строк),
// и различия были ОТСТАВАНИЕМ, а не своим: у вкладки связанных ресурсов не было
// продолжения курсора («Показать ещё») и серверного сужения по родителю, поэтому
// она судила о наборе по первой странице; карточка ресурса, адресуемого через
// родителя, отправляла запрос с незакрытой подстановкой в адресе и получала
// отказ, неотличимый от «ресурса нет»; переход `navigate` уезжал в расширение
// промисом под видом void. Своего у копии не было вовсе: единственный проп,
// которого нет у общей (`extraTabs`), этот модуль не передавал ни разу — его
// доменные вкладки приходят реестром расширений (`@/registerExtensions`).
//
// Из-за копии правка общей оболочки до модуля не доезжала, и в одной таблице
// жили два поведения: `ResourceLink` этого модуля не знал признака
// `property-row-context`, которым канон (раздел 4) объявляет держащимся различие
// «строка свойств ↔ ячейка таблицы».
export * from "@shared/components/organisms/ResourceShell";
