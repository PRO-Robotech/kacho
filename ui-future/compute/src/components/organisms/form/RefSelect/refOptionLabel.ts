// Подписи опций RefSelect живут в @shared в ЕДИНСТВЕННОМ экземпляре — здесь
// только реэкспорт под прежними именами.
//
// Здесь стояла тонкая надстройка: общие ресурсы делегировались в @shared, а свой
// случай был ровно один — приписка типа машины (vCPU · память · семейство),
// которой у общей реализации не было. Тип машины при этом ресурс ОБЩЕГО реестра,
// и поле `machineTypeId` объявлено ссылкой на него там же, — то есть подпись
// принадлежала общему, а жила в одном модуле: остальные показывали список
// неразличимых имён. Случай перенесён в `refOptionExtra`, надстройке нечего
// надстраивать.
//
// Имена оставлены прежними намеренно: переименование landed-символа — отдельный
// предмет, а этот файл существует ровно затем, чтобы вызывающий не заметил
// переезда реализации. Заодно снята слепая зона переписи форков: копия с ДРУГИМИ
// именами не спорит с общим ни одним символом, поэтому признак по символу её не
// видел — теперь такие ловятся признаком по адресу.
export { refOptionExtra as extraInfoFor, refOptionHead as headLabelFor } from "@shared/components/organisms/form/RefSelect/refOptionLabel";
