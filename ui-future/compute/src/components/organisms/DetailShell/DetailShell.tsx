// Единственная реализация живёт в `shared/`. Копия в модуле была форком и
// отставала от неё: правка, севшая в одну копию, молча минует остальные,
// и пользователь читает разницу как «другое место продукта» (#405).
//
// Ре-экспорт ведёт на БАРЕЛЬ общего модуля, а не на один файл из него. Прежде
// он вёл на файл, и наружу выходила ровно оболочка: соседние части того же
// языка карточки — поверхность-секция `DetailSurface`, строка свойства
// `PropertyRows` вместе с признаком `property-row-context`, вкладка `JsonTab`,
// шапка страницы `PageHead` — оставались за барьером. Из-за этого «Обзор»
// рисовался здесь своей таблицей antd: пока общее нечем позвать, своё выглядит
// единственным выходом, и узкий барель оказывается тем, чем держится форк.
export * from "@shared/components/organisms/DetailShell";
