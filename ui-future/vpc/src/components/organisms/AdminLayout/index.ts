// Прослойка: реализация одна и живёт в `shared/`. Здесь была КОПИЯ, и она уже
// разошлась с оригиналом — свой горизонтальный ряд вкладок вместо общего рейла и
// поясняющий абзац, снятый решением владельца (#447). Правка доезжала до одного
// места из двух.
export { AdminLayout, default } from "@shared/components/organisms/AdminLayout";
