// Copyright (c) PRO-Robotech
// SPDX-License-Identifier: BUSL-1.1

/*
 * Второе выставленное имя. Настоящий remote выставляет наружу не одно имя, а два —
 * страницу и её навигацию, — и разбор бандла делается плагином ПО КАЖДОМУ имени
 * отдельно. Проба с единственным именем не отличила бы «работает» от «работает для
 * первого»; здесь оба имени берутся из точки входа и оба обязаны выполниться.
 *
 * Стиля тут нет намеренно: у выставленного имени БЕЗ своих стилей объявление законно
 * пусто, и гейт обязан не путать это с потерянным объявлением у того имени, у
 * которого стиль есть.
 */

export const marker = "kacho-federation-probe";

export function sharedResolved() {
  return true;
}

export default { marker, sharedResolved };
