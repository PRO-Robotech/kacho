{
  "info": {
    "_postman_id": "2823ff81-e360-5756-9289-8bd9b89e1b8c",
    "name": "kacho-api-gateway / newman / cluster_admin",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "if (!pm.environment.get('runId') || pm.environment.get('runId') === '') {",
          "  const t = Date.now().toString(36);",
          "  const r = Math.floor(Math.random() * 1e9).toString(36);",
          "  pm.environment.set('runId', ('r' + t + r).replace(/[^a-z0-9]/g, '').slice(0, 11));",
          "}",
          "(function () {",
          "  var _u = '';",
          "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
          "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
          "  if (!_all) { return; }",
          "  var _n = null;",
          "  for (var _i = 0; _i < _all.length; _i++) {",
          "    var _c = _all[_i].slice(2, -2);",
          "    if (!pm.variables.has(_c)) { _n = _c; break; }",
          "  }",
          "  if (_n === null) { return; }",
          "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
          "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
          "  });",
          "  pm.execution.skipRequest();",
          "})();"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "CLUSTER-ADMIN-GET-OK — GET /iam/v1/internal/cluster as cluster system_admin → 200, singleton id",
      "description": "class:CRUD | class:AUTHZ | priority:P0",
      "item": [
        {
          "name": "get-cluster-as-admin",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('cluster id is the singleton cluster_root', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, JSON.stringify(j)).to.eql('cluster_root');",
                  "});",
                  "pm.test('cluster carries a createdAt timestamp (string, RFC3339)', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.createdAt, JSON.stringify(j)).to.be.a('string');",
                  "  pm.expect(j.createdAt, JSON.stringify(j)).to.match(/^\\d{4}-\\d{2}-\\d{2}T/);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CLUSTER-ADMIN-GET-403-ORDINARY — GET /iam/v1/internal/cluster as a never-granted user → 403 PermissionDenied",
      "description": "class:NEG | class:AUTHZ | priority:P0",
      "item": [
        {
          "name": "get-cluster-as-ordinary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtPureNoBindings'",
                  "const __t = pm.environment.get('jwtPureNoBindings') || pm.variables.get('jwtPureNoBindings') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtPureNoBindings is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtPureNoBindings is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 403', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(403));",
                  "pm.test('grpc code 7 (PERMISSION_DENIED)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(7);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CLUSTER-ADMIN-LIST-OK — ListAdmins as cluster system_admin → 200, every entry well-formed",
      "description": "class:CRUD | class:AUTHZ | priority:P0",
      "item": [
        {
          "name": "list-admins-as-admin",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster/admins",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster",
                "admins"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster/admins';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('admins is an array', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.admins || [], JSON.stringify(j)).to.be.an('array');",
                  "});",
                  "pm.test('EVERY admin entry carries subjectId / subjectType / grantedAt', () => {",
                  "  const j = pm.response.json();",
                  "  (j.admins || []).forEach(a => {",
                  "    pm.expect(a.subjectId, JSON.stringify(a)).to.be.a('string');",
                  "    pm.expect(a.subjectId, JSON.stringify(a)).to.not.eql('');",
                  "    pm.expect(a.subjectType, JSON.stringify(a)).to.be.a('string');",
                  "    pm.expect(a.grantedAt, 'grantedAt must be a JSON string timestamp: ' + JSON.stringify(a)).to.be.a('string');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CLUSTER-ADMIN-LIST-403-ORDINARY — ListAdmins as a never-granted user → 403 PermissionDenied (roster not observable)",
      "description": "class:NEG | class:AUTHZ | priority:P0",
      "item": [
        {
          "name": "list-admins-as-ordinary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster/admins",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster",
                "admins"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtPureNoBindings'",
                  "const __t = pm.environment.get('jwtPureNoBindings') || pm.variables.get('jwtPureNoBindings') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtPureNoBindings is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtPureNoBindings is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster/admins';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 403', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(403));",
                  "pm.test('grpc code 7 (PERMISSION_DENIED)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(7);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CLUSTER-ADMIN-GRANT-403-ORDINARY — GrantAdmin as a never-granted user → 403 PermissionDenied (no self-escalation)",
      "description": "class:NEG | class:AUTHZ | class:SEC | priority:P0",
      "item": [
        {
          "name": "grant-as-ordinary",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster/admins",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster",
                "admins"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"subjectType\": \"USER\", \"subjectId\": \"usr00000000000000zzz\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtPureNoBindings'",
                  "const __t = pm.environment.get('jwtPureNoBindings') || pm.variables.get('jwtPureNoBindings') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtPureNoBindings is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtPureNoBindings is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster/admins';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 403', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(403));",
                  "pm.test('grpc code 7 (PERMISSION_DENIED)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(7);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CLUSTER-ADMIN-GRANT-400-INVALID-USER — GrantAdmin with a well-formed but non-existent user id → 400 InvalidArgument",
      "description": "class:VAL | class:NEG | priority:P0",
      "item": [
        {
          "name": "grant-nonexistent-user",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster/admins",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster",
                "admins"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"subjectType\": \"USER\", \"subjectId\": \"usr00000000000000zzz\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster/admins';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('grpc code 3 (INVALID_ARGUMENT)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(3);",
                  "});",
                  "pm.test('error message exactly equals \"User usr00000000000000zzz not found\"', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.message, JSON.stringify(j)).to.eql(\"User usr00000000000000zzz not found\");",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CLUSTER-ADMIN-SEED-TARGET-USER — Seed a fresh, run-scoped User as the grant/revoke target (no shared-subject mutation)",
      "description": "class:SETUP | priority:P0",
      "item": [
        {
          "name": "upsert-target-user",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/users:upsertFromIdentity",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "users:upsertFromIdentity"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"externalId\": \"cluster-admin-target-{{runId}}\", \"email\": \"cluster-admin-target-{{runId}}@kacho.local\", \"displayName\": \"Cluster Admin Target {{runId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/users:upsertFromIdentity';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('target user id has the usr prefix', () => {",
                  "  const j = pm.response.json();",
                  "  const uid = (j.metadata && j.metadata.userId) || (j.user && j.user.id) || '';",
                  "  pm.expect(uid, 'user id must match ^usr<17>: ' + JSON.stringify(j)).to.match(/^usr[0-9a-hjkmnp-tv-z]{17}$/);",
                  "});",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = ((j.metadata && j.metadata.userId) || (j.user && j.user.id));",
                  "  if (v !== undefined && v !== null) pm.environment.set('clusterTargetUserId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-upsert-target-user",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// OPERATION guard (legal skip): the mutation under test was refused on",
                  "// purpose, so there is no Operation to poll and nothing to assert.",
                  "if (!pm.environment.get('opId')) { pm.execution.skipRequest(); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "const j = pm.response.json();",
                  "if (pm.environment.get('_pollStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_pollCount', '0');",
                  "  pm.environment.set('_pollStarted', pm.info.requestName);",
                  "}",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 45) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _ipd = Date.now(); while (Date.now() - _ipd < 500) void 0;  /* real inter-poll delay: cap 45 x 500ms budget (testing.md) */",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.environment.unset('_pollStarted');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "pm.test('operation succeeded (no error)', () => {",
                  "  pm.expect(j.error, 'op error: ' + JSON.stringify(j.error || {})).to.be.oneOf([undefined, null]);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CLUSTER-ADMIN-REVOKE-404-NOT-ADMIN — RevokeAdmin a user who has never been an admin → 404 NotFound, exact text",
      "description": "class:NEG | class:VAL | priority:P0",
      "item": [
        {
          "name": "revoke-non-admin",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster/admins/{{clusterTargetUserId}}",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster",
                "admins",
                "{{clusterTargetUserId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster/admins/{{clusterTargetUserId}}';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 404', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(404));",
                  "pm.test('grpc code 5 (NOT_FOUND)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(5);",
                  "});",
                  "pm.test('error message names the non-admin user verbatim', () => {",
                  "  const j = pm.response.json();",
                  "  const u = pm.environment.get('clusterTargetUserId');",
                  "  pm.expect(j.message, JSON.stringify(j)).to.eql('User ' + u + ' is not an active cluster admin');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CLUSTER-ADMIN-GRANT-OK — GrantAdmin → 200 Operation(iop) done=true, target appears in ListAdmins",
      "description": "class:CRUD | class:AUTHZ | priority:P0",
      "item": [
        {
          "name": "grant-target",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster/admins",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster",
                "admins"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"subjectType\": \"USER\", \"subjectId\": \"{{clusterTargetUserId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster/admins';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('IAM Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id must start with iop: ' + JSON.stringify(j)).to.match(/^iop[a-z0-9]+$/);",
                  "  pm.expect(j.done, 'operation.done present').to.be.a('boolean');",
                  "});",
                  "pm.test('Operation metadata echoes the grant id and subject', () => {",
                  "  const j = pm.response.json();",
                  "  const md = j.metadata || {};",
                  "  pm.expect(md.clusterAdminGrantId, JSON.stringify(j)).to.be.a('string');",
                  "  pm.expect(md.subjectId, JSON.stringify(j)).to.eql(pm.environment.get('clusterTargetUserId'));",
                  "});",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.metadata && j.metadata.clusterAdminGrantId);",
                  "  if (v !== undefined && v !== null) pm.environment.set('clusterGrantId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-grant",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// OPERATION guard (legal skip): the mutation under test was refused on",
                  "// purpose, so there is no Operation to poll and nothing to assert.",
                  "if (!pm.environment.get('opId')) { pm.execution.skipRequest(); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "const j = pm.response.json();",
                  "if (pm.environment.get('_pollStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_pollCount', '0');",
                  "  pm.environment.set('_pollStarted', pm.info.requestName);",
                  "}",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 45) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _ipd = Date.now(); while (Date.now() - _ipd < 500) void 0;  /* real inter-poll delay: cap 45 x 500ms budget (testing.md) */",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.environment.unset('_pollStarted');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "pm.test('operation succeeded (no error)', () => {",
                  "  pm.expect(j.error, 'op error: ' + JSON.stringify(j.error || {})).to.be.oneOf([undefined, null]);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "list-admins-includes-target",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster/admins",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster",
                "admins"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster/admins';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('ListAdmins includes the newly-granted target', () => {",
                  "  const j = pm.response.json();",
                  "  const u = pm.environment.get('clusterTargetUserId');",
                  "  const hit = (j.admins || []).filter(a => a.subjectId === u);",
                  "  pm.expect(hit.length, 'target not in roster: ' + JSON.stringify(j)).to.eql(1);",
                  "});",
                  "pm.test('the granted entry is enriched and typed USER', () => {",
                  "  const j = pm.response.json();",
                  "  const u = pm.environment.get('clusterTargetUserId');",
                  "  const a = (j.admins || []).find(x => x.subjectId === u) || {};",
                  "  pm.expect(a.subjectType, JSON.stringify(a)).to.eql('USER');",
                  "  pm.expect(a.subjectEmail, 'subject email JOINed from users: ' + JSON.stringify(a))",
                  "    .to.eql('cluster-admin-target-' + pm.environment.get('runId') + '@kacho.local');",
                  "  pm.expect(a.grantedAt, JSON.stringify(a)).to.be.a('string');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CLUSTER-ADMIN-GRANT-OK-IDEMPOTENT — GrantAdmin twice → success, SAME grant id, exactly one roster entry",
      "description": "class:CRUD | class:AUTHZ | priority:P0",
      "item": [
        {
          "name": "grant-target-again",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster/admins",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster",
                "admins"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"subjectType\": \"USER\", \"subjectId\": \"{{clusterTargetUserId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster/admins';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('IAM Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id must start with iop: ' + JSON.stringify(j)).to.match(/^iop[a-z0-9]+$/);",
                  "  pm.expect(j.done, 'operation.done present').to.be.a('boolean');",
                  "});",
                  "pm.test('idempotent re-grant returns the EXISTING grant id', () => {",
                  "  const j = pm.response.json();",
                  "  const md = j.metadata || {};",
                  "  pm.expect(md.clusterAdminGrantId, JSON.stringify(j))",
                  "    .to.eql(pm.environment.get('clusterGrantId'));",
                  "});",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-grant-idem",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// OPERATION guard (legal skip): the mutation under test was refused on",
                  "// purpose, so there is no Operation to poll and nothing to assert.",
                  "if (!pm.environment.get('opId')) { pm.execution.skipRequest(); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "const j = pm.response.json();",
                  "if (pm.environment.get('_pollStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_pollCount', '0');",
                  "  pm.environment.set('_pollStarted', pm.info.requestName);",
                  "}",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 45) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _ipd = Date.now(); while (Date.now() - _ipd < 500) void 0;  /* real inter-poll delay: cap 45 x 500ms budget (testing.md) */",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.environment.unset('_pollStarted');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "pm.test('operation succeeded (no error)', () => {",
                  "  pm.expect(j.error, 'op error: ' + JSON.stringify(j.error || {})).to.be.oneOf([undefined, null]);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "list-admins-no-duplicate",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster/admins",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster",
                "admins"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster/admins';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('roster still has exactly ONE entry for the target', () => {",
                  "  const j = pm.response.json();",
                  "  const u = pm.environment.get('clusterTargetUserId');",
                  "  const n = (j.admins || []).filter(a => a.subjectId === u).length;",
                  "  pm.expect(n, JSON.stringify(j)).to.eql(1);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CLUSTER-ADMIN-REVOKE-OK — RevokeAdmin → 200 Operation(iop) done=true, target no longer in ListAdmins",
      "description": "class:CRUD | class:AUTHZ | priority:P0",
      "item": [
        {
          "name": "revoke-target",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster/admins/{{clusterTargetUserId}}",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster",
                "admins",
                "{{clusterTargetUserId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster/admins/{{clusterTargetUserId}}';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('IAM Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id must start with iop: ' + JSON.stringify(j)).to.match(/^iop[a-z0-9]+$/);",
                  "  pm.expect(j.done, 'operation.done present').to.be.a('boolean');",
                  "});",
                  "pm.test('Operation metadata echoes the revoked grant id and subject', () => {",
                  "  const j = pm.response.json();",
                  "  const md = j.metadata || {};",
                  "  pm.expect(md.clusterAdminGrantId, JSON.stringify(j))",
                  "    .to.eql(pm.environment.get('clusterGrantId'));",
                  "  pm.expect(md.subjectId, JSON.stringify(j)).to.eql(pm.environment.get('clusterTargetUserId'));",
                  "});",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-revoke",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// OPERATION guard (legal skip): the mutation under test was refused on",
                  "// purpose, so there is no Operation to poll and nothing to assert.",
                  "if (!pm.environment.get('opId')) { pm.execution.skipRequest(); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "const j = pm.response.json();",
                  "if (pm.environment.get('_pollStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_pollCount', '0');",
                  "  pm.environment.set('_pollStarted', pm.info.requestName);",
                  "}",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 45) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _ipd = Date.now(); while (Date.now() - _ipd < 500) void 0;  /* real inter-poll delay: cap 45 x 500ms budget (testing.md) */",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.environment.unset('_pollStarted');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "pm.test('operation succeeded (no error)', () => {",
                  "  pm.expect(j.error, 'op error: ' + JSON.stringify(j.error || {})).to.be.oneOf([undefined, null]);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "list-admins-excludes-target",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{internalBaseUrl}}/iam/v1/internal/cluster/admins",
              "host": [
                "{{internalBaseUrl}}"
              ],
              "path": [
                "iam",
                "v1",
                "internal",
                "cluster",
                "admins"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// per-step auth: bearer from env 'jwtBootstrap'",
                  "const __t = pm.environment.get('jwtBootstrap') || pm.variables.get('jwtBootstrap') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtBootstrap is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtBootstrap is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh, or prodseed_all.py under production posture) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "// HARNESS-CONFIG GUARD — internalBaseUrl is injected by the newman runner (--env-var).",
                  "// Missing value = misconfigured harness, NOT a legal mode: FAIL, then skip.",
                  "const __cfgUrl = pm.environment.get('internalBaseUrl') || pm.variables.get('internalBaseUrl') || '';",
                  "if (__cfgUrl) {",
                  "  pm.request.url = __cfgUrl + '/iam/v1/internal/cluster/admins';",
                  "} else {",
                  "  pm.test('harness config: internalBaseUrl is set — Internal* RPCs live ONLY on the cluster-internal REST listener', () => {",
                  "    pm.expect.fail('internalBaseUrl is not set — the newman runner (deploy/scripts/newman-parallel.sh --env-var) did not inject it. This step cannot run, and a check that cannot run MUST NOT be silently dropped.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('revoked target is excluded from the active roster', () => {",
                  "  const j = pm.response.json();",
                  "  const u = pm.environment.get('clusterTargetUserId');",
                  "  const found = (j.admins || []).some(a => a.subjectId === u);",
                  "  pm.expect(found, JSON.stringify(j)).to.eql(false);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": []
}
