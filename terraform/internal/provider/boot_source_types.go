// Copyright (c) PRO-Robotech
// SPDX-License-Identifier: BUSL-1.1

package provider

// bootSourceTypes — дискриминаторы ВЛАДЕЛЬЦА источника загрузки машины.
//
// Это не вид ресурса, и путать одно с другим дорого: набор `IMAGE` / `SNAPSHOT` /
// `VOLUME` выглядит осмысленным, читается как «что грузим» — и не принимается
// краем ни одним значением. Модуль `compute-machine` проверял им вход, а его
// собственная проба тот же набор утверждала: два артефакта соглашались друг с
// другом и расходились с продуктом, поэтому оставались зелёными, пока не дошло
// бы до применения у пользователя.
//
// Перечень живёт здесь ОДИН раз и расходится с продуктом не молча: пробa
// `TestBootSourceTypesMatchTheProduct` сверяет его с константами сервиса.
var bootSourceTypes = []string{"storage.image", "registry.image"}
