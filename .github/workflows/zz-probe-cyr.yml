# ОДНОДНЕВКА: проба гипотезы #1073 — кириллический КЛЮЧ задания.
# Отличается от zz-probe-lat.yml РОВНО одним: ключ под jobs: записан кириллицей.
# Всё остальное — отображаемые имена и этот комментарий — тоже кириллица, и она
# в обеих пробах одинакова. Снимается тем же PR, что и чинит предмет.
name: проба кириллицы (ключ)

on:
  push:
    branches: ['issue-1073']

jobs:
  проба-ключ:
    name: проба кириллицы — отображаемое имя
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: сказать что-нибудь
        run: echo "проба исполнилась"
