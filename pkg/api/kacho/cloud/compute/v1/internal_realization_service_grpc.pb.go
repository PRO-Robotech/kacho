// Copyright (c) PRO-Robotech
// SPDX-License-Identifier: Apache-2.0

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: kacho/cloud/compute/v1/internal_realization_service.proto

package computev1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	InternalRealizationService_ReportInstanceRealization_FullMethodName = "/kacho.cloud.compute.v1.InternalRealizationService/ReportInstanceRealization"
)

// InternalRealizationServiceClient is the client API for InternalRealizationService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// InternalRealizationService — входящий канал узлового агента.
//
// # Зачем отдельный сервис, а не поле в публичном контракте
//
// Наша запись о машине — НАМЕРЕНИЕ. Что на узле происходит на самом деле, знает
// только узел, и это два разных факта: намерение пишет арендатор, наблюдаемое
// сообщает исполнитель. Держать их одной колонкой значит потерять расхождение
// между ними — а именно расхождение и есть то, ради чего наблюдаемое собирают.
//
// Две колонки на ПУБЛИЧНОЙ проекции завели бы второй источник истины для
// арендатора и вынесли бы наружу факт исполнителя. Поэтому наблюдаемое живёт
// только здесь, а публично отдаётся ОДНО производное состояние: расхождение
// разрешается в пользу наблюдаемого — машина есть машина, — а причина
// сообщается человекочитаемым полем, уже существующим на публичной проекции.
//
// # Только внутренний слушатель
//
// :9091, взаимный TLS, отдельное отношение записи. Агент — новый класс
// принципала, и право писать о ЧУЖОЙ машине сужается областью: без неё
// предъявитель сертификата отчитывался бы о любой машине облака.
type InternalRealizationServiceClient interface {
	// ReportInstanceRealization — узел сообщает, что он наблюдает.
	//
	// # Почему синхронный ответ, а не операция
	//
	// Общее правило продукта: мутации возвращают операцию. Здесь исключение, и
	// оно обосновано предметом: агент сообщает УЖЕ СЛУЧИВШИЙСЯ факт, а не просит
	// что-то сделать. Операция описывала бы намерение изменить наблюдаемое — то
	// есть ровно то, чего сделать нельзя: наблюдаемое не назначают.
	//
	// # Почему номер события, а не поколение ресурса
	//
	// Отчёт привязывается к номеру события доставки, по которому агент получил
	// намерение. Монотонная колонка на самом ресурсе — это то же поколение под
	// другим именем: она требует правки при КАЖДОЙ мутации и заводит вторую
	// механику ревизии рядом с той, что уже выбрана для доставки намерения.
	//
	// Меньший номер молча отбрасывается как устаревший — переупорядочение в сети
	// не должно откатывать наблюдаемое. Неизвестный отвергается: отчёт о
	// намерении, которого мы не отправляли, означает расхождение, а не новость.
	ReportInstanceRealization(ctx context.Context, in *ReportInstanceRealizationRequest, opts ...grpc.CallOption) (*ReportInstanceRealizationResponse, error)
}

type internalRealizationServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewInternalRealizationServiceClient(cc grpc.ClientConnInterface) InternalRealizationServiceClient {
	return &internalRealizationServiceClient{cc}
}

func (c *internalRealizationServiceClient) ReportInstanceRealization(ctx context.Context, in *ReportInstanceRealizationRequest, opts ...grpc.CallOption) (*ReportInstanceRealizationResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ReportInstanceRealizationResponse)
	err := c.cc.Invoke(ctx, InternalRealizationService_ReportInstanceRealization_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// InternalRealizationServiceServer is the server API for InternalRealizationService service.
// All implementations must embed UnimplementedInternalRealizationServiceServer
// for forward compatibility.
//
// InternalRealizationService — входящий канал узлового агента.
//
// # Зачем отдельный сервис, а не поле в публичном контракте
//
// Наша запись о машине — НАМЕРЕНИЕ. Что на узле происходит на самом деле, знает
// только узел, и это два разных факта: намерение пишет арендатор, наблюдаемое
// сообщает исполнитель. Держать их одной колонкой значит потерять расхождение
// между ними — а именно расхождение и есть то, ради чего наблюдаемое собирают.
//
// Две колонки на ПУБЛИЧНОЙ проекции завели бы второй источник истины для
// арендатора и вынесли бы наружу факт исполнителя. Поэтому наблюдаемое живёт
// только здесь, а публично отдаётся ОДНО производное состояние: расхождение
// разрешается в пользу наблюдаемого — машина есть машина, — а причина
// сообщается человекочитаемым полем, уже существующим на публичной проекции.
//
// # Только внутренний слушатель
//
// :9091, взаимный TLS, отдельное отношение записи. Агент — новый класс
// принципала, и право писать о ЧУЖОЙ машине сужается областью: без неё
// предъявитель сертификата отчитывался бы о любой машине облака.
type InternalRealizationServiceServer interface {
	// ReportInstanceRealization — узел сообщает, что он наблюдает.
	//
	// # Почему синхронный ответ, а не операция
	//
	// Общее правило продукта: мутации возвращают операцию. Здесь исключение, и
	// оно обосновано предметом: агент сообщает УЖЕ СЛУЧИВШИЙСЯ факт, а не просит
	// что-то сделать. Операция описывала бы намерение изменить наблюдаемое — то
	// есть ровно то, чего сделать нельзя: наблюдаемое не назначают.
	//
	// # Почему номер события, а не поколение ресурса
	//
	// Отчёт привязывается к номеру события доставки, по которому агент получил
	// намерение. Монотонная колонка на самом ресурсе — это то же поколение под
	// другим именем: она требует правки при КАЖДОЙ мутации и заводит вторую
	// механику ревизии рядом с той, что уже выбрана для доставки намерения.
	//
	// Меньший номер молча отбрасывается как устаревший — переупорядочение в сети
	// не должно откатывать наблюдаемое. Неизвестный отвергается: отчёт о
	// намерении, которого мы не отправляли, означает расхождение, а не новость.
	ReportInstanceRealization(context.Context, *ReportInstanceRealizationRequest) (*ReportInstanceRealizationResponse, error)
	mustEmbedUnimplementedInternalRealizationServiceServer()
}

// UnimplementedInternalRealizationServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedInternalRealizationServiceServer struct{}

func (UnimplementedInternalRealizationServiceServer) ReportInstanceRealization(context.Context, *ReportInstanceRealizationRequest) (*ReportInstanceRealizationResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ReportInstanceRealization not implemented")
}
func (UnimplementedInternalRealizationServiceServer) mustEmbedUnimplementedInternalRealizationServiceServer() {
}
func (UnimplementedInternalRealizationServiceServer) testEmbeddedByValue() {}

// UnsafeInternalRealizationServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to InternalRealizationServiceServer will
// result in compilation errors.
type UnsafeInternalRealizationServiceServer interface {
	mustEmbedUnimplementedInternalRealizationServiceServer()
}

func RegisterInternalRealizationServiceServer(s grpc.ServiceRegistrar, srv InternalRealizationServiceServer) {
	// If the following call panics, it indicates UnimplementedInternalRealizationServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&InternalRealizationService_ServiceDesc, srv)
}

func _InternalRealizationService_ReportInstanceRealization_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ReportInstanceRealizationRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(InternalRealizationServiceServer).ReportInstanceRealization(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: InternalRealizationService_ReportInstanceRealization_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(InternalRealizationServiceServer).ReportInstanceRealization(ctx, req.(*ReportInstanceRealizationRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// InternalRealizationService_ServiceDesc is the grpc.ServiceDesc for InternalRealizationService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var InternalRealizationService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "kacho.cloud.compute.v1.InternalRealizationService",
	HandlerType: (*InternalRealizationServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "ReportInstanceRealization",
			Handler:    _InternalRealizationService_ReportInstanceRealization_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "kacho/cloud/compute/v1/internal_realization_service.proto",
}
