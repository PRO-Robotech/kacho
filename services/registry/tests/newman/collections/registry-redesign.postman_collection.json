{
  "info": {
    "_postman_id": "c6bcfab7-404a-5cd0-9a57-55d6d27e9e38",
    "name": "kacho-registry / newman / registry-redesign",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "if (!pm.environment.get('runId') || pm.environment.get('runId') === '') {",
          "  const t = Date.now().toString(36);",
          "  const r = Math.floor(Math.random() * 1e9).toString(36);",
          "  pm.environment.set('runId', (t + r).replace(/[^a-z0-9]/g, '').slice(-10));",
          "}",
          "pm.environment.set('_suiteProjectId', pm.environment.get('existingProjectId'));",
          "pm.environment.set('_suiteProjectCrossId', pm.environment.get('existingProjectCrossId'));",
          "pm.environment.set('_suiteRegionId', pm.environment.get('existingRegionId'));",
          "pm.environment.set('_suiteRegionAltId', pm.environment.get('existingRegionAltId'));",
          "// Default auth: project-editor JWT on project A (sufficient for most happy-path steps).",
          "// Per-step auth= overrides via _auth_pre_script.",
          "const __defaultJwt = pm.environment.get('jwtProjectEditorA') || pm.variables.get('jwtProjectEditorA') || '';",
          "if (__defaultJwt && !pm.request.headers.has('Authorization')) {",
          "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __defaultJwt});",
          "}",
          "const __needed = ['baseUrl', 'existingProjectId', 'existingRegionId', 'jwtProjectEditorA'];",
          "const __missing = __needed.filter((k) => !(pm.environment.get(k) || pm.variables.get(k) || ''));",
          "if (__missing.length > 0) {",
          "  pm.test('FIXTURE REQUIRED: ' + __missing.join(', '), () => pm.expect.fail('the suite fixture seed did not run: ' + __missing.join(', ') + ' are empty. Seed via tests/authz-fixtures (prodrun.sh patches the suite env). An empty id is not a skip — it is sent as-is and the request is refused on authorization, so every later failure names the wrong cause.'));",
          "  pm.execution.skipRequest();",
          "}",
          "(function () {",
          "  var _u = '';",
          "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
          "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
          "  if (!_all) { return; }",
          "  var _n = null;",
          "  for (var _i = 0; _i < _all.length; _i++) {",
          "    var _c = _all[_i].slice(2, -2);",
          "    if (!pm.variables.has(_c)) { _n = _c; break; }",
          "  }",
          "  if (_n === null) { return; }",
          "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
          "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
          "  });",
          "  pm.execution.skipRequest();",
          "})();"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "REG-RD-SETUP — Setup: create shared REGIONAL registry {{rdRegId}} (regionId, REG-1 F4)",
      "description": "class:CRUD | priority:P0",
      "item": [
        {
          "name": "create-rdRegId",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"name\": \"redesign-{{runId}}\", \"projectId\": \"{{existingProjectId}}\", \"regionId\": \"{{existingRegionId}}\", \"description\": \"REG-1 redesign coverage\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-1",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "capture-rdRegId-rya14",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('setup op ok (no error)', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "const r = (pm.response.json().response) || {};",
                  "pm.test('id prefix reg', () => pm.expect((r.id||'').startsWith('reg')).to.be.true);",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = ((j.response&&j.response.id)||'');",
                  "  if (v !== undefined && v !== null) pm.environment.set('rdRegId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "warm-rdRegId-rya1",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F4-PLACEMENT-REGIONAL — Get REGIONAL registry → regionId echoed, placementType REGIONAL, zoneId absent (anycast)",
      "description": "class:CRUD | class:CONF | priority:P0",
      "item": [
        {
          "name": "get-placement",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _rc = parseInt(pm.environment.get('_rdRetry') || '0', 10);",
                  "if ((pm.response.code === 403 || pm.response.code === 404) && _rc < 20) {",
                  "  pm.environment.set('_rdRetry', String(_rc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* EC read-your-writes wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_rdRetry');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "const j = pm.response.json();",
                  "pm.test('regionId echoes existingRegionId', () => pm.expect(j.regionId).to.eql(pm.environment.get('existingRegionId')));",
                  "pm.test('placementType REGIONAL (const, not a choice)', () => pm.expect(j.placementType).to.eql('REGIONAL'));",
                  "pm.test('zoneId absent (regional-anycast, zone-independent)', () => pm.expect(j.zoneId === undefined || j.zoneId === '').to.be.true);"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F4-NEG-REGION-REQUIRED — Create without regionId → 400 INVALID_ARGUMENT (\"regionId is required\"), no Operation",
      "description": "class:NEG | class:VAL | priority:P0",
      "item": [
        {
          "name": "create-noregion",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"name\": \"noregion-{{runId}}\", \"projectId\": \"{{existingProjectId}}\", \"description\": \"missing region\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('grpc code 3 (INVALID_ARGUMENT)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(3);",
                  "});",
                  "pm.test('regionId required text', () => pm.expect((pm.response.json().message||'')).to.include('regionId is required'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F4-NEG-REGION-NOTFOUND — Create with nonexistent regionId → FAILED_PRECONDITION (code 9) peer-validate geo",
      "description": "class:NEG | priority:P1",
      "item": [
        {
          "name": "create-badregion",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"name\": \"badregion-{{runId}}\", \"projectId\": \"{{existingProjectId}}\", \"regionId\": \"{{garbageRegionId}}\", \"description\": \"dangling region\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('rejected (FAILED_PRECONDITION → 400)', () => pm.expect(pm.response.code, pm.response.text()).to.eql(400));",
                  "pm.test('grpc code 9 (FAILED_PRECONDITION, peer-validate lane)', () => pm.expect(pm.response.json().code).to.eql(9));",
                  "pm.test('region not found text', () => pm.expect((pm.response.json().message||'').toLowerCase()).to.include('region').and.to.include('not found'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F4-NEG-REGION-IMMUTABLE — Update updateMask=regionId / placementType → 400 INVALID_ARGUMENT (immutable placement anchor)",
      "description": "class:NEG | class:CONF | priority:P1",
      "item": [
        {
          "name": "update-region",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"updateMask\": \"regionId\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('grpc code 3 (INVALID_ARGUMENT)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(3);",
                  "});",
                  "pm.test('regionId immutable text', () => pm.expect(pm.response.json().message).to.eql('regionId is immutable after Registry.Create'));"
                ]
              }
            }
          ]
        },
        {
          "name": "update-placement",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"updateMask\": \"placementType\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('grpc code 3 (INVALID_ARGUMENT)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(3);",
                  "});",
                  "pm.test('placementType immutable text', () => pm.expect(pm.response.json().message).to.eql('placementType is immutable after Registry.Create'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F1-FIELD-ABSENCE — Get registry → body has NO globalSlug/displayName/top-level visibility/infra fields",
      "description": "class:CONF | priority:P1",
      "item": [
        {
          "name": "get-absence",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _rc = parseInt(pm.environment.get('_rdRetry') || '0', 10);",
                  "if ((pm.response.code === 403 || pm.response.code === 404) && _rc < 20) {",
                  "  pm.environment.set('_rdRetry', String(_rc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* EC read-your-writes wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_rdRetry');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "const j = pm.response.json();",
                  "pm.test('no globalSlug (ban #15: URL carries immutable id, not a slug)', () => pm.expect(j).to.not.have.property('globalSlug'));",
                  "pm.test('no displayName (UI pretty-name lives in labels)', () => pm.expect(j).to.not.have.property('displayName'));",
                  "pm.test('no top-level visibility (authoritative gate is per-repo)', () => pm.expect(j).to.not.have.property('visibility'));",
                  "pm.test('no infra engineNamespace (two-projection, Internal* only)', () => pm.expect(j).to.not.have.property('engineNamespace'));",
                  "pm.test('no infra bucketPrefix (two-projection)', () => pm.expect(j).to.not.have.property('bucketPrefix'));",
                  "pm.test('no infra numericInfraId (two-projection)', () => pm.expect(j).to.not.have.property('numericInfraId'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F1-NEG-ID-IMMUTABLE — Update updateMask=id → 400 (id immutable); POST :rename → route absent (no id-rename)",
      "description": "class:NEG | class:CONF | priority:P0",
      "item": [
        {
          "name": "update-id",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"updateMask\": \"id\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('grpc code 3 (INVALID_ARGUMENT)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(3);",
                  "});",
                  "pm.test('id immutable text', () => pm.expect(pm.response.json().message).to.eql('id is immutable after Registry.Create'));"
                ]
              }
            }
          ]
        },
        {
          "name": "post-rename-absent",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}:rename",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}:rename"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('no :rename verb on Registry (never 200 success)', () => pm.expect(pm.response.code).to.be.oneOf([400, 403, 404, 405, 501]));",
                  "let _d; try { _d = pm.response.json(); } catch (e) { _d = null; }",
                  "pm.test('403 here must be an UNCATALOGUED-method denial, not a permission check on a live route', () => {",
                  "  if (pm.response.code !== 403) return;",
                  "  const types = [];",
                  "  ((_d && _d.details) || []).forEach(d => ((d && d.violations) || []).forEach(v => types.push(v && v.type)));",
                  "  pm.expect(types, JSON.stringify(_d)).to.include('authz.catalog');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F2-RENAME-STABLE-ID — Rename name via Update → name changes, id/endpoint unchanged (URL/pull addressed by immutable id)",
      "description": "class:CRUD | class:CONF | priority:P0",
      "item": [
        {
          "name": "create-rnRegId",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"name\": \"rename-old-{{runId}}\", \"projectId\": \"{{existingProjectId}}\", \"regionId\": \"{{existingRegionId}}\", \"description\": \"REG-1 redesign coverage\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-2",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "capture-rnRegId-rya15",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('setup op ok (no error)', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "const r = (pm.response.json().response) || {};",
                  "pm.test('id prefix reg', () => pm.expect((r.id||'').startsWith('reg')).to.be.true);",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = ((j.response&&j.response.id)||'');",
                  "  if (v !== undefined && v !== null) pm.environment.set('rnRegId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "warm-rnRegId-rya2",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rnRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rnRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));"
                ]
              }
            }
          ]
        },
        {
          "name": "get-before-rename",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rnRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rnRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _rc = parseInt(pm.environment.get('_rdRetry') || '0', 10);",
                  "if ((pm.response.code === 403 || pm.response.code === 404) && _rc < 20) {",
                  "  pm.environment.set('_rdRetry', String(_rc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* EC read-your-writes wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_rdRetry');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "const j = pm.response.json();",
                  "pm.environment.set('rnEndpointBefore', j.endpoint||'');",
                  "pm.test('endpoint derived by id (contains id)', () => pm.expect(j.endpoint||'').to.include(pm.environment.get('rnRegId')));"
                ]
              }
            }
          ]
        },
        {
          "name": "rename-rya16",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rnRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rnRegId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"updateMask\": \"name\", \"name\": \"rename-new-{{runId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-3",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "get-after-rename-rya17",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rnRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rnRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('rename op ok (no error)', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "const j = pm.response.json();",
                  "pm.test('name mutated', () => pm.expect(j.name).to.eql('rename-new-'+pm.environment.get('runId')));",
                  "pm.test('id unchanged (identity anchor stable)', () => pm.expect(j.id).to.eql(pm.environment.get('rnRegId')));",
                  "pm.test('endpoint unchanged (derived by id, not name)', () => pm.expect(j.endpoint||'').to.eql(pm.environment.get('rnEndpointBefore')));"
                ]
              }
            }
          ]
        },
        {
          "name": "delete-rnRegId-rya18",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rnRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rnRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.environment.set('opId', '');",
                  "pm.test('delete accepted (200/404)', () => pm.expect(pm.response.code).to.be.oneOf([200, 404]));",
                  "const j = pm.response.json(); if (j && j.id) pm.environment.set('opId', String(j.id));"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-4",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));",
                  "// ИСХОД УДАЛЕНИЯ, А НЕ ТОЛЬКО ЕГО ЗАВЕРШЕНИЕ: операция, завершившаяся",
                  "// ошибкой, тоже done — без этого утверждения отказ удаления читается как",
                  "// успех, ресурс остаётся жить, а вердикт не меняется.",
                  "(function () {",
                  "  var _do; try { _do = pm.response.json(); } catch (e) { return; }",
                  "  pm.test('delete operation succeeded (no operation.error)', function () {",
                  "    pm.expect(_do.error && JSON.stringify(_do.error), 'operation.error').to.eql(undefined);",
                  "  });",
                  "})();"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F5-INHERIT-PRIVATE — defaultRepositoryVisibility PRIVATE seeds new Repository.visibility when visibility omitted",
      "description": "class:CRUD | priority:P1",
      "item": [
        {
          "name": "get-default-vis",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _rc = parseInt(pm.environment.get('_rdRetry') || '0', 10);",
                  "if ((pm.response.code === 403 || pm.response.code === 404) && _rc < 20) {",
                  "  pm.environment.set('_rdRetry', String(_rc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* EC read-your-writes wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_rdRetry');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('defaultRepositoryVisibility PRIVATE (fail-safe default)', () => pm.expect(pm.response.json().defaultRepositoryVisibility).to.eql('PRIVATE'));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-create-rya3",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"repository\": \"inherit/vis-{{runId}}\", \"description\": \"redesign repo\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-5",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-capture-rya19",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('createRepo op ok (no error)', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "const r = (pm.response.json().response) || {};",
                  "pm.test('new repo inherits visibility PRIVATE', () => pm.expect(r.visibility).to.eql('PRIVATE'));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-warm-rya4",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories/inherit/vis-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories",
                "inherit",
                "vis-{{runId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F5-NEG-PUBLIC-ADMIN-GATE — Non-admin drives defaultRepositoryVisibility→PUBLIC (masked AND empty-mask full-PATCH) → 403 PERMISSION_DENIED; description-only Update → OK",
      "description": "class:NEG | class:AZ | priority:P0",
      "item": [
        {
          "name": "update-default-public",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"updateMask\": \"defaultRepositoryVisibility\", \"defaultRepositoryVisibility\": \"PUBLIC\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 403', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(403));",
                  "pm.test('grpc code 7 (PERMISSION_DENIED)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(7);",
                  "});",
                  "pm.test('names required capability (registry admin)', () => pm.expect((pm.response.json().message||'').toLowerCase()).to.include('registry admin'));"
                ]
              }
            }
          ]
        },
        {
          "name": "update-default-public-emptymask",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"defaultRepositoryVisibility\": \"PUBLIC\", \"description\": \"sneak-{{runId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 403', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(403));",
                  "pm.test('grpc code 7 (PERMISSION_DENIED)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(7);",
                  "});",
                  "pm.test('empty-mask full-PATCH does not bypass the admin-gate', () => pm.expect((pm.response.json().message||'').toLowerCase()).to.include('registry admin'));"
                ]
              }
            }
          ]
        },
        {
          "name": "verify-still-private",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _rc = parseInt(pm.environment.get('_rdRetry') || '0', 10);",
                  "if ((pm.response.code === 403 || pm.response.code === 404) && _rc < 20) {",
                  "  pm.environment.set('_rdRetry', String(_rc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* EC read-your-writes wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_rdRetry');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('registry stays PRIVATE after denied flips', () => pm.expect(pm.response.json().defaultRepositoryVisibility).to.eql('PRIVATE'));"
                ]
              }
            }
          ]
        },
        {
          "name": "update-description-ok",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"updateMask\": \"description\", \"description\": \"editor-path-{{runId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.test('editor description-path not broken by narrow admin-gate', () => pm.expect(pm.response.json().id||'').to.match(/^(rop|reo)[a-z0-9]+$/));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F7-CREATE-DURABLE — CreateRepository (no lifecycle) → lifecycle DURABLE by default (survives-empty), tagCount 0",
      "description": "class:CRUD | priority:P0",
      "item": [
        {
          "name": "repo-create-rya5",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"repository\": \"durable/svc-{{runId}}\", \"description\": \"redesign repo\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-6",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-capture-rya20",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('createRepo op ok (no error)', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "const r = (pm.response.json().response) || {};",
                  "pm.test('lifecycle DURABLE (explicit intent-create default)', () => pm.expect(r.lifecycle).to.eql('DURABLE'));",
                  "pm.test('tagCount 0 (durable-empty survives)', () => pm.expect(Number(r.tagCount||0)).to.eql(0));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-warm-rya6",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories/durable/svc-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories",
                "durable",
                "svc-{{runId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F7-NEG-CREATE-EPHEMERAL — CreateRepository lifecycle=EPHEMERAL → 400 INVALID_ARGUMENT (accepted-and-ignored input refused; field named in details)",
      "description": "class:NEG | class:VAL | priority:P1",
      "item": [
        {
          "name": "repo-create-ephemeral",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"repository\": \"ephemeral/svc-{{runId}}\", \"description\": \"redesign repo\", \"lifecycle\": \"EPHEMERAL\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('grpc code 3 (INVALID_ARGUMENT)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(3);",
                  "});",
                  "pm.test('field violation on \"lifecycle\"', () => {",
                  "  const j = pm.response.json();",
                  "  const det = (j.details || []).find(d => (d['@type']||'').includes('BadRequest'));",
                  "  pm.expect(det, 'BadRequest detail').to.be.an('object');",
                  "  const fv = (det.fieldViolations || []).find(v => v.field === 'lifecycle');",
                  "  pm.expect(fv, 'fieldViolation for lifecycle').to.be.an('object');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-ephemeral-absent",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories/ephemeral/svc-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories",
                "ephemeral",
                "svc-{{runId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('refused create left no repository behind', () => pm.expect(pm.response.code).to.be.oneOf([403, 404]));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F7-CREATE-DURABLE-EXPLICIT — CreateRepository lifecycle=DURABLE (explicit) → lifecycle DURABLE (the implemented class is accepted)",
      "description": "class:CRUD | priority:P1",
      "item": [
        {
          "name": "repo-create-rya7",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"repository\": \"durexpl/svc-{{runId}}\", \"description\": \"redesign repo\", \"lifecycle\": \"DURABLE\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-7",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-capture-rya21",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('createRepo op ok (no error)', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "const r = (pm.response.json().response) || {};",
                  "pm.test('lifecycle DURABLE (explicit, honoured)', () => pm.expect(r.lifecycle).to.eql('DURABLE'));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-warm-rya8",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories/durexpl/svc-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories",
                "durexpl",
                "svc-{{runId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F7-NEG-LIFECYCLE-READONLY — UpdateRepository updateMask=lifecycle → 400 INVALID_ARGUMENT (lifecycle output-only, system-managed)",
      "description": "class:NEG | class:VAL | priority:P1",
      "item": [
        {
          "name": "repo-create-rya9",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"repository\": \"lifemask/svc-{{runId}}\", \"description\": \"redesign repo\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-8",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-capture-rya22",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('createRepo op ok (no error)', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "const r = (pm.response.json().response) || {};",
                  "pm.test('lifecycle DURABLE (setup)', () => pm.expect(r.lifecycle).to.eql('DURABLE'));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-warm-rya10",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories/lifemask/svc-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories",
                "lifemask",
                "svc-{{runId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));"
                ]
              }
            }
          ]
        },
        {
          "name": "update-lifecycle-mask",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories/lifemask/svc-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories",
                "lifemask",
                "svc-{{runId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"updateMask\": \"lifecycle\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('grpc code 3 (INVALID_ARGUMENT)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(3);",
                  "});",
                  "pm.test('lifecycle read-only text', () => pm.expect(pm.response.json().message).to.eql('lifecycle is read-only (system-managed)'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F6-NEG-REGISTRYID-IMMUTABLE — UpdateRepository updateMask=registryId → 400 INVALID_ARGUMENT (registryId immutable, natural key)",
      "description": "class:NEG | class:CONF | priority:P1",
      "item": [
        {
          "name": "repo-create-rya11",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"repository\": \"regidmask/svc-{{runId}}\", \"description\": \"redesign repo\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-9",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-capture-rya23",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('createRepo op ok (no error)', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "const r = (pm.response.json().response) || {};",
                  "pm.test('repo created (setup)', () => pm.expect(r.name||'').to.include('regidmask/svc-'));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-warm-rya12",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories/regidmask/svc-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories",
                "regidmask",
                "svc-{{runId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));"
                ]
              }
            }
          ]
        },
        {
          "name": "update-registryid-mask",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}/repositories/regidmask/svc-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}",
                "repositories",
                "regidmask",
                "svc-{{runId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"updateMask\": \"registryId\", \"description\": \"move attempt\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('grpc code 3 (INVALID_ARGUMENT)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(3);",
                  "});",
                  "pm.test('registryId immutable text', () => pm.expect(pm.response.json().message).to.eql('registryId is immutable after Repository.Create'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F8-EMPTY-MASK-IMMUTABLE-IGNORED — Empty updateMask → mutable applied (description/name); identity/placement anchors untouched (not expressible in Update)",
      "description": "class:CRUD | class:CONF | priority:P1",
      "item": [
        {
          "name": "create-emRegId",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"name\": \"emptymask-{{runId}}\", \"projectId\": \"{{existingProjectId}}\", \"regionId\": \"{{existingRegionId}}\", \"description\": \"REG-1 redesign coverage\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-10",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "capture-emRegId-rya24",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('setup op ok (no error)', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "const r = (pm.response.json().response) || {};",
                  "pm.test('id prefix reg', () => pm.expect((r.id||'').startsWith('reg')).to.be.true);",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = ((j.response&&j.response.id)||'');",
                  "  if (v !== undefined && v !== null) pm.environment.set('emRegId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "warm-emRegId-rya13",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{emRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{emRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));"
                ]
              }
            }
          ]
        },
        {
          "name": "update-emptymask-rya25",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{emRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{emRegId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"description\": \"patched-{{runId}}\", \"labels\": {\"team\": \"pay\"}, \"name\": \"emptymask-new-{{runId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-11",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "get-emptymask-rya26",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{emRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{emRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('empty-mask op ok (no error)', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "pm.test('status 200', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(200));",
                  "const j = pm.response.json();",
                  "pm.test('mutable description applied', () => pm.expect(j.description).to.eql('patched-'+pm.environment.get('runId')));",
                  "pm.test('mutable name applied (F2 cosmetic label)', () => pm.expect(j.name).to.eql('emptymask-new-'+pm.environment.get('runId')));",
                  "pm.test('immutable id untouched by full-object PATCH', () => pm.expect(j.id).to.eql(pm.environment.get('emRegId')));",
                  "pm.test('immutable regionId untouched by full-object PATCH', () => pm.expect(j.regionId).to.eql(pm.environment.get('existingRegionId')));"
                ]
              }
            }
          ]
        },
        {
          "name": "delete-emRegId-rya27",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{emRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{emRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.environment.set('opId', '');",
                  "pm.test('delete accepted (200/404)', () => pm.expect(pm.response.code).to.be.oneOf([200, 404]));",
                  "const j = pm.response.json(); if (j && j.id) pm.environment.set('opId', String(j.id));"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-12",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));",
                  "// ИСХОД УДАЛЕНИЯ, А НЕ ТОЛЬКО ЕГО ЗАВЕРШЕНИЕ: операция, завершившаяся",
                  "// ошибкой, тоже done — без этого утверждения отказ удаления читается как",
                  "// успех, ресурс остаётся жить, а вердикт не меняется.",
                  "(function () {",
                  "  var _do; try { _do = pm.response.json(); } catch (e) { return; }",
                  "  pm.test('delete operation succeeded (no operation.error)', function () {",
                  "    pm.expect(_do.error && JSON.stringify(_do.error), 'operation.error').to.eql(undefined);",
                  "  });",
                  "})();"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-F8-NEG-PAGESIZE-OVERMAX — List pageSize=1001 (> max 1000) → 400 INVALID_ARGUMENT (rejected not clamped, format-validate before authz)",
      "description": "class:NEG | class:BVA | class:VAL | priority:P1",
      "item": [
        {
          "name": "list-pagesize-overmax",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries?projectId={{existingProjectId}}&pageSize=1001",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries?projectId={{existingProjectId}}&pageSize=1001"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 400', () => pm.expect(pm.response.code, JSON.stringify(pm.response.text())).to.eql(400));",
                  "pm.test('grpc code 3 (INVALID_ARGUMENT)', () => {",
                  "  let j; try { j = pm.response.json(); } catch (e) { j = {}; }",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(3);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-RD-CLEANUP — Teardown — Delete shared {{rdRegId}} → poll (tolerant to prior removal)",
      "description": "class:IDEM | priority:P3",
      "item": [
        {
          "name": "delete-rdRegId",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{rdRegId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{rdRegId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.environment.set('opId', '');",
                  "pm.test('delete accepted (200/404)', () => pm.expect(pm.response.code).to.be.oneOf([200, 404]));",
                  "const j = pm.response.json(); if (j && j.id) pm.environment.set('opId', String(j.id));"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-13",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));",
                  "// ИСХОД УДАЛЕНИЯ, А НЕ ТОЛЬКО ЕГО ЗАВЕРШЕНИЕ: операция, завершившаяся",
                  "// ошибкой, тоже done — без этого утверждения отказ удаления читается как",
                  "// успех, ресурс остаётся жить, а вердикт не меняется.",
                  "(function () {",
                  "  var _do; try { _do = pm.response.json(); } catch (e) { return; }",
                  "  pm.test('delete operation succeeded (no operation.error)', function () {",
                  "    pm.expect(_do.error && JSON.stringify(_do.error), 'operation.error').to.eql(undefined);",
                  "  });",
                  "})();"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": []
}