{
  "info": {
    "_postman_id": "0af34f63-677b-582a-b4e4-5cc69a27a311",
    "name": "kacho-registry / newman / registry-authz",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "if (!pm.environment.get('runId') || pm.environment.get('runId') === '') {",
          "  const t = Date.now().toString(36);",
          "  const r = Math.floor(Math.random() * 1e9).toString(36);",
          "  pm.environment.set('runId', (t + r).replace(/[^a-z0-9]/g, '').slice(-10));",
          "}",
          "pm.environment.set('_suiteProjectId', pm.environment.get('existingProjectId'));",
          "pm.environment.set('_suiteProjectCrossId', pm.environment.get('existingProjectCrossId'));",
          "pm.environment.set('_suiteRegionId', pm.environment.get('existingRegionId'));",
          "pm.environment.set('_suiteRegionAltId', pm.environment.get('existingRegionAltId'));",
          "// Default auth: project-editor JWT on project A (sufficient for most happy-path steps).",
          "// Per-step auth= overrides via _auth_pre_script.",
          "const __defaultJwt = pm.environment.get('jwtProjectEditorA') || pm.variables.get('jwtProjectEditorA') || '';",
          "if (__defaultJwt && !pm.request.headers.has('Authorization')) {",
          "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __defaultJwt});",
          "}",
          "const __needed = ['baseUrl', 'existingProjectId', 'existingRegionId', 'jwtProjectEditorA'];",
          "const __missing = __needed.filter((k) => !(pm.environment.get(k) || pm.variables.get(k) || ''));",
          "if (__missing.length > 0) {",
          "  pm.test('FIXTURE REQUIRED: ' + __missing.join(', '), () => pm.expect.fail('the suite fixture seed did not run: ' + __missing.join(', ') + ' are empty. Seed via tests/authz-fixtures (prodrun.sh patches the suite env). An empty id is not a skip — it is sent as-is and the request is refused on authorization, so every later failure names the wrong cause.'));",
          "  pm.execution.skipRequest();",
          "}",
          "(function () {",
          "  var _u = '';",
          "  try { _u = pm.request.url.toString(); } catch (e) { return; }",
          "  var _all = _u.match(/\\{\\{[A-Za-z0-9_]+\\}\\}/g);",
          "  if (!_all) { return; }",
          "  var _n = null;",
          "  for (var _i = 0; _i < _all.length; _i++) {",
          "    var _c = _all[_i].slice(2, -2);",
          "    if (!pm.variables.has(_c)) { _n = _c; break; }",
          "  }",
          "  if (_n === null) { return; }",
          "  pm.test('предусловие: {{' + _n + '}} не было захвачено — запрос не отправлен', function () {",
          "    pm.expect.fail(_n + ' не определена ни в одной области. Мутация, которая должна была её захватить, не вернула Operation (была отвергнута), либо захват не состоялся. Отправка ушла бы литералом и повтор не сошёлся бы никогда.');",
          "  });",
          "  pm.execution.skipRequest();",
          "})();"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "REG-AZ-SETUP-FIXTURE — Setup: Create registry as project-editor → Operation → poll → capture regIdAz (prefix reg)",
      "description": "class:AZD | priority:P1",
      "item": [
        {
          "name": "create-fixture",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"name\": \"az-fixture-{{runId}}\", \"projectId\": \"{{existingProjectId}}\", \"regionId\": \"{{existingRegionId}}\", \"description\": \"authz coverage fixture\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-1",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "capture-fixture-id-rya4",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code).to.eql(200));",
                  "pm.test('setup op ok', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = ((j.response&&j.response.id)||'');",
                  "  if (v !== undefined && v !== null) pm.environment.set('regIdAz', String(v));",
                  "} catch (e) {}",
                  "pm.test('fixture regId captured (prefix reg)', () => pm.expect((pm.environment.get('regIdAz')||'').startsWith('reg')).to.be.true);"
                ]
              }
            }
          ]
        },
        {
          "name": "reg-warm-rya1",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code).to.eql(200));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-AZ-GET-STRANGER-HIDDEN — Get as jwtStranger: существующий regId и well-formed отсутствующий отвечают побайтово одинаково (модуло сам id) → denied (401/403/404, never 200), no deny_reasons (gated !=401)",
      "description": "class:AZD | class:NEG | priority:P0",
      "item": [
        {
          "name": "get-stranger-absent",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/reg-DOESNOTEXIST00000",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "reg-DOESNOTEXIST00000"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtStranger'",
                  "const __t = pm.environment.get('jwtStranger') || pm.variables.get('jwtStranger') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtStranger is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtStranger is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('denied (401/403/404), never 200', () => pm.expect(pm.response.code).to.be.oneOf([401, 403, 404]));",
                  "pm.environment.set('_azHideMissBody', pm.response.text().split('reg-DOESNOTEXIST00000').join('<ID>'));",
                  "pm.environment.set('_azHideMissCode', String(pm.response.code));"
                ]
              }
            }
          ]
        },
        {
          "name": "get-stranger",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtStranger'",
                  "const __t = pm.environment.get('jwtStranger') || pm.variables.get('jwtStranger') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtStranger is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtStranger is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('denied (401/403/404), never 200', () => pm.expect(pm.response.code).to.be.oneOf([401, 403, 404]));",
                  "const _rid = pm.environment.get('regIdAz') || '';",
                  "const _missBody = pm.environment.get('_azHideMissBody');",
                  "const _missCode = pm.environment.get('_azHideMissCode');",
                  "pm.test('the absent-id probe recorded a comparison base', () => {",
                  "  pm.expect(_rid, 'regIdAz is empty — nothing to compare').to.not.equal('');",
                  "  pm.expect(_missBody, 'the well-formed-absent probe did not record a body').to.be.a('string');",
                  "});",
                  "if (_rid && typeof _missBody === 'string') {",
                  "  const _seen = pm.response.text().split(_rid).join('<ID>');",
                  "  pm.test('hiding an existing registry is byte-identical to a genuine miss (no existence oracle)',",
                  "    () => pm.expect(_seen, 'existing: ' + pm.response.text() + ' | absent: ' + _missBody).to.equal(_missBody));",
                  "  pm.test('and indistinguishable by status code too',",
                  "    () => pm.expect(String(pm.response.code)).to.equal(_missCode));",
                  "}",
                  "if (pm.response.code === 404) {",
                  "  pm.test('the refusal carries the platform not-found tone with the caller-supplied id',",
                  "    () => pm.expect((pm.response.json() || {}).message).to.equal('Registry ' + _rid + ' not found'));",
                  "}",
                  "if (pm.response.code !== 401) {",
                  "  pm.test('authenticated deny -> no resource-existence leak', () => pm.expect(JSON.stringify(pm.response.json())).to.not.include('deny_reasons'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-AZ-GET-VIEWER-OK — Get as jwtProjectViewerA on existing regId → 200 (viewer has v_get) — positive control (fixture-gated)",
      "description": "class:AZD | priority:P1",
      "item": [
        {
          "name": "get-viewer",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtProjectViewerA'",
                  "const __t = pm.environment.get('jwtProjectViewerA') || pm.variables.get('jwtProjectViewerA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtProjectViewerA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtProjectViewerA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _viewer = pm.environment.get('jwtProjectViewerA') || '';",
                  "if (!_viewer) {",
                  "  pm.test('FIXTURE REQUIRED: jwtProjectViewerA (viewer-tier boundary NOT verified)', () => pm.expect.fail('jwtProjectViewerA is empty: the viewer subject was never seeded, so this case exercised NO viewer at all. Seed it via tests/authz-fixtures (prodseed_matrix.py writes the token into the suite env). Absence of a fixture is a refusal, not a skip.'));",
                  "  return;",
                  "}",
                  "const _n = parseInt(pm.environment.get('_azViewerRetry') || '0', 10);",
                  "if (pm.response.code === 404 && _n < 20) {",
                  "  pm.environment.set('_azViewerRetry', String(_n + 1));",
                  "  const _ipd1 = Date.now(); while (Date.now() - _ipd1 < 500) void 0; /* real inter-poll delay: cap 20 x 500ms ~= 10s budget (testing.md) */",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_azViewerRetry');",
                  "pm.test('status 200', () => pm.expect(pm.response.code).to.eql(200));",
                  "const j = pm.response.json();",
                  "pm.test('viewer sees fixture (v_get)', () => pm.expect(j.id).to.eql(pm.environment.get('regIdAz')));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-AZ-LIST-STRANGER-EMPTY — List as jwtStranger for existingProjectId → 200 + ПУСТОЙ registries (prod-посев) ЛИБО 401/16 (dev-посев); never reveals regId",
      "description": "class:AZD | class:NEG | priority:P0",
      "item": [
        {
          "name": "list-stranger",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries?projectId={{existingProjectId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries?projectId={{existingProjectId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtStranger'",
                  "const __t = pm.environment.get('jwtStranger') || pm.variables.get('jwtStranger') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtStranger is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtStranger is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.response.code === 200) {",
                  "  pm.test('non-member sees an EMPTY registries page (scope-filtered rows)', () => {",
                  "    const _arr = pm.response.json().registries;",
                  "    pm.expect(_arr, JSON.stringify(pm.response.json())).to.be.an('array');",
                  "    pm.expect(_arr.length, 'non-member must see nothing (LEAK!): ' + pm.response.text()).to.eql(0);",
                  "  });",
                  "} else {",
                  "  pm.test('unauthenticated subject is refused (401, grpc 16)', () => {",
                  "    pm.expect(pm.response.code, pm.response.text()).to.eql(401);",
                  "    let _j; try { _j = pm.response.json(); } catch (e) { _j = null; }",
                  "    pm.expect(_j && _j.code, JSON.stringify(_j)).to.eql(16);",
                  "  });",
                  "}",
                  "const _rid = pm.environment.get('regIdAz') || '';",
                  "if (_rid) pm.test('a listing never names a registry the caller did not ask about', () => pm.expect(pm.response.text()).to.not.include(_rid));",
                  "if (pm.response.code !== 401) {",
                  "  pm.test('authenticated deny -> no resource-existence leak', () => pm.expect(JSON.stringify(pm.response.json())).to.not.include('deny_reasons'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-AZ-UPDATE-VIEWER-DENY — Update as jwtProjectViewerA (no v_update) → 403/404 (existence-hidden); no deny_reasons (fixture-gated)",
      "description": "class:AZD | class:NEG | priority:P0",
      "item": [
        {
          "name": "update-viewer",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"updateMask\": \"description\", \"description\": \"viewer-edit-{{runId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtProjectViewerA'",
                  "const __t = pm.environment.get('jwtProjectViewerA') || pm.variables.get('jwtProjectViewerA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtProjectViewerA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtProjectViewerA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _viewer = pm.environment.get('jwtProjectViewerA') || '';",
                  "if (!_viewer) {",
                  "  pm.test('FIXTURE REQUIRED: jwtProjectViewerA (viewer-tier boundary NOT verified)', () => pm.expect.fail('jwtProjectViewerA is empty: the viewer subject was never seeded, so this case exercised NO viewer at all. Seed it via tests/authz-fixtures (prodseed_matrix.py writes the token into the suite env). Absence of a fixture is a refusal, not a skip.'));",
                  "  return;",
                  "}",
                  "pm.test('denied 403/404 (no v_update)', () => pm.expect(pm.response.code).to.be.oneOf([403, 404]));",
                  "if (pm.response.code !== 401) {",
                  "  pm.test('authenticated deny -> no resource-existence leak', () => pm.expect(JSON.stringify(pm.response.json())).to.not.include('deny_reasons'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-AZ-DELETE-VIEWER-DENY — Delete as jwtProjectViewerA (no v_delete) → 403/404 (existence-hidden); no deny_reasons (fixture-gated)",
      "description": "class:AZD | class:NEG | priority:P0",
      "item": [
        {
          "name": "delete-viewer",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtProjectViewerA'",
                  "const __t = pm.environment.get('jwtProjectViewerA') || pm.variables.get('jwtProjectViewerA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtProjectViewerA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtProjectViewerA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _viewer = pm.environment.get('jwtProjectViewerA') || '';",
                  "if (!_viewer) {",
                  "  pm.test('FIXTURE REQUIRED: jwtProjectViewerA (viewer-tier boundary NOT verified)', () => pm.expect.fail('jwtProjectViewerA is empty: the viewer subject was never seeded, so this case exercised NO viewer at all. Seed it via tests/authz-fixtures (prodseed_matrix.py writes the token into the suite env). Absence of a fixture is a refusal, not a skip.'));",
                  "  return;",
                  "}",
                  "pm.test('denied 403/404 (no v_delete)', () => pm.expect(pm.response.code).to.be.oneOf([403, 404]));",
                  "if (pm.response.code !== 401) {",
                  "  pm.test('authenticated deny -> no resource-existence leak', () => pm.expect(JSON.stringify(pm.response.json())).to.not.include('deny_reasons'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-AZ-CREATE-STRANGER-DENY — Create as jwtStranger in existingProjectId → denied (401/403/404, never 200); no deny_reasons (gated !=401)",
      "description": "class:AZD | class:NEG | priority:P0",
      "item": [
        {
          "name": "create-stranger",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"name\": \"az-intruder-{{runId}}\", \"projectId\": \"{{existingProjectId}}\", \"regionId\": \"{{existingRegionId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtStranger'",
                  "const __t = pm.environment.get('jwtStranger') || pm.variables.get('jwtStranger') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtStranger is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtStranger is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('denied (401/403/404), never 200', () => pm.expect(pm.response.code).to.be.oneOf([401, 403, 404]));",
                  "if (pm.response.code !== 401) {",
                  "  pm.test('authenticated deny -> no resource-existence leak', () => pm.expect(JSON.stringify(pm.response.json())).to.not.include('deny_reasons'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-AZ-UPDATE-STRANGER-DENY — Update as jwtStranger on existing regId → denied (401/403/404, never 200 success); no deny_reasons (gated !=401)",
      "description": "class:AZD | class:NEG | priority:P0",
      "item": [
        {
          "name": "update-stranger",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"updateMask\": \"description\", \"description\": \"x\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtStranger'",
                  "const __t = pm.environment.get('jwtStranger') || pm.variables.get('jwtStranger') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtStranger is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtStranger is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('denied 401/403/404 (stranger, never 200 success)', () => pm.expect(pm.response.code).to.be.oneOf([401, 403, 404]));",
                  "if (pm.response.code !== 401) {",
                  "  pm.test('authenticated deny -> no resource-existence leak', () => pm.expect(JSON.stringify(pm.response.json())).to.not.include('deny_reasons'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-AZ-DELETE-STRANGER-DENY — Delete as jwtStranger on existing regId → denied (401/403/404, never 200 success); no deny_reasons (gated !=401)",
      "description": "class:AZD | class:NEG | priority:P0",
      "item": [
        {
          "name": "delete-stranger",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtStranger'",
                  "const __t = pm.environment.get('jwtStranger') || pm.variables.get('jwtStranger') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtStranger is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtStranger is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('denied 401/403/404 (stranger, never 200 success)', () => pm.expect(pm.response.code).to.be.oneOf([401, 403, 404]));",
                  "if (pm.response.code !== 401) {",
                  "  pm.test('authenticated deny -> no resource-existence leak', () => pm.expect(JSON.stringify(pm.response.json())).to.not.include('deny_reasons'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-AZ-GET-ANON-401 — anonymous Get on existing regId → 401 (authN required, no bearer)",
      "description": "class:AZD | class:NEG | priority:P0",
      "item": [
        {
          "name": "get-anon",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: anonymous (strip Authorization header)",
                  "pm.request.headers.remove('Authorization');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 401', () => pm.expect(pm.response.code).to.eql(401));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REPO-AZ-SETUP — Setup: CreateRepository durable overlay repo under {{regIdAz}} (editor) → poll → capture",
      "description": "class:AZD | priority:P1",
      "item": [
        {
          "name": "repo-az-create-rya2",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}/repositories",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}",
                "repositories"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"repository\": \"az-repo-{{runId}}\", \"description\": \"per-repo authz fixture\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-2",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-az-confirm-rya5",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code).to.eql(200));",
                  "pm.test('repo setup op ok (no error)', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));"
                ]
              }
            }
          ]
        },
        {
          "name": "repo-az-warm-rya3",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}/repositories/az-repo-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}",
                "repositories",
                "az-repo-{{runId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// bounded read-your-writes retry over the owner-tuple materialization window",
                  "// (eventual-consistency); retries SELF only on 403/404 of own fresh resource.",
                  "// ПОЛОСА — синхронный отказ ЭТОГО шага. У асинхронной мутации отказ ВЛАДЕЛЬЦА",
                  "// приезжает внутри Operation и сюда НЕ попадает: нужен прогрев чтением.",
                  "",
                  "// ЖДАТЬ МОЖНО ТОЛЬКО ПРАВА, А НЕ ИМЯ. Если адрес шага собран из переменной,",
                  "// которую предыдущий шаг не захватил, в пути стоит пустой сегмент либо сама",
                  "// подстановка — и запрос спрашивает НЕ О РЕСУРСЕ. Окно видимости прав такой",
                  "// адрес не наполнит никогда, поэтому повтор здесь — ожидание, которое не может",
                  "// сработать: та самая форма без содержания, которую эта функция уже отказывается",
                  "// строить выше (вырожденный retry_on). Хуже того, отказ по пустому адресу",
                  "// приходит кодом 403 — он в полосе ожидания, и шаг выжигает ВЕСЬ бюджет.",
                  "// Замер прогона 31951162447, часть registry: 1863 запроса из 3903 (48%) ушли",
                  "// по пустому сегменту в 23 обёрнутых шагах — около 18 минут стенда на вопросы",
                  "// ни о чём, при этом ни одно утверждение о продукте не проверялось.",
                  "// Отказ здесь называет ПРЕДМЕТ (переменная не захвачена), а не следствие",
                  "// («ожидал 200, получил 403») — падение уборки виновника не называет.",
                  "const _rpath = ((pm.request.url && pm.request.url.path) || []).map(function (s) {",
                  "  return String(s && s.value !== undefined ? s.value : s);",
                  "});",
                  "const _rblank = _rpath.some(function (v) { return v === '' || /\\{\\{[^}]*\\}\\}/.test(v); });",
                  "if (_rblank) {",
                  "  pm.environment.unset('_authRetryCount');",
                  "  pm.environment.unset('_authRetryStarted');",
                  "  pm.test('step addresses a captured variable', function () {",
                  "    pm.expect.fail('адрес шага собран из НЕЗАХВАЧЕННОЙ переменной (/' + _rpath.join('/') +",
                  "      '): предыдущий шаг не захватил её, повторять нечего — ждать можно право на существующее имя, а не появление самого имени');",
                  "  });",
                  "}",
                  "if (pm.environment.get('_authRetryStarted') !== pm.info.requestName) {",
                  "  pm.environment.set('_authRetryCount', '0');",
                  "  pm.environment.set('_authRetryStarted', pm.info.requestName);",
                  "}",
                  "const _arc = parseInt(pm.environment.get('_authRetryCount') || '0', 10);",
                  "if (!_rblank && [403,404].includes(pm.response.code) && _arc < 80) {",
                  "  pm.environment.set('_authRetryCount', String(_arc + 1));",
                  "  const _ard = Date.now(); while (Date.now() - _ard < 600) { /* owner-tuple materialization wait */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_authRetryCount');",
                  "pm.environment.unset('_authRetryStarted');",
                  "pm.test('status 200', () => pm.expect(pm.response.code).to.eql(200));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REPO-AZ-GET-STRANGER-HIDDEN — GetRepository as jwtStranger → denied/hidden (401/403/404), never 200-success; no leak",
      "description": "class:AZD | class:NEG | priority:P0",
      "item": [
        {
          "name": "repo-get-stranger",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}/repositories/az-repo-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}",
                "repositories",
                "az-repo-{{runId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtStranger'",
                  "const __t = pm.environment.get('jwtStranger') || pm.variables.get('jwtStranger') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtStranger is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtStranger is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('denied (401/403/404), never 200 success', () => pm.expect(pm.response.code).to.be.oneOf([401, 403, 404]));",
                  "if (pm.response.code !== 401) {",
                  "  pm.test('authenticated deny -> no resource-existence leak', () => pm.expect(JSON.stringify(pm.response.json())).to.not.include('deny_reasons'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REPO-AZ-GET-VIEWER-OK — GetRepository as jwtProjectViewerA (v_get) → 200 (positive control, fixture-gated)",
      "description": "class:AZD | priority:P1",
      "item": [
        {
          "name": "repo-get-viewer",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}/repositories/az-repo-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}",
                "repositories",
                "az-repo-{{runId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtProjectViewerA'",
                  "const __t = pm.environment.get('jwtProjectViewerA') || pm.variables.get('jwtProjectViewerA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtProjectViewerA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtProjectViewerA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _viewer = pm.environment.get('jwtProjectViewerA') || '';",
                  "if (!_viewer) {",
                  "  pm.test('FIXTURE REQUIRED: jwtProjectViewerA (viewer-tier boundary NOT verified)', () => pm.expect.fail('jwtProjectViewerA is empty: the viewer subject was never seeded, so this case exercised NO viewer at all. Seed it via tests/authz-fixtures (prodseed_matrix.py writes the token into the suite env). Absence of a fixture is a refusal, not a skip.'));",
                  "  return;",
                  "}",
                  "const _n = parseInt(pm.environment.get('_azRepoViewerRetry') || '0', 10);",
                  "if (pm.response.code === 404 && _n < 20) {",
                  "  pm.environment.set('_azRepoViewerRetry', String(_n + 1));",
                  "  const _ipd2 = Date.now(); while (Date.now() - _ipd2 < 500) void 0; /* real inter-poll delay: cap 20 x 500ms ~= 10s budget (testing.md) */",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_azRepoViewerRetry');",
                  "pm.test('status 200', () => pm.expect(pm.response.code).to.eql(200));",
                  "pm.test('viewer sees repo (v_get)', () => pm.expect(pm.response.json().name).to.eql('az-repo-'+pm.environment.get('runId')));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REPO-AZ-UPDATE-VIEWER-DENY — UpdateRepository as jwtProjectViewerA (no v_update) → 403/404 (existence-hidden); no leak (fixture-gated)",
      "description": "class:AZD | class:NEG | priority:P0",
      "item": [
        {
          "name": "repo-update-viewer",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}/repositories/az-repo-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}",
                "repositories",
                "az-repo-{{runId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"updateMask\": \"description\", \"description\": \"viewer-edit-{{runId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtProjectViewerA'",
                  "const __t = pm.environment.get('jwtProjectViewerA') || pm.variables.get('jwtProjectViewerA') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtProjectViewerA is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtProjectViewerA is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _viewer = pm.environment.get('jwtProjectViewerA') || '';",
                  "if (!_viewer) {",
                  "  pm.test('FIXTURE REQUIRED: jwtProjectViewerA (viewer-tier boundary NOT verified)', () => pm.expect.fail('jwtProjectViewerA is empty: the viewer subject was never seeded, so this case exercised NO viewer at all. Seed it via tests/authz-fixtures (prodseed_matrix.py writes the token into the suite env). Absence of a fixture is a refusal, not a skip.'));",
                  "  return;",
                  "}",
                  "pm.test('denied 403/404 (no v_update), never 200 op', () => pm.expect(pm.response.code).to.be.oneOf([403, 404]));",
                  "if (pm.response.code !== 401) {",
                  "  pm.test('authenticated deny -> no resource-existence leak', () => pm.expect(JSON.stringify(pm.response.json())).to.not.include('deny_reasons'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REPO-AZ-DELETE-STRANGER-DENY — DeleteRepository as jwtStranger → denied (401/403/404), never 200 op-success; fixture untouched",
      "description": "class:AZD | class:NEG | priority:P0",
      "item": [
        {
          "name": "repo-delete-stranger",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}/repositories/az-repo-{{runId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}",
                "repositories",
                "az-repo-{{runId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtStranger'",
                  "const __t = pm.environment.get('jwtStranger') || pm.variables.get('jwtStranger') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtStranger is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtStranger is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('denied (401/403/404), never 200 op-success', () => pm.expect(pm.response.code).to.be.oneOf([401, 403, 404]));",
                  "if (pm.response.code !== 401) {",
                  "  pm.test('authenticated deny -> no resource-existence leak', () => pm.expect(JSON.stringify(pm.response.json())).to.not.include('deny_reasons'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REPO-AZ-CREATE-STRANGER-HIDDEN — CreateRepository as jwtStranger in {{regIdAz}} → denied (401/403/404), never 200 (namespace call-gate, X04)",
      "description": "class:AZD | class:NEG | priority:P1",
      "item": [
        {
          "name": "repo-create-stranger",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}/repositories",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}",
                "repositories"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"repository\": \"intruder/svc-{{runId}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtStranger'",
                  "const __t = pm.environment.get('jwtStranger') || pm.variables.get('jwtStranger') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtStranger is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtStranger is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('denied (401/403/404), never 200', () => pm.expect(pm.response.code).to.be.oneOf([401, 403, 404]));",
                  "if (pm.response.code !== 401) {",
                  "  pm.test('authenticated deny -> no resource-existence leak', () => pm.expect(JSON.stringify(pm.response.json())).to.not.include('deny_reasons'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-AZ-HIDE-EXISTENCE-BYTE-IDENTITY — Registry deny-404 format byte-identical to absent-miss 404 (security.md #6, no existence-oracle)",
      "description": "class:AZD | class:NEG | class:CONF | priority:P1",
      "item": [
        {
          "name": "reg-miss-absent",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/reg00000000000000000",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "reg00000000000000000"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 404', () => pm.expect(pm.response.code).to.eql(404));",
                  "pm.test('grpc code 5 (NOT_FOUND)', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(5);",
                  "});",
                  "pm.environment.set('regMissMsg', pm.response.json().message||'');"
                ]
              }
            }
          ]
        },
        {
          "name": "reg-deny-stranger",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// AZD per-step: bearer from env 'jwtStranger'",
                  "const __t = pm.environment.get('jwtStranger') || pm.variables.get('jwtStranger') || '';",
                  "if (__t) {",
                  "  pm.request.headers.upsert({key: 'Authorization', value: 'Bearer ' + __t});",
                  "} else {",
                  "  pm.test('harness config: jwtStranger is set (subject under test)', () => {",
                  "    pm.expect.fail('jwtStranger is not set — the authz-fixture seed (tests/authz-fixtures/setup.sh) did not provide this subject. Running the step anonymously would test a DIFFERENT principal and pass for the wrong reason.');",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('stranger denied (401/403/404), never 200', () => pm.expect(pm.response.code).to.be.oneOf([401, 403, 404]));",
                  "if (pm.response.code !== 404) {",
                  "  console.log('byte-identity comparison applies to the 404 shape only; this stand answered '+pm.response.code+' (authz-first ordering) — the deny/miss format pair is not comparable here');",
                  "  return;",
                  "}",
                  "pm.test('grpc code 5 (NOT_FOUND)', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(5);",
                  "});",
                  "const _absId = 'reg00000000000000000';",
                  "const _regIdAz = pm.environment.get('regIdAz') || '';",
                  "const _norm = (pm.response.json().message||'').split(_regIdAz).join(_absId);",
                  "pm.test('deny-404 format byte-identical to absent-miss (no existence-oracle / no FGA-type leak)', () => pm.expect(_norm).to.eql(pm.environment.get('regMissMsg')));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REG-AZ-CLEANUP-FIXTURE — Cleanup: Delete fixture registry as project-editor (LAST) → Operation → poll → Get 404 byte-identical to a genuine miss",
      "description": "class:AZD | class:CONF | priority:P2",
      "item": [
        {
          "name": "capture-genuine-miss",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/reg00000000000000000",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "reg00000000000000000"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 404', () => pm.expect(pm.response.code).to.eql(404));",
                  "pm.test('grpc code 5 (NOT_FOUND)', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(5);",
                  "});",
                  "const _m = (pm.response.json().message)||'';",
                  "pm.test('genuine miss names the resource and echoes the id', () => pm.expect(_m).to.eql('Registry reg00000000000000000 not found'));",
                  "pm.environment.set('regGenuineMissMsg', _m);"
                ]
              }
            }
          ]
        },
        {
          "name": "delete-fixture",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status 200', () => pm.expect(pm.response.code).to.eql(200));",
                  "pm.test('Operation envelope returned', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.id, 'operation.id').to.match(/^(rop|reo)[a-z0-9]+$/);",
                  "  pm.expect(j.metadata, 'operation.metadata').to.be.an('object');",
                  "});",
                  "pm.environment.unset('opId');",
                  "pm.environment.set('opId', '');",
                  "try {",
                  "  const j = pm.response.json();",
                  "  const v = (j.id);",
                  "  if (v !== undefined && v !== null) pm.environment.set('opId', String(v));",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "poll-op-3",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/operations/{{opId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "operations",
                "{{opId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('opId')) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "pm.test('poll status 200', () => pm.expect(pm.response.code, pm.response.text()).to.eql(200));",
                  "if (pm.response.code !== 200) {",
                  "  pm.environment.unset('_pollCount');",
                  "  return;",
                  "}",
                  "const j = pm.response.json();",
                  "const pc = parseInt(pm.environment.get('_pollCount') || '0', 10);",
                  "if (!j.done && pc < 60) {",
                  "  pm.environment.set('_pollCount', String(pc + 1));",
                  "  const _pd = Date.now(); while (Date.now() - _pd < 500) { /* inter-poll delay ~500ms (Koren #1) */ }",
                  "  pm.execution.setNextRequest(pm.info.requestName);",
                  "  return;",
                  "}",
                  "pm.environment.unset('_pollCount');",
                  "pm.test('operation done', () => pm.expect(j.done, JSON.stringify(j)).to.eql(true));",
                  "if (j.error) pm.environment.set('lastOpError', JSON.stringify(j.error));",
                  "else pm.environment.unset('lastOpError');",
                  "if (j.response) pm.environment.set('lastOpResponse', JSON.stringify(j.response));",
                  "// ИСХОД УДАЛЕНИЯ, А НЕ ТОЛЬКО ЕГО ЗАВЕРШЕНИЕ: операция, завершившаяся",
                  "// ошибкой, тоже done — без этого утверждения отказ удаления читается как",
                  "// успех, ресурс остаётся жить, а вердикт не меняется.",
                  "(function () {",
                  "  var _do; try { _do = pm.response.json(); } catch (e) { return; }",
                  "  pm.test('delete operation succeeded (no operation.error)', function () {",
                  "    pm.expect(_do.error && JSON.stringify(_do.error), 'operation.error').to.eql(undefined);",
                  "  });",
                  "})();"
                ]
              }
            }
          ]
        },
        {
          "name": "confirm-deleted",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/registry/v1/registries/{{regIdAz}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "registry",
                "v1",
                "registries",
                "{{regIdAz}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('cleanup op ok', () => pm.expect(pm.environment.get('lastOpError')||'').to.eql(''));",
                  "pm.test('status 404', () => pm.expect(pm.response.code).to.eql(404));",
                  "pm.test('grpc code 5 (NOT_FOUND)', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j.code, JSON.stringify(j)).to.eql(5);",
                  "});",
                  "const _abs = 'reg00000000000000000';",
                  "const _norm = ((pm.response.json().message)||'').split(pm.environment.get('regIdAz')).join(_abs);",
                  "pm.test('deleted-registry 404 is byte-identical to a genuine miss (no existence-oracle)', () => pm.expect(_norm).to.eql(pm.environment.get('regGenuineMissMsg')));"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": []
}