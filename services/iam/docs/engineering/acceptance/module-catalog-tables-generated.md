<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: BUSL-1.1
-->

# Приёмка: таблицы типов порождаются, а гейт дрейфа остаётся

- **Статус:** DRAFT — круг 1, вердикт ставит `acceptance-reviewer`. Кодирование
  запрещено до `APPROVED` (запрет #1)
- **Ревизия измерения:** `2fb27446e` (`origin/main`; впереди — **0**, позади — **0**;
  `git status --porcelain` пуст). **Все** числа и координаты о дереве продукта сняты
  на ней. Единица счёта названа рядом с каждым числом
- **Ревизия воркспейса:** `78c9af5` — отдельный репозиторий; координаты `scripts/` и
  `.claude/` накрываются ею, а не продуктовой
- **Задача:** `PRO-Robotech/kacho#1092` (P1, `size:L`, `area:iam`, `release:modules`) —
  метки перемерены `gh issue view`
- **Эпик:** `PRO-Robotech/kacho#1027` — каталог модуля как данные. Решения линии,
  которые эта приёмка НЕ переоткрывает: `#1104` (ветка **A** — модульные типы
  рендерятся блоками модели), `#1087` (исход **B** — раздел `resources` порождается
  из аннотаций контрактов). `#1029` закрыт `wontfix`
- **Тип изменения:** ЗАМЕЩАЮЩЕЕ ИСТОЧНИК + НАБЛЮДАЕМОСТЬ. Ни одного нового поля
  контракта, ни одного нового входа. Меняется **происхождение** двух таблиц Go;
  утверждение гейта дрейфа сохраняется дословно и получает перепись по популяциям
- **Сервис:** `kacho-iam`. Предмет целиком внутри него, поэтому документ живёт рядом
  с кодом. **`proto/` НЕ затрагивается**: канонический `fga_model.fga` остаётся
  рукописным для платформенных типов
- **Миграции:** изменение их **не требует** (§2.8). Заведёт реализация миграцию —
  вместе с ней обязательна запись в `docs/acceptance-ledger.yaml`
- **Блокер, установленный замером:** раздел `resources` манифеста в дереве **не
  существует** (§0.6). Владелец схемы — `#1778`, OPEN. Работа по этой приёмке
  начинается **после** посадки схемы раздела

---

## 0. Замер посылок задачи — в обе стороны

Тело задачи есть **гипотеза**, а не факт. Из шести проверяемых утверждений `#1092`
**подтвердились два, не подтвердились четыре**, и одно из неподтвердившихся —
несущее: формулировка «замена ничего не ломает» неверна в обе стороны сразу.

### 0.1. Подтвердилось

| # | утверждение задачи | чем подтверждено |
|---|---|---|
| **П1** | `validResourceTypes` (23 имени): четырёх модель не объявляет вовсе, десяти каталожных типов не хватает | предикат ниже даёт ровно эти числа и ровно эти имена |
| **П2** | перечни имён типов живут в Go рукописно | четыре словаря, координаты в §1.2 |

Предикат П1 (единица: **ключ отображения Go**), прогнан на `2fb27446e`:

```
python3 - <<'PY'
import re,subprocess
sh=lambda c: subprocess.run(c,shell=True,capture_output=True,text=True).stdout
model=set(re.findall(r'^type (\S+)',sh("cat services/iam/internal/authzmodel/fga_model.fga"),re.M))
ft=sh("awk '/^var objectTypes = map/,/^}/' services/iam/internal/authzmap/fga_types.go")
cat=dict(re.findall(r'"([a-z]+\.[a-zA-Z]+)":\s+"([a-z_]+)"',ft))
vt=sh("awk '/^var validResourceTypes = map/,/^}/' services/iam/internal/domain/types.go")
vrt=set(re.findall(r'"([a-z_*]+)":',vt))
print("A:",sorted(vrt-model-{'*'}))
print("B:",sorted(set(cat.values())-vrt))
print("C:",sorted(model-set(cat.values())))
PY
```

```
A: ['iam_account', 'iam_project', 'loadbalancer_nlb', 'loadbalancer_target_group']
B: ['iam_access_binding', 'nlb_listener', 'nlb_network_load_balancer', 'nlb_target_group',
    'registry_registry', 'registry_repository', 'storage_image', 'storage_snapshot',
    'storage_volume', 'vpc_address_pool']
C: ['cluster', 'group', 'iam_fgaproxy', 'service_account', 'user']
```

**Четыре имени из «A» не производит НИЧТО.** Единица: строковый литерал в дереве.

```
for t in loadbalancer_nlb loadbalancer_target_group iam_account iam_project; do
  git grep -l "\"$t\"" -- '*.go' ':!*_test.go' ':!services/iam/internal/domain/types.go'
  git grep -c "$t" -- 'services/iam/internal/migrations/*.sql'
done
```

По всем четырём — **пусто** и **0**. То есть вызывающий вправе прислать
`resource_type: "iam_account"`, проверка примет, и совпасть это не сможет никогда:
запрет «принято-и-проигнорировано» (`api-conventions.md`) в чистом виде. Ни
`retiredTypes` (3 записи, `services/iam/internal/domain/retired_types.go`), ни
комментарий у самого объявления этих имён не объясняют.

### 0.2. НЕ подтвердилось — Н1 несущее

**Н1. «Заменить сверку на вывод… замена ничего не ломает» — неверно в обе стороны.**

Правая сторона гейта дрейфа — **не таблицы Go**, а **модель**. Прочитано целиком:
`services/iam/internal/authzmap/fga_model_drift_test.go`, **473** строки, **7**
функций `Test`, утверждения объявлены в шапке (`D-1…D-4`, `R-1…R-3`):

| утверждение | что сверяет | сохраняется ли при порождении |
|---|---|---|
| `D-1` | каждый тип каталога существует как `type` модели | **да** — модель порождением не задета |
| `D-2` | глагольный тип несёт ПОЛНЫЙ закрытый набор `v_*` | **да** |
| `D-3` | `TypeHasVerbRelations(t)` ⟺ модель объявляет `v_*` на `t`, в обе стороны | **да** |
| `D-4` | ярусные типы каталога не несут `v_*` | **да** |
| `R-1` | каждый `type` модели — либо тип каталога, либо документированный невыдаваемый | **да**, и порождением НЕ достижимо |
| `R-2` | тип модели вне каталога не несёт `v_*` | **да**, и порождением НЕ достижимо |
| `R-3` | множество выразимых отношений равно модельному, в обе стороны | **да** |

Значит **ни одно из семи утверждений порождением не закрывается**: порождение
меняет ПРОИСХОЖДЕНИЕ левой стороны и не трогает правую. Снимать гейт не за что —
у него не исчезает предмет.

**И одновременно противоположная опасность, которой задача не видит.** Ветка **A**
(`#1104`) рендерит **модульные** блоки модели из манифеста. Как только это сядет,
у модульных типов **обе** стороны гейта будут производиться из одного источника, и
сверка «модель ⟷ таблицы» для них станет проверкой того, что генератор согласен сам
с собой. Это происходит **молча**: гейт зелен, число утверждений растёт, обход не
пуст (`testing.md` §«Гейт на класс», п. 9 — негативное утверждение замолкает).
Решение — §2.4.

**Н2. `forbiddenObjectTypes` живёт НЕ в iam и предметом этой задачи не является.**

Задача называет его среди «трёх разошедшихся перечней». Замер: файла
`services/iam/**/policy.go` в дереве **нет**; объявление лежит в
`pkg/authz/proxytuple/policy.go` — **общий фундамент**, потребители — все сервисы,
не iam. Записей **7**: `cluster`, `account`, `project`, `user`, `service_account`,
`group`, `role`.

Уточнение к утверждению задачи: `user`, `service_account`, `group` — это **типы
модели** (субъектные), а не «отсутствующие `iam_*`». В модели их **три из трёх**
(предикат: `grep -c "^type user$"` и т. д. → 1 · 1 · 1). Не является типом модели
только `role` (`grep -c "^type role$"` → **0**). То есть словарь перечня —
субъектный, а не ресурсный, и «не несёт ни одного из шести `iam_*`» верно
буквально, но описывает не расхождение, а **другой словарь**.

Порождением из манифеста **одного модуля** этот перечень не закрывается by
construction: он есть список отказа общего компонента, и его предмет — «чем никогда
не бывает ресурс модуля», а не «какие ресурсы у модуля есть». Исход — §2.7,
собственная задача, и разбор её в этой приёмке не ведётся (`security.md`
§«Публичные артефакты»: координата, условие и следствие в одной сборке запрещены).

**Н3. `objectTypeTable` — не один перечень из 16, а ПЯТЬ перечней в пяти чужих
сервисах, и расхождением это не является.**

Единица: запись отображения в файле сервиса.

```
git grep -h -E '^\s*"[a-z_]+":' -- '*/existence_probe.go' | sed 's/.*"\([a-z_]*\)".*/\1/' | sort -u | wc -l
```

| сервис | записей | координата |
|---|---:|---|
| compute | 1 | `services/compute/internal/repo/existence_probe.go` |
| nlb | 3 | `services/nlb/internal/repo/kacho/pg/existence_probe.go` |
| registry | 1 | `services/registry/internal/repo/kacho/pg/existence_probe.go` |
| storage | 3 | `services/storage/internal/repo/pg/existence_probe.go` |
| vpc | 8 | `services/vpc/internal/repo/kacho/pg/existence_probe.go` |
| **уникальных** | **16** | — |

Три причины, по которым порождение из манифеста iam здесь **невозможно и не нужно**:

1. значение отображения — **имя таблицы в собственной базе сервиса**. Манифест iam
   его не знает и знать не вправе (ban #8, база на сервис). Порождение потребовало бы
   опубликовать чужую схему в чужом же манифесте;
2. узость **объявлена причиной**, а не является дрейфом. Дословно у compute:
   «Один тип ровно потому, что ровно один скрывает рантайм… Замер по карте прав
   compute: пообъектный тип из неё выводится РОВНО один»;
3. неизвестный тип — **ошибка**, а не «нет»: `ObjectExists` отвечает
   `unprobeable object type`, вызывающий трактует это как «не могу подтвердить
   отсутствие» и оставляет отказ отказом. Пропуск записи **закрывает** дверь, а не
   открывает.

Предикат задачи для этого перечня («либо порождается, либо получает объявленную
границу с причиной») **выполнен уже сегодня** — граница объявлена в самих файлах.
Работы нет; это исход «посылка не подтвердилась», а не отложенный долг.

**Н4. `go generate` — механизм, которого в этом дереве НЕТ.**

```
git grep -c 'go:generate' -- '*.go'   → пусто (0 вхождений)
```

Предикат задачи «`go generate` даёт побайтово те же таблицы» называет механизм,
которого дерево не знает. Идиома дерева — **цель Makefile плюс гейт побайтового
равенства**, и она имеет два живых образца: `fga-model-embed`
(`deploy/Makefile:1271`) с гейтом
`services/iam/internal/authzmodel/identity_test.go`
`TestEmbeddedModelIsByteIdenticalToCanonical`, и каталог прав
(`gateway/scripts/gen-permission-catalog.sh`) с гейтом
`make -C gateway permission-catalog-check`. Приёмка берёт идиому дерева (§2.2), а не
вводит вторую.

**Н5. Поверхность литералов — 151 файл / 500 вхождений, а не 107 / 314.**

Моя единица названа явно: **не-тестовый файл `.go` всего дерева, несущий строковый
литерал одного из 27 имён типа каталога**; вхождение — совпадение литерала.
Предикат — скрипт §0.1 с обходом `git ls-files '*.go'`.

| срез | файлов | вхождений |
|---|---:|---:|
| всё дерево | **151** | **500** |
| из них `services/iam/` | 64 | 244 |
| вне iam | 87 | 256 |

Числа задачи (107 / 314) моей командой **не воспроизводятся**. Расхождение
**не разрешено выбором правдоподобного** и не разрешено вовсе: единицу счёта задачи
восстановить не удалось (иной набор имён, иной срез дерева либо иная ревизия — три
гипотезы, ни одна не проверена). В приёмке действует моё число **со своей единицей**;
число задачи не отвергается, а объявляется измеряющим другое.

### 0.3. Моя собственная ошибка замера — названа первой

Первый прогон поиска `forbiddenObjectTypes` и `objectTypeTable` я сузил формой
`--include='*.go' services/iam/` и получил **пусто по обоим**, после чего едва не
записал в приёмку, что оба перечня «в дереве отсутствуют». Оба существуют — просто
вне `services/iam/`. Тот же промах повторился на `knownModules`: пathspec
`-- 'services/iam/**' ':!*_test.go'` дал только строки приёмки-соседа, тогда как
объявление живёт в `services/iam/internal/domain/module_set.go:22` и цело.

Класс: **предикат искал по месту, а не по свойству** (`multi-agent-flow.md` §14а,
вид 1 — гипотеза о месте). Отрицательный ответ узкого предиката о дереве не
утверждает ничего; проверять надо обходом всего дерева, а сужать — после.

### 0.4. Что задача НЕ назвала, а оно несущее

**Каталог читается на пути запроса.** Единица: вызов функции `authzmap` в пакете
цепи вердикта.

```
git grep -h -o 'authzmap\.[A-Za-z]*' -- services/iam/internal/repo/kacho/pg/relverdict | sort | uniq -c
      9 authzmap.CatalogTypeName
      5 authzmap.DottedType
```

Обе читают `objectTypes` (вторая — через обратный индекс `dottedByFGAType`). Значит
у формы порождения есть требование, которого в задаче нет: **порождается
компилируемая таблица Go**, а не строки, читаемые в рантайме. Иначе горячий путь
вердикта приобретает обращение к базе на каждый вопрос о доступе. См. §2.5.

**Словарей не два, а четыре**, и задача называет два. Перепись — §1.2.

### 0.5. Граница всего замера

Ни одного прогона не делалось: **всё измерено чтением ствола** на `2fb27446e`.
Ни `go test`, ни `go build`, ни стенд не запускались (ресурс сессии — текстовая
работа). Следствие названо прямо: утверждения о **поведении** ниже суть требования
к реализации, а не наблюдения. Проверяются они пробами §5, а не этим документом.

### 0.6. Блокер, обнаруженный замером: раздела `resources` НЕ существует

Единица: ключ верхнего уровня схемы манифеста.

```
python3 -c "import json;print(list(json.load(open('services/iam/schema/module-manifest-seed.schema.json'))['properties']))"
  → ['apiVersion', 'module', 'seed']
```

Разделов **три**, и `resources` среди них нет. Более того, приёмка предшественника
(`module-manifest-seed-contract.md`, сценарий **MOD-MF-07**) требует, чтобы документ,
несущий `resources`, **отвергался явно** с указанием задачи-преемника `#1778`.

`#1778` — OPEN, «iam: схема манифеста — разделы `resources`, `roles`,
`deprecatedVerbs`», метки `P1 size:L area:iam release:modules` (метка `blocked`
снята — развилку `#1104` решили). Проверено `gh issue view 1778 -R PRO-Robotech/kacho`.

**Следствие для порядка работ:** `#1092` не может быть начата раньше посадки схемы
раздела. Приёмка это фиксирует требованием, а не пожеланием (§9, шаг 0).

---

## 1. Предмет

### 1.1. Что неверно

Каталог типов объектов — знание о том, **какие ресурсы существуют и какие глаголы
они несут**, — записан в Go четырьмя рукописными словарями. Добавление типа требует
правки Go в четырёх местах, из которых **согласие держит гейт только между двумя**.

Гейт дрейфа при этом работает и работает верно: он сверяет таблицы `authzmap` с
моделью и падает на расхождении. Дефект не в нём, а в том, что **согласие
достигается сверкой, а не построением**: словари, которые гейт не покрывает, уже
разошлись (§0.1, «A» и «B»), и разошлись молча.

### 1.2. Четыре словаря — числом, с единицей счёта

Единица: **запись отображения либо элемент списка** в объявлении Go.

| # | словарь | координата | записей | чем удержан сегодня |
|---:|---|---|---:|---|
| 1 | `objectTypes` | `services/iam/internal/authzmap/fga_types.go:478` | **27** | гейт дрейфа `D-1`, `R-1` |
| 2 | `typeVerbRelations` | `services/iam/internal/authzmap/fga_types.go:369` | **27** | гейт дрейфа `D-2`, `D-3`, `R-2` |
| 3 | `labelSelectableTypes` | `services/iam/internal/domain/feed_registry.go:28` | **25** | `authzmap/verb_type_materializable_test.go` |
| 4 | `validResourceTypes` | `services/iam/internal/domain/types.go:86` | **23** (с `*`) | **ничем относительно модели** |

Плюс закрытый набор модулей `knownModules` — **6** элементов,
`services/iam/internal/domain/module_set.go:22`, удержан
`authzmap/module_set_drift_test.go` `TestModuleSetDrift_AuthzmapVsDomain` в
привязке к префиксам ключей `objectTypes`.

Прочие величины той же ревизии:

| величина | значение | предикат |
|---|---:|---|
| типов в модели | **32** | `grep -c '^type ' services/iam/internal/authzmodel/fga_model.fga` |
| выдаваемых типов каталога | **27** | скрипт §0.1 |
| невыдаваемых типов модели | **5** | «C» в §0.1 |
| проб в пакете `authzmap` | **46** файлов, **117** функций `Test` | `git ls-files … \| grep -c _test.go`; `git grep -h '^func Test' … \| wc -l` |
| строк аннотации `required_relation` в контрактах | **302** | `git grep -c required_relation -- 'proto/**/*.proto'` |

### 1.3. Расхождение словаря 4 — не «отставание», а другой вопрос

`validResourceTypes` **не обязан** равняться каталогу, и это уже записано в дереве.
`services/iam/internal/domain/access_binding_target.go:178…195` объясняет: два
словаря «отвечают на разные вопросы и записаны в разных соглашениях», а вывод одного
имени из другого — «та же ошибка, что чтение региона из имени зоны».

Значит предметом `#1092` здесь является **не равенство каталогу**, а более слабое и
безусловно верное свойство: **ни одно имя этого словаря не должно отсутствовать в
модели**. Четыре имени «A» его нарушают, и ни одно из них не производится ничем
(§0.1).

---

## 2. Решение

### 2.1. ГРАНИЦА: что порождается, что объявляется, чем удержано согласие

Это центральное требование приёмки — задача просит его назвать явно.

| артефакт | источник | форма получения | чем удержано |
|---|---|---|---|
| раздел `resources` манифеста | **аннотации контрактов** (`required_relation`, `scope_extractor`) | порождается, `pkg/authz/catalogderive` | `internal/repohygiene` `TestNoServiceDeclaresItsPermissionsASecondTime` |
| `objectTypes` (словарь 1) | **раздел `resources` манифеста** | порождается целью Makefile | гейт побайтового равенства (§2.2) |
| `typeVerbRelations` (словарь 2) | **раздел `resources` манифеста** | порождается той же целью | тот же гейт |
| блоки модели модульных типов | манифест (ветка **A**, `#1104`) | рендер — **не предмет `#1092`** | `#1778` и задача рендера |
| блоки модели платформенных типов | **рукопись** `proto/kacho/cloud/iam/v1/fga_model.fga` | не порождается | код-ревью; `identity_test.go` держит вшитую копию |
| `labelSelectableTypes` (словарь 3) | **рукопись** | не порождается — §2.6 | `verb_type_materializable_test.go` |
| `validResourceTypes` (словарь 4) | **рукопись** | не порождается — §2.6 | новый гейт §2.6 |
| `knownModules` | **рукопись** | не предмет `#1092` — §2.7 | `module_set_drift_test.go` |
| `objectTypeTable` × 5 | **рукопись сервиса-владельца** | не порождается никогда — §0.2 Н3 | граница объявлена в файлах |
| `forbiddenObjectTypes` | **рукопись общего фундамента** | не порождается — §2.7 | своя задача |

**Правило границы, из которого выведена таблица:** порождается то, у чего
источником является **манифест iam**; объявляется руками то, чей источник — **чужая
база** (имена таблиц), **общий фундамент** (список отказа) либо **сам механизм
вердикта** (форма вывода платформенных типов).

### 2.2. Идиома порождения — цель Makefile плюс гейт побайтового равенства

Приёмка требует именно этой формы и **запрещает** вводить `go:generate` (в дереве
его ноль, §0.2 Н4).

Требования к цели:

1. **детерминизм**: два прогона на одном входе дают побайтово равный выход. Порядок
   записей — сортировка по точечному ключу, а не порядок обхода отображения;
2. **порождаемый файл помечен как порождаемый** — шапка называет цель, которой он
   получен, и запрещает править его руками;
3. **гейт побайтового равенства** — проба, перепорождающая таблицы во временный
   каталог и сверяющая с лежащим в дереве. Красное = «дерево не перегенерировано»,
   и текст отказа называет **цель**, которую надо позвать;
4. **имя цели существует** — держит `internal/repohygiene` `TestNamedMakeTargetExists`
   (координата: `internal/repohygiene/namedmaketarget_test.go`).

### 2.3. Гейт дрейфа НЕ снимается и НЕ переводится — он сохраняет утверждение дословно

Норма корпуса не оставляет третьего исхода: проверка, чей предмет снят, либо
снимается вместе с ним, либо переводится на признак, который дерево производит. Здесь
**предмет не снимается**, и это установлено замером (§0.2 Н1): все семь утверждений
сверяют левую сторону с **моделью**, а модель порождением таблиц не задета.

Поэтому:

- **ни одна из семи функций `Test` не удаляется**;
- **ни одно утверждение не ослабляется**;
- изменение затрагивает у гейта ровно одно — он начинает **печатать перепись по
  популяциям** (§2.4).

Обратное — «раз таблицы теперь порождаются, сверять нечего» — есть та самая ошибка,
против которой написан §0.2 Н1: порождение делает левую сторону производной от
манифеста, а гейт спрашивает про её отношение к модели. Это разные вопросы.

### 2.4. Вакуумность приходит с РЕНДЕРОМ, и гейт обязан увидеть её сам

Как только ветка **A** сядет, у **модульных** типов обе стороны гейта станут
производными одного источника, и сверка для них выродится в «генератор согласен сам
с собой» — молча.

Требование: гейт делит осматриваемые типы на **две популяции** и печатает **обе
величины**, а не одну.

```
[CENSUS] типов модели 32 · каталога 27 · судимо сверкой 27 · судимо рантаймом 0
```

- **платформенная популяция** — тип, чей блок модели рукописен. Провенанс сторон
  независим ⇒ сверка содержательна ⇒ утверждения `D-1…R-3` действуют без изменений;
- **модульная популяция** — тип, чей блок модели отрендерен из манифеста. Провенанс
  общий ⇒ равенство тавтологично ⇒ **сверкой не судится**, и это говорится вслух
  переписью, а не умалчивается зелёным.

**Модульная популяция судится предикатом, который дерево производит** — DoD-1 эпика
`#1027`: применение манифеста с новым типом обязано давать **ненулевой** ответ
синтетическому вопросу вердикту. Это условие успеха самой операции применения.

**Послабление истекает само.** Пока рендера нет, модульная популяция равна **нулю**,
и вся сегодняшняя популяция (27 типов) судится сверкой — то есть `#1092` **ничего не
ослабляет сегодня**. Как только модульная популяция станет ненулевой, а предиката
рантайма в дереве не окажется, гейт обязан **покраснеть**: «типов вне сверки K, а
предиката рантайма для них нет». Это единственная новая ветка отказа, и её
способность падать доказывается инъекцией (§7).

### 2.5. Горячий путь не дорожает — порождается КОМПИЛИРУЕМАЯ таблица

Порождением получается исходник Go, а не строки в базе. Цепь вердикта
(`relverdict`, **9** вызовов `CatalogTypeName` и **5** `DottedType`, §0.4) продолжает
читать отображение в памяти. Ни одного нового соединения, ни одного нового запроса
на пути вопроса о доступе.

Обратная форма — читать каталог строками в рантайме — приёмкой **отвергается** и
предметом `#1092` не является: она переносит стоимость установки модуля (раз в
релиз) на **каждый** вопрос о доступе и заводит перекос реплик там, где сегодня его
нет by construction.

### 2.6. Три оставшихся словаря — исход по каждому

Задача требует по каждому расхождению один из двух исходов. Ответы:

| словарь | исход | обоснование |
|---|---|---|
| `objectTypeTable` × 5 | **граница объявлена, работы нет** | §0.2 Н3: значение — имя чужой таблицы; узость объявлена причиной; неизвестный тип fail-closed |
| `forbiddenObjectTypes` | **граница + своя задача** | §0.2 Н2: общий фундамент, субъектный словарь, не предмет манифеста одного модуля |
| `validResourceTypes` | **граница + ГЕЙТ**, порождения нет | §1.3: словарь отвечает на другой вопрос; но подмножество модели обязано держаться |

Для четвёртого словаря `#1092` обязана дать **два** результата:

1. **четыре имени «A» приведены к факту.** Каждое либо получает написанную причину
   (какие живые строки его несут и почему запись остаётся), либо снимается. Решение
   принимает реализация **по замеру живых строк**, а не по этому документу: приёмка
   мерила дерево, а не базу. Отсутствие причины и отсутствие снятия — не исход;
2. **гейт**: ни одно имя `validResourceTypes` (кроме `*`) не отсутствует в модели.
   Обе стороны рукописны ⇒ утверждение содержательно и не вырождается ни при каком
   развитии ветки A.

`labelSelectableTypes` порождением **не** закрывается: «выбирается ли тип по меткам»
есть свойство того, эмитит ли владелец метки в указателе принадлежности, — факт о
**чужом сервисе**, которого манифест iam не знает. Расхождение с каталогом сегодня
ровно одно (`vpc.addressPool`, предикат в §1.2) и удержано
`verb_type_materializable_test.go`.

### 2.7. Явно вне решения

- **`knownModules`** — закрытый набор модулей. Владелец — `#1030` (каталог
  таблицами); `#1092` его не трогает, и `module_set_drift_test.go` остаётся как есть;
- **`forbiddenObjectTypes`** — заводится своей задачей. Разбор здесь не ведётся
  осознанно: `security.md` §«Публичные артефакты» запрещает сборку «координата +
  условие + следствие» в публичном репозитории, а обе стороны условия у этого
  перечня публикуемы порознь и не публикуемы вместе.

### 2.8. Миграция не требуется

Ни один из четырёх словарей не имеет колонки, схемы или ограничения, которое
меняется этим изменением. Порождение производит исходник Go. Заведёт реализация
миграцию по иной причине — она обязана цитировать APPROVED-приёмку и получить запись
в `docs/acceptance-ledger.yaml` (гейт
`internal/repohygiene` `TestNewMigrationCitesAnApprovedAcceptance`).

### 2.9. Production-complete в своих границах

Изменение не вводит режима «пока так, потом допилим» (ban #14): порождение либо
работает и держится гейтом равенства, либо не садится. Отложенного нет (ban #11):
модульная популяция сегодня равна нулю **по факту дерева**, а не по отсрочке, и её
появление роняет гейт, если предикат рантайма к тому моменту не заведён.

---

## 3. Чего эта приёмка НЕ решает

1. **Схему раздела `resources`** — владелец `#1778`. Без неё порождать не из чего;
2. **Рендер блоков модели** из манифеста (ветка A) — своя задача. `#1092` только
   готовит гейт к её появлению переписью популяций;
3. **Каталог таблицами** (`catalog_module` / `catalog_resource` / `catalog_verb`) —
   `#1030`. `#1092` порождает **компилируемые** таблицы, а не строки;
4. **Тенантные модули.** Набор `module` схемы манифеста сегодня ЗАКРЫТ платформенными
   шестью (`domain.IsKnownModule`). Пока он закрыт, порождение не даёт арендатору
   объявить свой тип — цель эпика этим шагом **не** достигается, и говорить обратное
   значило бы заявить шире сделанного;
5. **Живые строки базы.** Судьба четырёх фантомных имён требует замера строк, которого
   в этой приёмке нет (§0.5).

---

## 4. Out-of-scope — явно

- любое изменение контракта `proto/` — ни поля, ни RPC, ни аннотации;
- любое изменение канонического `fga_model.fga`;
- снятие или ослабление **любой** из 7 функций гейта дрейфа;
- снятие или ослабление любой из **117** проб пакета `authzmap`;
- правка `objectTypeTable` в пяти сервисах-владельцах;
- правка `knownModules`;
- перенос чтения каталога в рантайм;
- разбор `forbiddenObjectTypes` по существу.

---

## 5. Сценарии

ID трассируются в имена проб. Отрицательные — **только** в паре с положительным
контролем; пара названа у каждого.

### 5.1. Порождение

**MOD-CT-01 — порождение даёт побайтово то, что лежит в дереве**

**Given** дерево на ревизии без правок в манифесте и в порождаемом файле
**When** позвана цель порождения таблиц типов
**And** результат сравнён с файлом, лежащим в дереве
**Then** различий **ноль**, побайтово
**And** положительный контроль пары: порождение **исполнилось** — напечатано число
порождённых записей (**27** сегодня), а не только «различий нет»

> Без второй половины «различий ноль» неотличимо от «порождение не запускалось»
> (`testing.md`, «ноль находок против ноль прочитанного»).

**MOD-CT-02 — порождение детерминировано**

**Given** один и тот же вход манифеста
**When** порождение выполнено **дважды** в разные временные каталоги
**Then** оба выхода побайтово равны между собой
**And** порядок записей — по точечному ключу, а не по обходу отображения

**MOD-CT-03 — новый ресурс в манифесте меняет обе таблицы без правки Go руками**

**Given** манифест, в котором объявлен ресурс, отсутствующий в сегодняшних 27
**When** позвана цель порождения
**Then** в `objectTypes` появляется его точечный ключ и имя типа модели
**And** в `typeVerbRelations` появляется его набор глаголов
**And** ни один файл Go не правился руками — единственный изменённый файл помечен как
порождаемый

**MOD-CT-04 — устаревшее дерево краснеет и называет цель**

**Given** манифест изменён, а порождённый файл не перегенерирован
**When** прогнан гейт побайтового равенства
**Then** отказ, и его текст называет **цель**, которую надо позвать
**And** парный положительный: то же дерево после вызова цели — гейт молчит

### 5.2. Гейт дрейфа — сохранение утверждения и перепись

**MOD-CT-05 — семь утверждений сохранены дословно**

**Given** дерево после посадки порождения
**When** прогнан `services/iam/internal/authzmap/fga_model_drift_test.go`
**Then** функций `Test` в файле по-прежнему **7**, и каждая из `D-1…D-4`, `R-1…R-3`
утверждает то же, что и до изменения
**And** ни одно утверждение не получило исключения, списка прощённых и толерантности

**MOD-CT-06 — перепись печатает ОБЕ величины**

**Given** дерево на сегодняшнем состоянии (рендера нет)
**When** прогнан гейт дрейфа
**Then** напечатана перепись вида «типов модели 32 · каталога 27 · судимо сверкой 27 ·
судимо рантаймом 0»
**And** число «судимо сверкой» равно числу типов каталога — то есть послабления
сегодня нет ни на один тип

**MOD-CT-07 — тип, ушедший из-под сверки без предиката рантайма, РОНЯЕТ гейт**

**Given** тип, чей блок модели помечен как отрендеренный из манифеста
**And** предиката рантайма (DoD-1 эпика) для него в дереве нет
**When** прогнан гейт дрейфа
**Then** **отказ**, называющий этот тип поимённо и говорящий, что он вне сверки и без
замены
**And** парный положительный: тот же тип при наличии предиката рантайма — гейт молчит
и печатает «судимо рантаймом 1»

> Это самоистечение послабления. Без сценария новая популяция росла бы молча, а
> зелёный гейт означал бы «не нашла», а не «нет».

### 5.3. Словарь якорей

**MOD-CT-08 — имя якоря, отсутствующее в модели, отвергается**

**Given** `validResourceTypes`, содержащий имя, которого модель не объявляет
**When** прогнан гейт подмножества
**Then** отказ, называющий **имя** и то, что модель его не объявляет
**And** парный положительный: словарь без таких имён — гейт молчит и печатает число
осмотренных имён

**MOD-CT-09 — подстановка `*` гейтом не судится**

**Given** словарь, содержащий `*`
**When** прогнан тот же гейт
**Then** молчание: `*` — не имя типа, и его отсутствие в модели нарушением не является
**And** парный отрицательный: имя `iam_account` в том же прогоне даёт отказ

> Пара нужна в обе стороны: гейт, пропускающий `*`, не должен пропускать всё.

**MOD-CT-10 — четыре фантомных имени получили исход**

**Given** дерево после посадки `#1092`
**When** прочитано объявление `validResourceTypes`
**Then** для каждого из `iam_account`, `iam_project`, `loadbalancer_nlb`,
`loadbalancer_target_group` верно ровно одно: имени в объявлении **нет**, либо рядом
с ним написана причина, называющая, какие живые строки его несут
**And** парный положительный: гейт `MOD-CT-08` на получившемся дереве зелен

### 5.4. Краевые

**MOD-CT-11 — пустой обход роняет гейт**

**Given** манифест, из которого не читается ни одной записи `resources`
**When** позвано порождение
**Then** **отказ**, а не пустая таблица: ноль порождённых записей есть беспредметный
прогон, и он не выдаётся за успех
**And** парный положительный: непустой манифест порождает и печатает число

**MOD-CT-12 — горячий путь остаётся чтением памяти**

**Given** дерево после посадки
**When** перечислены обращения цепи вердикта к каталогу
**Then** ни одно из них не является запросом к базе — отображение читается из
компилируемой таблицы
**And** число вызовов `CatalogTypeName` и `DottedType` в `relverdict` не изменилось

**MOD-CT-13 — идентификаторы порождённого файла латиницей**

**Given** порождённый файл Go
**When** прогнан `internal/repohygiene` `TestIdentifiersAreASCII`
**Then** молчание: имена, произведённые генератором из точечных ключей манифеста,
латинские (ban #17)
**And** парный отрицательный: манифест, объявляющий ресурс с кириллическим именем,
отвергается **порождением**, а не доезжает до компилятора

---

## 6. Чем держится каждое требование

| # | требование | чем держится |
|---:|---|---|
| 1 | порождение детерминировано и побайтово воспроизводимо | гейт равенства (`MOD-CT-01`, `MOD-CT-02`) |
| 2 | дерево не отстаёт от манифеста | тот же гейт (`MOD-CT-04`) |
| 3 | семь утверждений дрейфа сохранены | сам гейт дрейфа + `MOD-CT-05` |
| 4 | вакуумность видна, а не молчалива | перепись популяций (`MOD-CT-06`) |
| 5 | послабление истекает само | `MOD-CT-07` |
| 6 | словарь якорей ⊆ модель | новый гейт (`MOD-CT-08`, `MOD-CT-09`) |
| 7 | фантомные имена получили исход | `MOD-CT-10` |
| 8 | пустой обход не выдаётся за успех | `MOD-CT-11` |
| 9 | горячий путь не дорожает | `MOD-CT-12` |
| 10 | имя цели Makefile существует | `internal/repohygiene` `TestNamedMakeTargetExists` |
| 11 | идентификаторы латиницей | `internal/repohygiene` `TestIdentifiersAreASCII` |
| 12 | отсрочки в дереве нет | `internal/repohygiene` `TestNoDeferredWorkInTheTree` |
| 13 | границы §2.6 по трём словарям **не нарушаются впредь** | **вниманием**, гейта нет |

Строка 13 названа честно. «Порождать этот словарь или объявлять» — суждение о
происхождении факта, а не предикат; машинно оно не решается. Проверяемый признак для
обзора: у каждого нового словаря имён типа в объявлении написано, **чей это факт** —
манифеста iam, чужой базы, общего фундамента или механизма вердикта.

---

## 7. Способность проверок УПАСТЬ — как доказывается

Каждая новая проверка доказывается **инъекцией настоящим входом** и **законным
близнецом**, а не прочтением (`testing.md` §«Гейт на класс», п. 2).

| проверка | инъекция (обязана краснеть) | законный близнец (обязан молчать) |
|---|---|---|
| равенство порождённого | сдвинуть один символ в порождённом файле | перегенерированное дерево |
| детерминизм | генератор, обходящий отображение без сортировки | генератор с сортировкой |
| перепись популяций | пометить тип отрендеренным, предиката рантайма не заводить | тот же тип с предикатом |
| подмножество якорей | добавить имя, которого модель не объявляет | добавить `*` и настоящее имя модели |
| пустой обход | манифест без записей `resources` | манифест с одной записью |

**Три требования к инъекции, каждое проверяемо:**

1. **инъекция роняет ТОЛЬКО проверяемое.** Прогонов **три**, а не два: контроль (всё
   цело — молчат оба) · инъекция нового (краснеет только новый) · инъекция
   существующего (краснеет только гейт дрейфа). Без третьего молчание существующего
   гейта неотличимо от его смерти (`testing.md` п. 2в);
2. **инъекция проверяет ТЕКСТ отказа, а не только красноту.** Находка обязана
   называть **координату** — имя типа, имя цели, имя файла, — иначе на неё потратят
   прогон и снимут гейт как непонятный (п. 8);
3. **близнец не привязан к снимаемому предмету.** Законный близнец берётся из
   платформенной популяции, которая рендером не затрагивается: близнец, привязанный к
   модульному типу, умер бы вместе с ним и унёс бы доказательство.

**Отдельно — доказательство после ПЕРЕУСТРОЙСТВА.** Гейт дрейфа этим изменением
переустраивается (добавляется деление популяций). Совпадение переписи с прежней —
**не** доказательство: гейт, потерявший способность краснеть, на чистом дереве
выглядит точно так же. Инъекция по всем семи утверждениям прогоняется **заново**
(п. 8), и её результат называется в PR числом.

---

## 8. Гейты дерева, которые обязаны увидеть новое — поимённо и с проверкой существования

Существование каждого проверено командой на `2fb27446e`; команда воспроизводима.

| гейт | координата | предикат существования | что увидит |
|---|---|---|---|
| дрейф модели | `services/iam/internal/authzmap/fga_model_drift_test.go` | `git grep -c '^func Test' -- …` → **7** | новую перепись; сохранение семи утверждений |
| канонический дрейф каталога | `services/iam/internal/authzmap/canonical_model_drift_test.go` | `git grep -n 'func TestCatalogTypesExistInTheCanonicalModel'` → строка **47** | порождённый каталог вместо рукописного |
| материализуемость глагольных типов | `services/iam/internal/authzmap/verb_type_materializable_test.go` | `git grep -n 'func TestEveryVerbBearingTypeIsMaterializable'` → строка **135** | согласие порождённой таблицы со словарём 3 |
| дрейф набора модулей | `services/iam/internal/authzmap/module_set_drift_test.go` | `git grep -n 'func TestModuleSetDrift_AuthzmapVsDomain'` → строка **25** | префиксы порождённых ключей |
| побайтовость вшитой модели | `services/iam/internal/authzmodel/identity_test.go` | `git grep -n 'func TestEmbeddedModelIsByteIdenticalToCanonical'` → строка **24** | ничего — модель не задета; прогон обязателен как контроль |
| паритет каталога прав | `internal/repohygiene/catalogparity_test.go` | `git grep -l 'func TestNoServiceDeclaresItsPermissionsASecondTime'` → файл есть | что манифест не стал вторым объявлением права |
| названная цель сборки | `internal/repohygiene/namedmaketarget_test.go` | `git grep -l 'func TestNamedMakeTargetExists'` → файл есть | имя новой цели порождения |
| идентификаторы латиницей | `internal/repohygiene/asciiidentifiers_test.go` | `git grep -l 'func TestIdentifiersAreASCII'` → файл есть | имена в порождённом файле |
| отсрочка в дереве | `internal/repohygiene/deferredwork_test.go` | `git grep -l 'func TestNoDeferredWorkInTheTree'` → файл есть | что порождённый файл не несёт маркеров |
| вердикт приёмок продукта | воркспейс, `scripts/docs-gate/` (`check-04`) | `python3 -c "import sys;sys.path.insert(0,'scripts/docs-gate');import _lib;print(_lib.verdict(open('…').read()))"` | шапку этого документа |

**Проверка формы шапки этим самым распознавателем** (воркспейс `78c9af5`,
`scripts/docs-gate/_lib.py`, функция `verdict`) — обязательный шаг перед сдачей: она
читает **первый** токен закрытого набора в шапке, до первого заголовка `##`.

**Пакет `authzmap` прогоняется ЦЕЛИКОМ**, а не выборочно: **46** файлов проб, **117**
функций `Test`. Выборочный прогон полосы не зовёт гейт, живущий в соседнем файле, и
каждая ветка по отдельности зелена (`multi-agent-flow.md` §14).

---

## 9. Порядок работ после `APPROVED`

0. **Ждать `#1778`.** Раздела `resources` в схеме нет (§0.6); порождать не из чего.
   Начало работ раньше — работа по несуществующему источнику;
1. `class-exposure-analyst` — экспозиция классов на утверждённом замысле;
2. `superpowers:writing-plans` — план;
3. `integration-tester` — **RED** по сценариям §5, до кода (ban #12). Показать пару
   «RED → GREEN» в PR;
4. реализация: цель порождения · порождённый файл · гейт равенства · перепись
   популяций у гейта дрейфа · гейт подмножества якорей;
5. инъекции §7 — **три прогона** на каждую новую проверку, результат назван числом;
6. исход по четырём фантомным именам (`MOD-CT-10`), **по замеру живых строк**;
7. `go-style-reviewer`; `proto-api-reviewer` **не требуется** — контракт не задет;
   `db-architect-reviewer` **не требуется** — миграции нет;
8. **сужение относительно заголовка задачи записать в трекер до слияния линии.**
   Заголовок `#1092` говорит «вместо гейта дрейфа»; приёмка гейт **сохраняет** (§2.3).
   Расхождение заголовка и исполнения обязано быть записано, а не подразумеваться;
9. `landing-reviewer` перед посадкой.

---

## 10. Ссылки и следствия

- `#1027` — эпик. Тело по-прежнему объявляет принятой ветку **B**; развилка решена
  в пользу **A** (`#1104`). Приведение тела в согласие — за владельцем линии, и эта
  приёмка следует **решению**, а не телу эпика;
- `#1087` — исход **B**: раздел `resources` порождается из аннотаций;
- `#1088` — предшественник, `module-manifest-seed-contract.md` (APPROVED круга 2);
- `#1778` — схема разделов `resources` / `roles` / `deprecatedVerbs`. **Блокер**;
- `#1030` — каталог таблицами; владелец `knownModules`;
- следствие для эпика: `#1092` **не** закрывает DoD-4 («`knownModules` в дереве
  нет») и **не** закрывает DoD-3 («литералов чужих типов ноль») — она снимает
  необходимость править **две** таблицы из четырёх словарей. Заявлять шире нельзя.

---

## 11. Что рецензент обязан проверить прежде вердикта

Не чтением, а замером — по каждому пункту своя команда.

1. **§0.2 Н1 — несущее.** Прочитать шапку `fga_model_drift_test.go` целиком и
   ответить: сверяет ли хоть одно из семи утверждений таблицы **друг с другом**, а не
   с моделью. Если да — §2.3 неверен, и приёмка отвергается;
2. **§0.2 Н3.** Открыть любые два из пяти `existence_probe.go` и проверить, что
   неизвестный тип даёт **ошибку**, а не «нет». Если «нет» — узость перечня открывает
   дверь, и исход «границы достаточно» неверен;
3. **§0.2 Н4.** `git grep -c 'go:generate' -- '*.go'`. Непусто — идиома в дереве есть,
   и запрет §2.2 надо переписать;
4. **§0.1.** Прогнать скрипт и сверить три списка `A` / `B` / `C` дословно;
5. **§0.4.** Прогнать предикат по `relverdict` и подтвердить, что каталог читается на
   пути запроса. Если нет — требование §2.5 беспредметно;
6. **§0.6.** `python3 -c "import json;print(list(json.load(open('services/iam/schema/module-manifest-seed.schema.json'))['properties']))"`.
   Если `resources` там есть — блокер снят, и §9 шаг 0 надо убрать;
7. **§5.** Проверить, что у **каждого** отрицательного сценария назван парный
   положительный, и что положительный не выполняется тривиально на пустом входе;
8. **§8.** Прогнать все десять предикатов существования. Нерезолвящийся — находка;
9. **§0.5 и строка 13 §6** — границы. Убедиться, что ни одно утверждение документа не
   выдаёт требование к реализации за наблюдение и не обещает гейта там, где его нет.

**Возражение рецензента — тоже утверждение** (`git-issues.md`): несёт команду и
ревизию. Замечание без предиката автор вправе вернуть как неисполнимое.
