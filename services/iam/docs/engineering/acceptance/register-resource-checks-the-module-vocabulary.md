<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: BUSL-1.1
-->

# Приёмка: тип объекта проксируемого кортежа получает референт в каталоге модуля

- **Статус:** ❌ CHANGES REQUESTED — вердикт **круга 3** (`acceptance-reviewer`,
  ревизия `3543dc9fd`, §17). **Два блокирующих: Б6 и Б7**, и оба об одном
  предмете — о наборе носителей строки identity, то есть о той самой оси, которую
  круг 3 правил по Б5. Б6: §2.2 и §6 объявляют членство в группе
  **невыразимым ключом**, и названное основание — «первичный ключ `group_members`
  тройной» — **ложно**: тройной ключ есть годная цель составного внешнего ключа, и
  дерево уже несёт два трёхколоночных ключа и landed-идиому колонки-константы,
  которой такой ключ достигается. Б7: гейт §2.10 п. 1 выводит набор из
  **отставного** маршрута выдачи, поэтому обещание «число вырастет с первым же
  новым модулем» неверно, а зеркальная сторона гейта краснеет на верной работе.
  **Б4 и Б5 круга 2 — закрыты**, оба перемерены мной и подтверждены; исход (а) по
  Б4 принят, довод в его пользу — усилен и **исправлен по адресу** (§17.4, В1).
  Три собственные находки круга 3 (§16.0) **воспроизведены моим прогоном** и верны
  все три; опровержение замечания §15.4 п. 3 (§16.4) **подтверждено** чтением шапки
  от первой строки. Реализацию начинать **нельзя** до `APPROVED`
- **Ревизия измерения:** круг 1 — `9424765ab`; круг 2 — `2ff21ff96`; **круг 3 —
  та же рабочая копия `/tmp/claude-1000/wt-1031`, ветка `lane/1031` от
  `release/modules-4`, ревизия `052715e01`**. Все числа круга 3 сняты своими
  командами заново, а не перенесены; два опровержения получены **прогоном**
  (§16.0), а не чтением
- **Задача:** `PRO-Robotech/kacho#1031`
- **Предшественник:** `#1030` — референт сегмента правила роли (миграция
  `services/iam/internal/migrations/20260901113757_rule_segments_have_a_referent.sql`,
  влита в `release/modules-4` коммитом `5dcefe679`). Он завёл таблицы каталога
  строками; без них у типа объекта не было бы референта, и настоящая работа была
  бы невыразима конструкцией БД
- **Линия:** `release:modules`
- **Тип изменения:** новая миграция (одна) + сужение полосы проксируемой записи +
  два гейта дерева + один замок транспорта. **`proto/` не затрагивается** — ни
  одно поле, ни один глагол, ни одна аннотация каталога прав. `gateway/` не
  затрагивается: обе RPC живут только на внутреннем слушателе `:9091`
- **Сервис:** `kacho-iam` — предмет целиком. **Общий фундамент
  (`pkg/authz/proxytuple`) не затрагивается вовсе** — это правка круга 2: до неё
  §2.5 требовала дать чистому правилу базу, что неконструируемо (Б2)
- **Предмет приёмки — ВЫБОР и реализация.** Выбор: чем именно сужается полоса
  (членством в каталоге, а не грамматикой), где живёт сужение (ключ БД плюс
  явное чтение в той же транзакции, а не условие в Go), **каким кодом отказ
  приезжает вызывающему** и почему код у полос разный, что происходит с уже
  зарегистрированным и что остаётся производителю отказа. Обе половины умещаются
  в одну под-фазу, и она production-complete в своих границах (§2.11)

---

## 0. Перепись производителей — ДО единого сценария

Порядок обратный привычному и он обязателен. Сперва устанавливается, **что в
дереве уже проверяет этот вход, печатает этот текст, возвращает этот код, несёт
этот словарь**, — и только потом пишутся сценарии. Четыре приёмки этой линии
подряд получили `CHANGES REQUESTED` за требование к тому, у чего производителя
нет; та, где перепись строилась первой, закрыла шесть блокирующих за один круг.

**Круг 2 расширил эту перепись на ТРАНСПОРТ отказа** — и именно там нашлась дыра
Б1: перепись круга 1 обошла производителей **вердикта** и не обошла
производителей **кода на проводе**. Строки, добавленные кругом 2, помечены.

### 0.1. Что в дереве ЕСТЬ — с командой на каждую строку

| Производитель | Что производит | Координата | Предикат |
|---|---|---|---|
| `proxytuple.ValidateTuple` | вердикт «принять/отвергнуть» по тройке (субъект, отношение, тип объекта) | `pkg/authz/proxytuple/policy.go:222` | `git grep -n 'func ValidateTuple' -- pkg/authz/proxytuple` |
| `validateProxyTuple` | перевод вердикта в транспорт — `PermissionDenied "permission denied"` | `services/iam/internal/apps/kacho/api/internal_iam/handler.go:180` | `git grep -n 'func validateProxyTuple' -- services/iam` |
| обе RPC зовут его **до** use-case | сужение стоит на пути запроса, а не в глубине | `handler.go:192` и `handler.go:217` | `git grep -n 'validateProxyTuple(domain' -- services/iam` |
| `validateTuple` (use-case) | грамматика `<тип>:<id>` → `InvalidArgument` с именем поля | `services/iam/internal/apps/kacho/api/internal_iam/register_resource.go:629` | `git grep -n 'func validateTuple' -- services/iam` |
| `RelationWriteGate.Authorize` | личность модуля из проверенного SAN + `fga_writer` на кластере; возвращает **домен SAN** | `services/iam/internal/authzguard/fgaproxy.go:97` | `git grep -n 'func (g \*RelationWriteGate) Authorize' -- services/iam` |
| `ServiceAccountIDForService` | детерминированный `sva`+md5 id модульной учётки | `services/iam/internal/authzguard/fgaproxy.go:176` | `git grep -n 'func ServiceAccountIDForService' -- services/iam` |
| `authzmap.DottedType` / `FGAObjectType` | **единственный** переходник между словарём модели и словарём каталога | `services/iam/internal/authzmap/type_dictionaries.go` | `git grep -n 'Второго переходника' -- services/iam` |
| `tupleIntent.objectType()` | приведение типа кортежа к ключу зеркала тем же переходником | `register_resource.go:203` | `git grep -n 'func (t tupleIntent) objectType' -- services/iam` |
| `catalog_module` · `catalog_resource` · `catalog_verb` | словарь **строками**, с ключами `(module, resource, live)` и `(dotted, live)` | `20260901113757_rule_segments_have_a_referent.sql:121,153,242` | `git grep -n 'CREATE TABLE kacho_iam.catalog_' -- services/iam/internal/migrations` |
| гейт паритета литерала и строк | расхождение посева каталога с `authzmap` | `services/iam/internal/check/catalog_seed_parity.go` | `git grep -n 'catalogSeedCensus' -- services/iam` |
| гейт сверки намерения с приёмной стороной | тройка **каждого** эмитента прогоняется через `ValidateTuple` на сборке, в обе стороны | `internal/repohygiene/proxytupleintent_test.go` | `git grep -n 'Две половины, и вторая обязательна' -- internal/repohygiene` |
| замок текста отказа **полосы А** | `PermissionDenied` + **дословно** `"permission denied"`, без причины | `services/iam/internal/apps/kacho/api/internal_iam/proxy_tuple_refusal_transport_test.go:26` | `git grep -n 'func TestProxyTupleRefusalMapsToPermissionDenied' -- services/iam` |
| модульные учётки строками | `sva` + `substr(md5('kacho-<svc>'),1,17)` в `service_accounts` | `0009_sec_c_module_sa_least_priv.sql:63` | `grep -n "INSERT INTO kacho_iam.service_accounts" services/iam/internal/migrations/0009_sec_c_module_sa_least_priv.sql` |
| якорь права записи | `fga_writer` спрашивается на **кластере**, а не на прежнем внеиерархическом объекте | `20260823002000_relation_write_moves_onto_the_cluster.sql` | `git grep -n 'relationWriteObject = clusterRootObject' -- services/iam ':!*.md'` |
| `<exempt>` в каталоге прав | обе RPC освобождены от пообъектного Check, причина названа `INTERNAL_LISTENER` | `proto/kacho/cloud/iam/v1/internal_iam_service.proto:161` | `grep -n 'exempt_reason' proto/kacho/cloud/iam/v1/internal_iam_service.proto` |
| **`shared.MapRepoErr`** *(круг 2)* | **единственный** перевод sentinel → gRPC для обеих RPC; SQLSTATE **не читает**, хвост даёт `Internal "internal error"` | `services/iam/internal/apps/kacho/shared/errors.go:47`, хвост `:108` | `git grep -n 'func MapRepoErr' -- services/iam` |
| **`wrapPgErr`** *(круг 2)* | перевод SQLSTATE → sentinel: `23503` → `ErrFailedPrecondition`, текст по умолчанию `"referenced resource not found or still in use"` | `services/iam/internal/repo/kacho/pg/pgmaperr.go:45`, текст `:383` | `git grep -n 'func wrapPgErr' -- services/iam` |
| **`drainer.Classify`** *(круг 2)* | **экспортированный** классификатор исхода применения у модуля: постоянный ⟺ `InvalidArgument` либо `PermissionDenied` | `pkg/outbox/drainer/classify.go:82`, предикат `:145` | `git grep -n 'func Classify' -- pkg/outbox/drainer` |
| **`pkg/errors.Reason`** *(круг 2)* | закрытый **компилятором** словарь токенов полос резолва идентификатора; значений пять | `pkg/errors/reason.go:33`, закрытость `pkg/errors/reason_closed_test.go` | `git grep -n 'func AllReasons' -- pkg/errors` |
| **`isPureGrant`** *(круг 2)* | публичная выдача — **чистый кортеж**: строки зеркала не пишет вовсе | `register_resource.go:224` | `git grep -n 'func (t tupleIntent) isPureGrant' -- services/iam` |

### 0.2. Чего в дереве НЕТ — и что с этим делается

| Чего нет | Предикат | Исход |
|---|---|---|
| `catalog_module_identity` в любом написании | `git grep -c catalog_module_identity -- . ':!*.md'` → **пусто** | **заводится этой работой** (§2.2) |
| колонки, несущей имя типа в словаре **модели**, у `catalog_resource` | DDL `20260901113757:153`—`188`: `module`, `resource`, `dotted`, `retired_at`, `retired_reason`, `superseded_by`, `live` — и всё | **не заводится**: приведение делает единственный переходник (§2.3) |
| ключа у `resource_mirror.object_type` | `grep -n 'CONSTRAINT' services/iam/internal/migrations/0019_resource_mirror.sql` → PK + три проверки, **ноль** внешних ключей | **заводится этой работой** (§2.1) |
| записи в журнал на полосе отказа проксируемого кортежа | `grep -n 'h\.logger' services/iam/internal/apps/kacho/api/internal_iam/handler.go` → **одно** попадание, и оно присваивание (строка 146) | **заводится этой работой** (§2.6) |
| производителя текста «имя типа в отказе» **на полосе А** | замок `proxy_tuple_refusal_transport_test.go:37` требует **дословно** `"permission denied"` | полоса А не трогается; различимость полосы А — в журнал (§2.6) |
| производителя проверки отношения по `catalog_verb` | `catalog_verb` несёт **109** строк и **ноль** из `project`/`parent`/`owner`/`v_get` | обещание снимается: сверка невыразима (§2.4) |
| **пути от SQLSTATE к sentinel на полосе зеркала** *(круг 2)* | `wrapPgErr` **неэкспортирована** и живёт в пакете `pg`; зеркало — отдельный пакет `resource_mirror`, оборачивает голым `fmt.Errorf` (`emitter.go:190`, `:327`) | **заводится этой работой** (§2.6): перевод пишется **на полосе**, а не переиспользуется чужой |
| **замка на код полосы Б** *(круг 2)* | существующий замок зовёт `validateProxyTuple` **напрямую** (`proxy_tuple_refusal_transport_test.go:30`) — полосы БД не касается | **заводится этой работой** (§2.6, §6) |

### 0.3. Замер посылок задачи — в обе стороны

Посылки #1031 перемерены **своими** командами. Одна опровергнута целиком, одна
подтверждена в суженном виде, одна оказалась устаревшей координатой.

#### Посылка 1 — ОПРОВЕРГНУТА

> «`validateTuple` проверяет ТОЛЬКО грамматику… Ни объектный тип, ни имя отношения
> ни с одним словарём не сверяются.»

Верно про `validateTuple` **как функцию** и неверно про **полосу**. До неё, на пути
запроса, стоит `validateProxyTuple` → `proxytuple.ValidateTuple`, и он сверяет
**оба**: отношение — с закрытым набором, тип объекта — с запретительным набором и
с доменом вызывающей учётки.

Опровержение получено **опытом**, а не чтением: временная проба в
`pkg/authz/proxytuple`, прогон `go test -run`, файл снят (`git status --porcelain`
пуст). Исходы:

```
system_admin @ cluster:cluster_kacho_root, caller=vpc      -> refused
system_admin @ cluster:cluster_kacho_root, caller=""       -> refused
owner        @ cluster:cluster_kacho_root, caller=vpc      -> refused
owner        @ account:acc1,               caller=vpc      -> refused
v_get (user:mod1) @ vpc_network:n1,        caller=vpc      -> refused
not_a_relation    @ vpc_network:n1,        caller=vpc      -> refused
project @ vpc_network:n1,                  caller=vpc      -> ACCEPTED
v_get (user:*) @ registry_repository:r1/x, caller=registry -> ACCEPTED
```

**Следствие для §ПРЕДМЕТ задачи.** Названная там атака — модуль пишет
`relation=system_admin, object=cluster:cluster_kacho_root` — отвергается
**сегодня**, и отвергается **двумя независимыми оговорками**: отношение вне
закрытого набора (`policy.go:231`) и тип объекта в запретительном наборе
(`policy.go:239`). Ни одна из двух не заводится этой работой. Формулировка «каждый
установленный модуль может выписать себе кластерный супер-доступ» на этой ревизии
**ложна**, и приёмка её не воспроизводит.

#### Посылка 2 — ПОДТВЕРЖДЕНА в суженном виде, и это остаток

Тот же прогон, две строки, которые прошли:

```
project @ vpc_totally_invented:x1, caller=vpc -> ACCEPTED
project @ quux_invented:x1,        caller=""  -> ACCEPTED
```

Тип объекта связан **приставкой**, а не **членством в словаре**. Выдуманный тип с
правильной приставкой принимается, доезжает до зеркала и записывается туда
**дословно** (`register_resource.go:208`: «unknown types are kept verbatim»). То
есть у типа объекта проксируемого кортежа **нет референта** — ровно тот класс,
который `#1030` закрыл для сегментов правила роли, на втором писателе того же
словаря.

Вторая строка — про пустой домен вызывающего. Она достижима только при
`prodMode=false` (`fgaproxy.go:103`: невыверенный пир в dev-режиме → `("", nil)`),
а запрет #16 объявляет dev-посадку на поднятом стенде снятой. Полосу приёмка
закрывает (§2.2), но **не** называет её сегодняшней дырой: у неё нет
производителя на посадке, которую продукт допускает.

#### Посылка 3 — устаревшая координата

> «гейт `fga_writer@iam_fgaproxy:system` проверяет только факт наличия способности»

Якорь **переехал**: `relationWriteObject = clusterRootObject`
(`fgaproxy.go:54`, миграция `20260823002000_relation_write_moves_onto_the_cluster.sql`,
причина — `#914`: у внеиерархического якоря нет ни яруса, ни владельца, право не
выражалось выдачей и не отзывалось). Написание `iam_fgaproxy:system` осталось в
посеве `0009` как исторический факт. Существо посылки — «гейт спрашивает про
способность, а не про предмет записи» — **верно** и остаётся несущим.

### 0.4. Мои собственные ошибки — названы первыми

**Круг 1, ошибка первая.** Я прочитал `register_resource.go` целиком, увидел
`validateTuple` с одной грамматикой и **едва не записал посылку 1 как факт**:
сходство имени функции с именем из задачи выглядело подтверждением. Опровергло не
внимательное чтение, а вопрос «кто ещё стоит на этой полосе» и предикат
`git grep -n 'ValidateTuple(' -- '*.go' ':!*_test.go'`, давший **третью**
координату — `handler.go:181`, — которой в задаче нет вовсе.

**Круг 1, ошибка вторая.** Пятнадцать координат вида `файл:строка` я выписал по
памяти о просмотренном фрагменте, а не предикатом. Сверка (`sed -n "${l}p"`)
показала промах у **всех пятнадцати** — от одной строки до двадцати трёх, — и один
промах был содержательным. Все исправлены и перепроверены построчно.

**Круг 2, ошибка третья — и она дороже двух первых, потому что породила Б1.**
Перепись §0.1 круга 1 обошла производителей **вердикта** и не обошла
производителей **кода на проводе**. Я спросил «кто решает, принять ли кортеж», и
не спросил «кто превращает это решение в статус для вызывающего» — при том что
четыре из шести RED-сценариев утверждали именно **код и текст**. Строка §5.4
получила пометку «да» от функции, которая существует, называется похоже и полосе
не принадлежит.


**Круг 3, ошибка четвёртая — я классифицировал два сценария по НАМЕРЕНИЮ, а не по
исходу.** `IAM-MV-04` и `IAM-MV-06` стояли RED с припиской «сегодня принимается».
Прогон говорит обратное: `caller=vpc` + `nlb_listener:lsn1` → **refused** уже
сегодня, и на **обеих** RPC — `validateProxyTuple` стоит и в `RegisterResource`
(`handler.go:192`), и в `UnregisterResource` (`handler.go:217`), одной и той же
функцией. Приставка `nlb_` не равна `vpc_`, и полоса А отвергает вход **до**
любой новой оговорки. Круг 2 нашёл ровно эту ошибку у `IAM-MV-04` при «Тогда:
принято» и починил половину — а вторую половину («Тогда: отказ») оставил RED,
хотя отказ производится сегодня. Оба сценария переклассифицированы (§16.0).

**Круг 3, ошибка пятая — я назвал значение, которого не производит никто.**
`IAM-MV-05` требовал, чтобы текст отказа назвал `vpc.totallyInvented`. Прогон
`authzmap.DottedType`: незнакомый тип **не приводится вовсе** и уезжает дословно —
в зеркало ложится `vpc_totally_invented`, с подчёркиванием. Точечной формы у него
не бывает **никогда**, значит кейс требовал строки, которой нет производителя, —
ровно тот класс, за который эта же приёмка получила Б1 круга 1.

**Круг 3, ошибка шестая — я объявил проверяемой полосу, у которой сегодня нет
входа.** `IAM-MV-10` утверждал отказ на **снятом** типе через полосу прокси.
Снятый тип по построению отсутствует в карте платформы (его сняли вместе с
ресурсом), поэтому приведение отдаёт `compute_disk` дословно, а строка каталога
называется `compute.disk` — это разные ключи. Полоса `live` у ключа сегодня
**недостижима с полосы прокси**, и сценарий переписан на уровень, где вход
существует (§16.0, п. 3).

Признак, общий у трёх ошибок круга 3: **все три — о том, что производится, а не о
том, что задумано, и все три поймал ПРОГОН**. Чтение подтверждало каждую из них.

Признак, который следующий автор обязан применить к своей переписи: **у каждого
«Тогда», называющего код или текст, производитель ищется отдельным вопросом** —
не «кто принимает решение», а «кто пишет ответ». Это два разных производителя
всегда, когда решение рождается глубже транспорта.

### 0.5. Граница замера — чего я НЕ мерил

- **Прогона хранилища у меня не было ни в одном круге.** Все числа сняты
  статически — по дереву, по цепочке миграций и одним прогоном чистой функции
  `pkg/authz/proxytuple` (без БД, без контейнеров). Свободного места на диске
  **6.0 ГБ при 98 %** — подъём контейнеров не выполнялся осознанно, это «не
  выполнилось», а не красный вердикт.
- **Я не перечислял фактические типы, регистрируемые модулями в рантайме.** Поиск
  по литералам даёт **48** токенов вида `<модуль>_<слово>` в пяти сервисах, и
  большинство из них — имена таблиц и индексов, а не типы объектов. Единица счёта
  негодна, и число здесь названо **как негодное**, чтобы его не приняли за замер.
  Настоящий ответ даёт не поиск, а самопроверка миграции против **живых строк**
  (§2.7).
- **Я не мерил, сколько строк `resource_mirror` несут снятый тип** — по той же
  причине. Это названо блокирующим предусловием §8.
- **Круг 2: я не прогонял `drainer.Classify` на живом отказе.** Классификация
  установлена **по коду классификатора** (`classify.go:145`, предикат
  `st.Code() == codes.InvalidArgument || st.Code() == codes.PermissionDenied`), а
  не своим вызовом. Замок §6, который эту связь держит, прогона требует — и он
  входит в под-фазу.
- **Круг 3: прогон был, но только чистых функций.** Две временные пробы —
  `proxytuple.ValidateTuple` на восьми тройках и `authzmap.DottedType` на семи
  типах — заведены, прогнаны, **сняты**; `git status --porcelain` после снятия
  пуст. Ни одна из них не поднимает базы, поэтому всё, что я утверждаю о
  **строках** (число строк зеркала без референта, состав группы писателей
  отношений на живом стенде), остаётся неизмеренным и держится предусловием §8.
- **Круг 3: состав группы писателей отношений я мерил ПО МИГРАЦИЯМ, а не по
  базе.** Пять модулей — это то, что **посеяно** дереву; на стенде состав может
  отличаться, потому что группа заполняется из **действующего основания доступа**
  (`20260823002000:89`), то есть из строк, а не из литерала. Пять — ориентир для
  формы гейта, а не гарантия; сам гейт обязан печатать **обе** величины (§2.10).

---

## 1. Предмет

### 1.1. Что неверно

**У типа объекта проксируемого кортежа нет референта.** Полоса связывает тип
**приставкой** имени (`vpc` пишет `vpc_*`) и **не спрашивает**, существует ли такой
тип у платформы вообще. Выдуманный тип с законной приставкой принимается,
записывается в зеркало дословно и создаёт там строку, о типе которой не знает ни
один шаблон глаголов.

**«Дословно» здесь буквально, и это измерено прогоном (круг 3).** Приведение
`authzmap.DottedType` — поиск по карте, **без запасного пути**: незнакомый тип не
приводится вовсе и уезжает в зеркало с подчёркиванием
(`register_resource.go:208`, «unknown types are kept verbatim»).

```
vpc_network            знаком=true  -> в зеркало ложится "vpc.network"
nlb_listener           знаком=true  -> в зеркало ложится "loadbalancer.listeners"
vpc_totally_invented   знаком=false -> в зеркало ложится "vpc_totally_invented"
compute_disk           знаком=false -> в зеркало ложится "compute_disk"
```

Следствие, которое надо назвать сразу, потому что оно определяет форму решения:
строка зеркала с выдуманным типом **не в точечной форме**, а в форме модели с
подчёркиванием, — то есть она не промахивается мимо каталога, она **говорит на
другом языке**.

**Референт у типа в дереве ЕСТЬ — но он не там, где стояло в круге 2** (правка по
Б4, перемерена мной). Цепь авторитета одна, направление одно, и оба её звена
держатся гейтами:

| звено | что несёт | сколько | чем связано со следующим |
|---|---|---:|---|
| `proto/kacho/cloud/iam/v1/fga_model.fga` | **то, ЧТО умеет решаться** | 32 типа | `authzmap/canonical_model_drift_test.go` — каждый тип каталога обязан существовать в модели; обратное направление **намеренно** не требуется |
| `authzmap.objectTypes` | **то, что сервис умеет НАЗВАТЬ** — переходник «модель ↔ каталог» | 27 пар | `authzmap.CatalogSeedResources()` (`catalog_seed.go:50`) выводит посев **из** карты |
| `catalog_resource` строками | тот же словарь **данными**, на которые можно повесить ключ | 27 живых + 3 снятых | `seed.AssertCatalogParity` — сверка **двусторонняя**, отказ **фатален** для старта (`serve.go:1334`) |

Комментарий самой таблицы говорит это своим текстом: «Источник посева —
authzmap.objectTypes (27 живых) плюс domain.retiredTypes (3 снятых с
преемником)» (`20260901113757:192`).

**Значит референт есть КАРТА ПЛАТФОРМЫ, спроецированная в строки, а не строки
сами по себе.** Работа переносит инвариант в БД (запрет #10) — и не переносит
**авторитет**. Обратное утверждение было бы вторым местом об одном предмете, а
следующий читатель построил бы на нём портируемость, которой нет (§1.3).

Симметрично: **связь «домен учётки → модуль каталога» выражена соглашением в Go**,
а не строкой. `objectDomainForCaller` (`policy.go:208`) возвращает имя домена как
есть, а карта исключений `moduleObjectDomain` (`policy.go:157`) **пуста**. Пока
соглашение выполняется, всё работает; сказать «этот модуль владеет вот этими
типами» данными **нечем**.

### 1.2. Почему это дефект, а не уборка

Три следствия, каждое самостоятельно достаточное.

1. **Строка зеркала с типом вне каталога невидима отовсюду.** Реконсайлер выводит
   пообъектные глаголы из шаблона типа; типа, которого нет, шаблон не знает, и
   объект получает проекцию из нуля строк. Для арендатора это выглядит как
   «регистрация прошла, доступа нет» — то есть неотличимо от честного «права нет».
   Дерево уже платило за этот класс: `#1030` завёл `role_grant_orphan` именно
   потому, что исчезновение сегмента было **тише**, чем его отказ.
2. **Опечатка модуля становится необратимой строкой чужой БД.** `resource_mirror`
   не имеет ни одного внешнего ключа, а `object_id` — намеренно непрозрачная
   ссылка через границу сервиса. Строка с выдуманным типом не удаляется ничем,
   кроме административного вмешательства.
3. **Полоса — единственная запись, которую модуль делает в БД iam.** Всё
   остальное модуль получает чтением. Значит именно здесь проходит граница между
   «модуль привозит своё» и «модуль пишет в чужое».

### 1.3. Почему задача несущая для ПОРТИРУЕМОСТИ

Владелец требует: iam — **стандартный** образ; модуль привозит своё данными,
максимум сайдкар с применением. Замер по этой полосе:

| величина | значение | предикат |
|---|---:|---|
| типов ресурсов в переходнике `authzmap` | **27** | перепись блока карты `fga_types.go` |
| из них **чужих** iam (не `iam.*`) | **20** | 9 vpc · 3 compute · 3 loadbalancer · 3 storage · 2 registry |
| чужих типов, **вкомпилированных в общий фундамент** | **1** | `publicReadObjectTypes` = `{registry_repository}` (`policy.go:128`) |
| модулей в закрытом наборе Go | **6** | `services/iam/internal/domain/module_set.go:22` |

Первые две строки — предмет `#1030`, и он **закрыт**: словарь стал строками.

**Третья строка — предмет, который круг 2 ВЫНЕС из этой под-фазы** (`#1830`).
Круг 1 объявлял её частью работы; замечание Б2 показало, что единственная форма,
оставляющая правило чистым, литерала из фундамента **не убирает**, — то есть цели
не достигает. Разбор — §2.5.

#### Цена названа: НОВЫЙ ТИП ТРЕБУЕТ РЕЛИЗА ПЛАТФОРМЫ, и эта под-фаза её не снимает

Правка круга 3 по Б4. Прежняя редакция утверждала обратное; утверждение было
шире измеренного, и вот замер.

| вопрос | ответ | предикат |
|---|---|---|
| новый **модуль** привозит свои типы строками? | **нет** | строка `catalog_resource` обязана удовлетворять `CHECK (dotted = module \|\| '.' \|\| resource)`; приведение незнакомого типа даёт форму **без точки** — такой строки не бывает |
| новый **тип существующего** модуля привозится строкой? | **нет** | та же причина плюс двусторонний паритет: строка сверх литерала — `ExtraRows` → **отказ старта** |
| лишняя строка каталога расширяет словарь? | **нет**, роняет старт | `catalog_parity.go:121` + `serve.go:1334` |
| что тогда требуется для нового типа? | правка `fga_model.fga` **и** `authzmap.objectTypes` **и** миграция посева — **одним релизом** | цепь §1.1 |

**Это остаток, а не обещание.** Портируемость СЛОВАРЯ вынесена задачей-преемником
с признаком и предикатом (§3.5): её предмет — модель прав, а не эта полоса, и
трогать его здесь значило бы менять то, «что умеет решаться», под видом сужения
записи.

**Что под-фаза даёт при этой цене — три вещи, и каждая измерима:**

1. **выдуманный тип перестаёт приниматься** — сегодня принимается (§0.3, прогон);
2. **владение становится ДАННЫМИ, а не совпадением приставки.** Сегодня приставка
   совпадает с доменом у всех пяти писателей, и проверка владения **не меняет ни
   одного вердикта** — я это перемерил и говорю прямо (§16.3). Её ценность в том,
   что совпадение перестаёт быть условием работы: тип, чьё имя в модели не
   начинается с домена его модуля, сегодня невыразим, а после — выразим;
3. **инвариант переезжает на уровень БД** (запрет #10): забудут чтение — ключ всё
   равно не пустит.

Чего под-фаза **не** даёт и что раньше здесь заявлялось: выбора между «модуль
неработоспособен без релиза iam» и «модуль выписывает себе кластерные права» она
не снимает — она **выбирает первый рог, и говорит об этом вслух**. Вторая половина
(кластерные права) уже закрыта сегодня и приёмкой не достигается (§0.3).

### 1.4. ТРИ написания одного типа, которые здесь легко спутать

Это самое опасное место работы, и оно измерено, а не предположено.

| написание | пример | где живёт | предикат |
|---|---|---|---|
| **словарь модели** | `nlb_listener` | тело кортежа FGA, `relation_fact.object_type`, `resource_parent_edge.*_type` | `type_dictionaries.go`, шапка |
| **словарь каталога** | `loadbalancer.listeners` | `resource_mirror.object_type`, `role_verb.object_type`, `catalog_resource.dotted` | там же |
| **домен SAN** | `nlb` | `spiffe://kacho.cloud/ns/<ns>/sa/kacho-nlb` → `SANToServiceDomain` | `fgaproxy.go:176` |

Для балансировщика **все три различаются**, и дерево говорит это своим текстом:
`module_set.go:18` — «the load-balancer module token is `loadbalancer` (NOT `nlb`)»;
`fga_types.go:498` — `"loadbalancer.listeners": "nlb_listener"`; профиль
развёртывания даёт учётку `kacho-nlb`.

**Цена ошибки названа заранее.** Предикат, сравнивающий домен SAN с модулем
каталога напрямую, для балансировщика **не совпадёт никогда** — и не покраснеет, а
**отнимет живое право**: три типа (`nlb_listener`, `nlb_network_load_balancer`,
`nlb_target_group`) перестанут регистрироваться, а наблюдаемо это будет как
«ресурс создан, доступа нет».

Отсюда несущее требование §2.3: **через границу словарей значение переводит
единственный переходник, и перевод стоит в ОБЕИХ полосах сравнения.**

---

## 2. Решение

### 2.1. Членство типа в каталоге — КЛЮЧОМ, а не условием в Go

**Норма.** Тип объекта, приведённый к словарю каталога, обязан иметь референт в
`catalog_resource(dotted, live)`. Выражается это **ключом**: `resource_mirror`
получает колонку `live boolean NOT NULL DEFAULT true` с проверкой `CHECK (live)` и
внешний ключ `(object_type, live) → catalog_resource (dotted, live)`.

> [!important] Правка круга 3: что этот ключ УТВЕРЖДАЕТ, а что НЕТ (Б4)
> Круг 2 читал ключ как «каталог — авторитет словаря». Это неверно, и следующий
> читатель построил бы на нём портируемость, которой нет.
>
> Ключ утверждает **одно**: тип, попавший в зеркало, назван словарём, который
> платформа знает. Авторитет словаря — карта платформы; строки каталога суть её
> проекция, и держит эту проекцию двусторонний паритет, чей отказ **фатален для
> старта** (§1.1). Ключ переносит инвариант **на уровень БД** (запрет #10) — и
> только это.
>
> Почему ключ всё равно нужен, если словарь и так сверяется на старте: паритет
> судит **каталог**, а не **зеркало**. Он молчит о строке зеркала с типом вне
> словаря — её сегодня не видит ни одна проверка, и именно она есть предмет
> §1.2.

**Почему ключ, а не проверка.** Запрет #10: инвариант внутри одной БД выражается
конструкцией БД. Словарь и факт лежат в **одной** схеме `kacho_iam`, значит
«прочитать, затем записать» не нужно и запрещено — под конкуренцией со снятием
типа оно даёт запись после того, как тип перестал существовать.

**Почему именно эта форма ключа.** Она landed и её образец рядом — дословно:

```sql
ADD CONSTRAINT role_verb_type_fk
FOREIGN KEY (object_type, live)
REFERENCES kacho_iam.catalog_resource (dotted, live)
ON DELETE NO ACTION ON UPDATE NO ACTION
DEFERRABLE INITIALLY IMMEDIATE;
```

(`20260901113757_rule_segments_have_a_referent.sql:594`, плюс
`role_verb_live_true CHECK (live)` на `:572`.) Вторая форма не заводится: два ключа
об одном предмете разошлись бы, и разошлись бы молча.

**Ключ обязан быть `DEFERRABLE INITIALLY IMMEDIATE`.** `RESTRICT` рядом с
`DEFERRABLE` принимается DDL и **молча инертен** — измерено `#1030` на PostgreSQL 16
и вынесено в гейт формы ключа (`catalog_seed_parity.go`, шапка). Новый ключ судится
тем же гейтом.

**Следствие, которое обязано быть названо: ключ УПОРЯДОЧИВАЕТ снятие типа.**
`ON UPDATE NO ACTION` означает, что перевод строки каталога в снятые
(`live` true→false) при живой ссылающейся строке зеркала отвергается `23503` — тот
же исход, что у `role_verb` (`rule-segments-have-a-referent.md:107`). Это не
побочный эффект, а требуемое поведение: снятие типа при живых объектах и раньше
оставляло их без проекции — разница в том, что теперь это **видно отказом**, а не
тишиной.

**Шагов при этом ТРИ, а не два, и третий — релиз платформы** (правка круга 3;
круг 2 писал «два шага» и умалчивал третий):

| шаг | что делается | чем принуждается |
|---:|---|---|
| 1 | строки зеркала этого типа переселяются на преемника либо снимаются | новый ключ: иначе шаг 2 отвергается `23503` |
| 2 | строка каталога переводится в снятые (`live=false`, `retired_at`, `superseded_by`) | миграцией; `catalog_resource_pkey (module, resource)` держит одну строку на тип, поэтому «живая и снятая одновременно» невозможна |
| 3 | пара снимается из `authzmap.objectTypes` **в том же релизе** | иначе паритет даёт `MissingRows` (`want` берётся из литерала, `got` — `WHERE live`) → **отказ старта** |

**Промежуточного состояния между шагами 2 и 3 не существует.** Тип, снятый
строкой, но оставшийся в литерале, роняет старт; тип, снятый из литерала, но
живой строкой, роняет старт тоже. Отсюда следствие, важное для §5: полоса `live`
у ключа **сегодня не имеет достижимого входа с полосы прокси** — снятый тип не
приводится (его нет в карте), а живой не снят. Проверяется она поэтому на уровне
БД, а не сквозным вызовом (§16.0, п. 3).

### 2.2. Принадлежность типа МОДУЛЮ вызывающей учётки — ЯВНЫМ ЧТЕНИЕМ с названной кардинальностью

Ключ §2.1 говорит «тип существует» и **не** говорит «тип принадлежит этому
вызывающему»: личность вызывающего не является колонкой ни одной записываемой
строки, и внешним ключом это невыразимо by construction.

**Норма.** Заводится `kacho_iam.catalog_module_identity` — **три** колонки, и все
три обязательны:

| колонка | что несёт | почему не выводится |
|---|---|---|
| `module` | имя модуля в словаре каталога (`loadbalancer`) | ключ в `catalog_module(module)` |
| `san_domain` | короткое имя службы из проверенного SAN (`nlb`) | **то, что есть у полосы запроса**; для балансировщика ≠ `module` (§1.4) |
| `sa_id` | `sva`+md5 идентификатор модульной учётки | ключ в `service_accounts(id)`: строка не вправе называть несуществующую учётку |

Плюс `UNIQUE (san_domain)`: один домен принадлежит одному модулю. Без этого два
модуля могли бы объявить один домен, и «чей это тип» перестало бы иметь ответ.

#### Кому строка ПОЛАГАЕТСЯ — по признаку, а не по соседнему перечню (правка круга 3, Б5)

Круг 2 назвал носителей перечнем `domain.KnownModules()` — **шесть** имён. Перечень
не тот, и я перемерил это своими командами:

```sh
sed -n '22p' services/iam/internal/domain/module_set.go
# {"iam", "vpc", "compute", "loadbalancer", "registry", "storage"}    ← шесть
git grep -ohE "'kacho-[a-z-]+'" -- services/iam/internal/migrations | sort -u | grep -c "^'kacho-iam'$"
# 0 — модульной учётки kacho-iam в дереве НЕТ ни в одной миграции
```

**Два перечня описывают разные предметы, и совпали они случайно:**
`domain.KnownModules()` — это **грантуемый домен правила роли** (над чем правило
может выдать право); строка identity — это **вызывающий проксируемой полосы** (кто
приходит с проверенным сертификатом и пишет). Разошлись они на `iam`, и разойдутся
снова на всяком домене, который грантуем, но проксируемой записи не делает.

**Признак, по которому строка полагается:** модуль, чья учётка **состоит в группе
писателей отношений** — то есть держит `fga_writer` на кластере и потому вообще
доходит до этой полосы. Он **читается данными**, а не перечнем:

```sql
SELECT member_id FROM kacho_iam.group_members
 WHERE group_id    = 'grp' || substr(md5('module.relation_writers'), 1, 17)
   AND member_type = 'service_account';
```

Это тот же состав, который спрашивает сам гейт записи
(`20260823002000_relation_write_moves_onto_the_cluster.sql:89` — группа заполнена
**из действующего основания доступа**, а не из литерала).

**Сегодня их пять**, и число снято по дереву, а не по базе (граница названа в
§0.5): `vpc` · `compute` · `nlb` · `registry` · `storage` — по одной выдаче
`fga_writer` в миграциях `0009` (три), `0044`, `0057`. Учётки `kacho-api-gateway` и
`kacho-vpc-operator` существуют и `fga_writer` **не получают** — комментарий `0009`
говорит это дословно; строка identity им не полагается.

**Заводить строку `kacho-iam` ради счёта — не исход.** Это принципал, которого
никто не предъявляет: `iam` собственную проксируемую RPC не зовёт, домена SAN у
него в этой роли нет, а `ServiceAccountIDForService("iam")` назвал бы учётку,
строки под которую в дереве не существует. Расширять поверхность ради зелёного
гейта запрещено.

**Членство в группе ключом НЕ выражается, и это сказано прямо.** Первичный ключ
`group_members` — тройка `(group_id, member_type, member_id)`, поэтому внешний ключ
из `catalog_module_identity(sa_id)` на «члена именно этой группы» невыразим без
второго уникального индекса, который был бы вторым местом об одном предмете.
Признак держат **самопроверка миграции** (§2.7 п. 3) и **гейт паритета** (§2.10
п. 1), а не конструкция БД, — и это ослабление названо, а не спрятано.

**Форма проверки — ДВА чтения, у каждого кардинальность 0 либо 1, оба в ТОЙ ЖЕ
транзакции, что и запись, и до неё:**

```sql
-- (1) личность вызывающего
SELECT module FROM kacho_iam.catalog_module_identity
 WHERE san_domain = $1 FOR SHARE;
-- (2) владелец типа; на полосе СНЯТИЯ — без предиката live (см. асимметрию ниже)
SELECT module FROM kacho_iam.catalog_resource
 WHERE dotted = $2 AND live FOR SHARE;
```

Дискриминатор — **пара** исходов, и он однозначен:

| исход | что означает | §2.6 |
|---|---|---|
| (1) ноль строк | у вызывающего нет объявленной личности | отказ принадлежности |
| (2) ноль строк | типа нет в каталоге живым | отказ референта |
| `module` (1) ≠ `module` (2) | тип чужой | отказ принадлежности |
| обе строки, `module` совпал | принято | — |

> [!important] Правка круга 2: прежняя форма `INSERT … SELECT … WHERE EXISTS` СНЯТА
> Круг 1 предлагал вплавить сверку в оператор записи и читать **ноль вставленных
> строк** как отказ. Замечание Б3 верно, и я перемерил его сам, прочитав обе
> полосы, которые предлагалось изменить:
>
> - **регистрация** — `resource_mirror/emitter.go:177-186`: `ON CONFLICT … DO
>   UPDATE … WHERE resource_mirror.source_version < EXCLUDED.source_version`;
>   `applied := tag.RowsAffected() > 0`, и `Outcome{Applied: false}` **возвращается
>   без ошибки** (`emitter.go:198`). Ноль строк здесь — **успех**: повторная
>   доставка дренажа, которую монотонный страж правильно погасил;
> - **снятие** — `emitter.go:321-328`: `DELETE … WHERE source_version <= $3`, и
>   результат **отбрасывается** (`if _, err := tx.Exec(...)`). Ноль строк —
>   молчаливый успех **намеренно**: контракт RPC объявляет «Idempotent: absent
>   tuple → OK, never NotFound» (`handler.go:210`).
>
> Прочесть ноль как отказ значило бы сломать идемпотентность, которую доставка
> «хотя бы однажды» делает **штатным** режимом; прочесть как успех — сделать
> сужение бездействующим. Третьего этот оператор не даёт.
>
> Явное чтение двузначности не имеет **by construction**: у `SELECT` по
> уникальному ключу кардинальность ноль означает ровно «строки нет», и повторная
> доставка на неё не влияет никак.

**Почему это НЕ check-then-act (запрет #10).** Три причины, каждая самостоятельно
достаточная:

1. **инвариант держит ключ §2.1, а не чтение.** Забудут чтение — запись не пройдёт
   всё равно. Чтение производит **сообщение**, а не гарантию;
2. **чтение и запись — одна транзакция**, а не две: «прочитал → записал» с окном
   между ними здесь невыразимо;
3. **`FOR SHARE` держит строку каталога до коммита**, поэтому снятие типа,
   начатое после чтения, ждёт этой транзакции.

Образец рядом и он назван в дереве дословно: «this FK is what closes the
delete-vs-attach TOCTOU (the software refcheck is only a best-effort early
message)» (`pgmaperr.go:331`). Настоящая работа повторяет этот приём, а не
изобретает свой.

**АСИММЕТРИЯ полос названа явно, и она несущая.** Проверка **принадлежности**
стоит на **обеих** полосах — модуль не вправе снимать чужую регистрацию.
Проверка **живости** типа стоит **только на регистрации**:

| | регистрация | снятие |
|---|---|---|
| личность вызывающего | требуется | требуется |
| тип принадлежит вызывающему | требуется | требуется |
| тип **жив** в каталоге | **требуется** | **НЕ требуется** |

**Почему так.** Снятие ничего не создаёт. Потребовать живости на снятии значило бы
запретить отзыв регистрации у типа, который только что сняли, — и тогда выдача
**пережила бы свой предмет**, а это over-grant, то есть ровно та беда, крупнее
предотвращаемой. Ключ §2.1 этой асимметрии не мешает: внешний ключ проверяется на
вставке и не проверяется на `DELETE` by construction.

**Что это даёт портируемости — и чего НЕ даёт (переписано кругом 3, Б4).**

Даёт: **связь «домен учётки → модуль каталога» перестаёт быть соглашением в Go и
становится строкой.** Прежняя карта исключений (`policy.go:157`, сегодня пустая)
перестаёт быть точкой расширения и **снимается вместе со своим предметом** — иначе
останется вторым местом об одном предмете.

Не даёт: **словарь типов остаётся в релизе платформы.** Новый модуль привозит
строку identity — но не привозит своих типов: строка `catalog_resource` для типа
вне карты невозможна `CHECK`-ом, а лишняя строка роняет старт (§1.3, таблица
цены). Круг 2 писал здесь «ни закрытый набор Go править не требуется»; это было
заявление шире измеренного, и оно снято. Предмет вынесен задачей-преемником
(§3.5).

### 2.3. Через границу словарей переводит ЕДИНСТВЕННЫЙ переходник, и перевод стоит с ОБЕИХ сторон

**Норма.** Значение, уезжающее в чужой словарь, переводится
`authzmap.DottedType` — и больше ничем. Второй переходник не заводится ни в SQL,
ни в Go: `type_dictionaries.go` объявляет это прямо, и настоящая работа этого
объявления не отзывает.

Практически: тип кортежа (`nlb_listener`, словарь модели) приводится к
`loadbalancer.listeners` **до** чтения §2.2, и сравнивается с
`catalog_resource.dotted` — колонкой того же словаря. Соединение колонок **разных**
словарей не совпало бы никогда и молча.

**Признак нарушения, который рецензент обязан искать:** в чтении стоит
`object_type` без приведения, либо приведение стоит **в одной** полосе из двух
(например, на регистрации и не на снятии). Односторонний перевод даёт проверку,
зелёную на всём сломанном. Сценарий, утверждающий это, — `IAM-MV-04` (§5.1): он
берёт **снятие** типа балансировщика, то есть ту полосу, где односторонний перевод
и остаётся незамеченным.

**Колонка с именем типа в словаре модели у `catalog_resource` НЕ заводится.**
Соблазн велик — она сделала бы чтение короче, — но это второе место, знающее
соответствие, и разойдётся оно тихо, на новом типе, которого в копии ещё нет.

### 2.4. Отношение с `catalog_verb` НЕ сверяется — и это решение, а не пропуск

Задача просит: «имя вне множества, производимого шаблоном, отвергается; `v_get`
проходит». Обещание **снимается**, и вот замер, из которого:

```sh
# что несёт каталог глаголов
sed -n '275,$p' services/iam/internal/migrations/20260901113757_rule_segments_have_a_referent.sql \
  | grep -oE "', '[a-zA-Z]+'\)" | tr -d "',)" | sort | uniq -c | sort -rn
#   27 list · 27 get · 26 update · 26 delete · 1 removetargets · 1 create · 1 addtargets
# сколько строк каталога несут отношения проксируемой полосы
for r in project parent owner v_get; do … done
#   project -> 0 · parent -> 0 · owner -> 0 · v_get -> 0
```

**109 строк каталога, ноль из проксируемых отношений.** Сверка отношения с
`catalog_verb` отвергла бы **каждую** законную проксируемую запись — то есть класс
из §1.4 в чистом виде, только с ценой в 100 % трафика полосы.

**Причина структурная, а не количественная.** `project` / `parent` / `owner` — это
**иерархия модели прав**, а не действия над ресурсом. Они объявлены в
`fga_model.fga`, принадлежат iam и модулем не привозятся: новый модуль не заводит
нового вида родства. Значит их закрытый набор — законное знание iam, портируемости
не мешает и остаётся там, где стоит (`policy.go:77`).

**И `v_get` проходит НЕ как отношение.** Измерено в §0.3: `v_get` с **именованным**
субъектом отвергается; проходит только пара `user:*` + `v_get` на типе из закрытого
списка. Формулировка задачи «`v_get` проходит» верна лишь для этой пары, и сценарий
§5.1 утверждает именно её.

### 2.5. Публичность ресурса — ВЫНЕСЕНА из под-фазы задачей `#1830` (правка круга 2)

Круг 1 предлагал: `catalog_resource` получает колонку `public_read_allowed`,
список `publicReadObjectTypes` (`policy.go:128`) снимается, а `publicReadAllowed`
спрашивает каталог **тем же оператором** §2.2.

**Замечание Б2 верно, и я перемерил его тремя своими прогонами:**

1. **правило чисто, и его чистота — landed-решение, а не случайность.**
   `git grep -nE 'database/sql|pgx|"context"' -- pkg/authz/proxytuple` → **пусто**;
   `handler.go:175` объявляет это дословно: «the rule itself is transport-free **by
   design** … so a consumer's domain layer can import it without taking a
   dependency on gRPC»;
2. **на этой чистоте держится существующий гейт.** `proxytupleintent_test.go`
   прогоняет тройку **каждого** эмитента через `ValidateTuple` **на сборке, без
   БД**. Функция, которой нужен каталог, делает его неисполнимым — а §9.1 требует,
   чтобы он остался зелёным;
3. **у этой полосы вообще нет строки зеркала.** Публичная выдача — **чистый
   кортеж** (`register_resource.go:224`, `isPureGrant`); ни ключ §2.1, ни чтение
   §2.2 её не касаются **by construction**. «Тем же оператором» не имело референта.

**Исход — второй из двух названных Б2: предмет вынесен целиком, с номером.**
Первый исход (спрашивать публичность в use-case, оставив закрытый список в
фундаменте вторым рубежом) я рассмотрел и **отверг**: он сохраняет литерал в
фундаменте, то есть цели §2.5 — «новый модуль с публично читаемым ресурсом не
требует релиза фундамента» — **не достигает**, а взамен заводит два места об одном
предмете.

Задача-преемник — **`#1830`**. У неё свой предмет и своя цена: перенос делает
отказ **ослабляемым строкой**, то есть переворачивает умолчание, и это обязано
пройти свою модель угроз (`security.md` §«Пустой список — это „не сужаем"»).

**Под-фаза от выноса не перестаёт быть production-complete** (§2.11): референт и
принадлежность — целое без публичности, а публичность — соседняя полоса, у которой
нет ни строки зеркала, ни ключа.

### 2.6. Транспорт отказа: полос ДВЕ, коды РАЗНЫЕ, обе ТЕРМИНАЛЬНЫ (переписано кругом 2)

Круг 1 объявлял, что текст на проводе не меняется, и указывал производителем
`handler.go:182` с замком `:37`. **Замечание Б1 верно**, и его существо я
подтвердил своим прогоном цепочки; предикат самого Б1 при этом **опровергнут** —
разбор в §14.1.

**Цепочка, установленная своим прогоном:** отказ, рождённый в БД, идёт
`resource_mirror` → `fmt.Errorf` (`emitter.go:190`, `:327`) → `uc.emit`
(`register_resource.go:762`, `:783`, снова `fmt.Errorf`) → `handler.go:204`/`:226`
→ `shared.MapRepoErr`. Тот sentinel-ориентирован (`errors.go:67`—`:100`), SQLSTATE
не читает, и несентинельная ошибка проваливается в хвост `errors.go:108` →
**`Internal "internal error"`**. Перевод SQLSTATE в дереве **есть** (`wrapPgErr`,
`pgmaperr.go:45`, `23503` → `ErrFailedPrecondition`), но он **неэкспортирован** и
живёт в пакете `pg`, а зеркало — отдельный пакет: на этой полосе он недостижим.

**Почему это не косметика, а несущее — замер, а не рассуждение.**
`pkg/outbox/drainer/classify.go:145`:

```go
return st.Code() == codes.InvalidArgument || st.Code() == codes.PermissionDenied
```

Постоянными для применяющего дренажа модуля являются **ровно два** кода. Всё
прочее — включая `Internal` и `FailedPrecondition` — **временное**, и комментарий
того же файла называет цену временной классификации отказа, который повтором не
лечится: строка держится на попытку ниже порога отравления, из блокирующего набора
заявки не выбывает, и **каждая последующая строка её партиции не клеймится
никогда**. Снятие регистрации стоит в очереди за регистрацией — значит выдача
переживает предмет, который её обосновывал.

Отсюда **исход (а) круга 1 — «переписать „Тогда" под то, что производится
сегодня» — исходом НЕ является**: он посадил бы полосу, чей отказ дренаж модуля
читает временным.

**Норма. Полосы две, у каждой свой код, и обе в постоянном наборе:**

| полоса | что произошло | код | текст | постоянен |
|---|---|---|---|---|
| **референт** (§2.1, §2.2 чтение 2) | вход называет тип, которого в каталоге нет живым | `InvalidArgument` | называет **приведённый тип, который прислал сам вызывающий**, и ничего больше | **да** |
| **принадлежность** (§2.2 чтение 1 и сверка) | тип чужой либо личности вызывающего нет | `PermissionDenied` | **дословно** `permission denied` | **да** |
| **полоса А** (`proxytuple`, форма запроса) | грамматика · отношение вне набора · запрещённый тип · чужая приставка | `PermissionDenied` | **дословно** `permission denied` | **да** |

**Полоса А не трогается вовсе.** Замок `proxy_tuple_refusal_transport_test.go`
остаётся как есть, `handler.go:180` — как есть.

**Почему у принадлежности текст ОПАКОВЫЙ и байт-в-байт с полосой А.** Это решение
о доступе; назвать оговорку значило бы ответить вызывающему на вопрос «а чей это
тип», то есть отозвать landed-решение `handler.go:178` («which clause refused is
deliberately not observable»). Байт-идентичность здесь того же рода, что у
скрытия существования (`security.md` §Hardening, п. 6): различимый текст сам по
себе есть оракул.

**Почему у референта текст НАЗЫВАЕТ тип — и почему это не оракул.** Текст
возвращает вызывающему **его собственный ввод**, приведённый единственным
переходником. Узнать по нему можно ровно одно: «типа, который я назвал, в каталоге
нет живым». Чужой тип вызывающий назвать не может — полоса А отвергает чужую
приставку **до** этого чтения; значит вопрос задаётся только о своём домене.

> [!important] Правка круга 3: текст НЕ ВПРАВЕ называть действие, которое ломает продукт (Б4)
> Круг 2 писал здесь: отказ «говорит, что делать дальше — **завести строку
> каталога**». Это указание **неисполнимо и вредно**, и оба свойства измерены:
> строка каталога для типа вне карты платформы невозможна `CHECK`-ом
> (`dotted = module || '.' || resource`), а заведённая руками строка сверх
> литерала даёт `ExtraRows` и **роняет старт iam** (`serve.go:1334`). Отказ,
> называющий действие, которое ломает продукт, **хуже опакового**.
>
> **И различить два случая полоса не может — это надо сказать вслух.** Опечатка
> `vpc_totally_invented` и законный новый `vpc_newthing` приводятся одинаково
> (дословно, обе мимо каталога) и получают один и тот же отказ. Различает их
> только человек, знающий, заводился ли тип релизом.
>
> **Что текст говорит вместо:** называет **приведённое значение ровно так, как оно
> уезжало бы в зеркало** — `vpc_totally_invented`, с подчёркиванием, потому что
> точечной формы у незнакомого типа не бывает (§1.1, прогон), — и сообщает, что
> это значение не названо словарём платформы. Ни одного действия текст не
> предписывает: «опечатка» и «тип ещё не заведён» неразличимы отсюда, и
> подсказывать одно из двух значило бы угадывать за вызывающего.
>
> Ось «разработчик» ban #18 при этом **не теряется, а становится честной**: время
> до диагноза падает с «читать чужой журнал» до «прочитать в отказе, какое
> значение сервис не узнал». Что делать с этим значением — вопрос, у которого два
> ответа, и приёмка не выбирает за разработчика.

**Нового токена полосы (`pkg/errors.Reason`) НЕ заводится, и это решение с
названным признаком отказа.** Словарь закрыт **компилятором**
(`pkg/errors/reason_closed_test.go`: «Гейт „компилятор — на шестом токене"»); его
пять значений описывают полосы резолва **идентификатора**, и ни одна из них не
эта. Единственный машинный потребитель этого отказа сегодня — `drainer.Classify`,
и он ключуется на **код**, а не на токен. **Признак, по которому решение
отвергается:** появится второй потребитель, которому кода недостаточно, — токен
заводится вместе с ним, а не «на будущее».

**Различимость оговорки — в журнале и счётчике, а не на проводе:**

- **в журнале** — запись уровня предупреждения, называющая домен вызывающего,
  приведённый тип и **какая** оговорка отказала. Производителя сегодня нет
  (`h.logger` в `handler.go` встречается один раз, и это присваивание) — он
  **заводится этой работой**;
- **счётчиком** — отказы полосы считаются по оговорке. «Ноль отказов за всю жизнь
  контроля» обязано быть заметно, иначе мёртвый контроль невидим
  (`security.md` §Hardening, п. 8).

**Держатель — НОВЫЙ замок, и он зовёт НАСТОЯЩИЙ классификатор.** `drainer.Classify`
экспортирована (`classify.go:82`), поэтому замок не переписывает классификацию, а
**спрашивает** её: для отказа каждой из двух полос он утверждает (а) код, (б)
дословный текст либо его форму, (в) `drainer.Classify(err) == ClassPermanent`.
Второго места об одном предмете не заводится by construction. Существующий замок
этого не покрывает и покрыть не может: он зовёт `validateProxyTuple` **напрямую**
(`proxy_tuple_refusal_transport_test.go:30`) и полосы БД не касается.

### 2.7. Миграция проверяет СВОЙ исход и отказывает, а не догадывается

`ADD CONSTRAINT` на неполном каталоге **отбирает у арендатора живой доступ**, а
применённую миграцию править нельзя (запрет #5). Поэтому миграция, прежде чем
навесить ключ, считает и роняет применение при ненуле:

1. строки `resource_mirror`, чей `object_type` не имеет живого референта в
   `catalog_resource(dotted, live)` — включая строки **снятых** типов
   (`compute.disk`, `compute.image`, `compute.snapshot`: три записи с `live=false`,
   `20260901113757:231`);
2. строки `catalog_module_identity` без референта в `catalog_module` либо в
   `service_accounts`;
3. **члены группы писателей отношений** без строки identity — иначе чтение §2.2
   отвергнет их запись, а миграция об этом промолчит. Направление именно такое:
   считается тот, кто **до полосы доходит**, а не тот, над кем правило может
   выдать право. Круг 2 считал здесь модули каталога и обещал шесть — счётчик был
   бы вечно ненулевым, потому что у `iam` модульной учётки не существует и не
   должно существовать (§2.2, Б5); миграция не применилась бы **ни разу**.

Текст отказа называет обе величины (найдено · ожидалось) и номер задачи — образец
рядом: `20260901113757:620`.

**Ненулевой счётчик по п. 1 — не повод поднять потолок.** Исходов два: перенести
строку на живой тип-преемник (`catalog_resource.superseded_by` заведён `#1030`
ровно для этого) либо снять её вместе с предметом. Ослабление ключа исходом **не**
является.

### 2.8. Что происходит с уже выданными правами — по каждому носителю

| носитель | что с ним | почему |
|---|---|---|
| кортежи в модели прав | **не трогаются** | приёмка сужает **запись**, а не отзывает записанное; отзыв — отдельный предмет `#1828` |
| строки `resource_mirror` с живым типом | **не трогаются** | ключ на них сходится by construction |
| строки `resource_mirror` со снятым типом | **предмет самопроверки** §2.7, п. 1 | их число неизвестно (§0.5) и обязано быть названо до посадки |
| проекции глаголов, выведенные реконсайлером | **не трогаются** | их источник — роли, а не эта полоса |
| право `fga_writer` у модульных учёток | **не трогается** | сужается предмет записи, а не способность писать; способность — `#1829` |
| **снятие регистрации у снятого типа** | **остаётся возможным** | асимметрия §2.2: иначе выдача пережила бы свой предмет |
| **порядок снятия типа впредь** | **три шага** — строки зеркала, затем `live=false`, затем снятие пары из карты платформы **тем же релизом** | следствие ключа и паритета; таблица шагов — §2.1 (правка круга 3: круг 2 называл два и умалчивал третий) |

### 2.9. Обратного пути нет, и причина названа

Секция `+goose Down` снимает ключи и таблицу identity. Она **не восстанавливает**
строки, снятые оператором по §2.7 п. 1, — и это сказано в самой миграции, а не
подразумевается: обещание отката, которого нет, хуже отсутствия отката.

### 2.10. Впредь свойство держит ГЕЙТ, а не эта миграция

Миграция — событие; свойство держат два гейта дерева:

1. **паритет identity** (переписан кругом 3, Б5): у каждой модульной учётки,
   которой миграции выдают `fga_writer`, есть ровно одна строка identity, её
   `sa_id` совпадает с `ServiceAccountIDForService(san_domain)`, а `module` —
   живая строка `catalog_module`. Набор **выводится обходом миграций**, а не
   выписывается: гейт ищет выдачи `fga_writer` в очередь намерений тем же
   способом, каким это уже делает
   `internal/repohygiene/grantedrelationinmodel_test.go` (он разбирает миграции на
   выдачи и на пустом обходе падает). Зеркальная сторона обязательна: **строка
   identity без выдачи `fga_writer`** — тоже находка, иначе перечень разрастётся
   принципалами, которых никто не предъявляет.

   **Перепись печатает ОБЕ величины** — «писателей N · строк identity M», — потому
   что одно число скрывает ровно тот случай, ради которого гейт заведён: при
   `N == M == 0` односоставная перепись выглядела бы так же, как при совпадении.
   Сегодня N — **пять** (ориентир §0.5, не гарантия: на стенде состав группы
   заполняется из строк, а не из литерала);
2. **отсутствие второго переходника**: в дереве ровно одно объявление соответствия
   «имя модели ↔ имя каталога» (`authzmap`), и ни SQL, ни новая полоса своего не
   заводят. Гейт судит по **узлу разбора**, а не по слову: имена типов встречаются
   в комментариях, объясняющих эту же защиту, и текстовый поиск краснел бы на
   собственном объяснении.

Оба печатают объём осмотренного и падают на пустом обходе.

### 2.11. Production-complete в своих границах

Под-фаза несёт: миграцию с самопроверкой · таблицу identity с тремя ключами ·
чтение с названной кардинальностью на **обеих** проксируемых полосах, с
асимметрией живости · приведение словаря с **обеих** сторон · снятие пустой карты
исключений вместе с предметом · **перевод отказа полосы в два кода, оба
постоянные** · журнал и счётчик отказов · два гейта дерева · замок транспорта,
зовущий настоящий классификатор · сценарии обеих сторон с положительными
контролями.

Отложенное вынесено **задачами с номерами**, а не обещаниями: `#1828` (отзыв
разности), `#1829` (отзыв способности у неактуальных учёток), `#1830`
(публичность строкой каталога) и — правкой круга 3 — `#1092` (портируемость
**словаря типов**, §3.5). Ни одна из четырёх не является частью предмета этой
под-фазы: первые две **отнимают** доступ, третья живёт на полосе, у которой нет ни
строки зеркала, ни ключа, четвёртая меняет **цепь авторитета словаря**, а не
запись кортежа.

**Границы под-фазы названы вслух, а не подразумеваются** (правка круга 3): она
**не** делает словарь типов данными и **не** снимает необходимости релиза
платформы для нового типа. «Production-complete в своих границах» означает здесь
именно это — границы очерчены и цена за ними названа (§1.3), а не «доделаем
потом».

### 2.12. Пять вопросов (запрет #18) — с признаком отказа у каждого

| вопрос | признак отказа | ответ |
|---|---|---|
| **благо продукта** | единственный выигрыш — меньше работы сейчас | нет: работы становится **больше** (миграция, таблица, два гейта, замок). Выигрыш у арендатора — регистрация с выдуманным типом перестаёт молча создавать невидимый объект. **Пересчитано кругом 3 (Б4):** выигрыш **не** в портируемости словаря — она не достигается и вынесена (§3.5); он в том, что инвариант переезжает на уровень БД и перестаёт зависеть от совпадения приставки с доменом (§1.3, три пункта) |
| **конкурентоспособность** | довод только «как у …» | сравнений нет. Своя ось — предсказуемость контракта: «регистрация прошла» перестаёт означать «объект может оказаться без прав». **Оговорка круга 3:** время до диагноза падает («в отказе названо значение, которого сервис не узнал»), но время до **починки** не падает у случая «тип ещё не заведён» — там нужен релиз платформы, и §2.6 это не скрывает |
| **перспективность** | ошибка необратима | обратима: внешне-адресуемых координат не трогает, данных арендатора не мигрирует. Необратим лишь снос строк по §2.7 п. 1 — и он назван, сосчитан и решается человеком. **Пересчитано кругом 3:** отзыв решения не требует миграции данных арендатора и здесь — ключ и таблица снимаются `Down`-секцией; необратимой координаты под-фаза не заводит **ни одной** |
| **безопасность** | поверхность расширяется, и расширение не названо | поверхность **только сужается**, и это перемерено кругом 3 в обе стороны: входов, принимаемых после и отвергаемых сегодня, — **ноль** (§16.3). Расширений в под-фазе не осталось вовсе: единственное, что круг 1 называл расширением (колонка публичности), вынесено в `#1830` вместе со своей моделью угроз |
| **производственность** | есть режим только для стенда либо часть «на потом» | нет: полоса одна на все посадки. Пустой домен вызывающего (§0.3) закрывается чтением §2.2, а не вторым режимом. Отложенное вынесено **четырьмя** задачами с номерами (§3), а не обещаниями |

---

## 3. Чего эта приёмка НЕ решает — по каждому пункту НОМЕР

### 3.1. Отзыв уже записанных кортежей вне каталога — `#1828`

Сужение действует **вперёд**. Кортеж, записанный до посадки на тип вне каталога,
продолжает существовать в модели прав, и ключ §2.1 его не касается by
construction: ключ судит строку зеркала, а кортеж живёт у внешнего хранилища прав.

Цена другая: `#1031` не отнимает ничего, а отзыв разности **отнимает доступ** —
значит ему нужен свой замер и своё решение владельца.

**Номер: `PRO-Robotech/kacho#1828`** (заведена кругом 2; круг 1 оставлял её
названным предусловием без номера, и это блокировало §8 п. 5).

### 3.2. Отзыв способности `fga_writer` у неактуальных модульных учёток — `#1829`

Посев `0009` завёл учётки, среди которых есть компонент, чьи рёбра сняты решением
владельца 2026-08-09 (`polyrepo.md`, врезка). Учётка и выданное ей право заведены
**применёнными** миграциями и снимаются новой.

Это другой предмет: «кто вправе писать» против «что он вправе записать», и путать
их нельзя — сужение предмета записи не отзывает способности, а отзыв способности
не сужает предмета.

**Номер: `PRO-Robotech/kacho#1829`.**

### 3.3. Публичность ресурса строкой каталога — `#1830` (вынесено кругом 2)

Разбор — §2.5. Кратко: единственная форма, оставляющая правило проксируемой записи
чистым, литерала из фундамента не убирает, то есть цели не достигает; а полоса
публичной выдачи не пишет строки зеркала вовсе, поэтому ни ключ §2.1, ни чтение
§2.2 к ней неприменимы.

**Номер: `PRO-Robotech/kacho#1830`.**

### 3.4. Пустой домен вызывающего в dev-посадке — не находка

`fgaproxy.go:103` возвращает `("", nil)` невыверенному пиру при `prodMode=false`.
Запрет #16 объявляет dev-посадку на поднятом стенде снятой, значит производителя у
этого состояния на допускаемой продуктом посадке нет. Чтение §2.2 закрывает полосу
и там — но приёмка **не** записывает это в свои заслуги и не заводит задачу: у
находки нет предмета.

### 3.5. Портируемость СЛОВАРЯ типов — задача `#1092`, и она уже существует (круг 3, Б4)

Предмет, который круг 2 заявлял достигнутым: «новый модуль привозит свои типы
строками». Он **не достигается** этой под-фазой (§1.3, таблица цены), и заводить
под него новую задачу не нужно — она заведена и описывает ровно этот предмет:

> **`#1092`** — «iam: таблицы типов ПОРОЖДАЮТСЯ из манифеста вместо гейта
> дрейфа» (`blocked` · `P1` · `size:L` · `area:iam` · `release:modules`,
> `Ref: #1087`). Её предикат сформулирован ровно тем словом, которым здесь
> названа цена: «после него **„добавить тип" перестаёт быть правкой Go**».

**Почему предмет принадлежит ей, а не этой под-фазе.** Словарь типов есть проекция
**модели прав** — того, «что умеет решаться» (§1.1). Сделать его данными значит
сделать данными модель, а это меняет цепь авторитета целиком; сужение записи
кортежа такого права не имеет. Граница проведена там, где меняется предмет, а не
там, где кончились силы.

**Что эта под-фаза оставляет `#1092` в лучшем виде, чем взяла:** к моменту её
исполнения словарь уже будет иметь **ключ** в БД, поэтому порождение таблиц из
манифеста получит готового энфорсера вместо ещё одного гейта дрейфа — то есть
ровно то, что `#1092` и просит («заменить **сверку** на **вывод**»).

**Смежная задача того же класса, названная, чтобы её не завели заново:** `#1824`
(«роли объявлены миграциями iam, а не данными модуля — новый модуль требует релиза
iam») — та же цена на соседней оси. Ни одна из двух этой приёмкой не закрывается и
не блокируется.

---

## 4. Out-of-scope — явно

- **Общий фундамент `pkg/authz/proxytuple` не трогается ни одной строкой.**
  Предикат, который обязан остаться пустым после работы:
  `git grep -nE 'database/sql|pgx|"context"' -- pkg/authz/proxytuple`.
- **Каталог прав (`permission`/`exempt_reason`) не трогается.** Обе RPC остаются
  `<exempt>` на внутреннем слушателе: их сужает ReBAC-гейт, а не пообъектный Check.
- **`proto/` не трогается.** Ни поле, ни глагол, ни аннотация.
- **Модель прав (`fga_model.fga`) не трогается.** Ни отношения, ни типы.
- **Форма очереди намерений не трогается.** `fga_outbox` разделяется многими
  производителями (посев старта, JIT, аварийный доступ, эта полоса —
  `services/iam/internal/repo/kacho/pg/fga_outbox/emitter.go:131`), и сужение,
  положенное в общий эмитент, отвергло бы собственные выдачи iam на `account` /
  `project` / `cluster`, которых в каталоге ресурсов нет. Чтение стоит **на полосе
  прокси**, а не в общем эмитенте.

> [!important] Правка круга 3: ключ §2.1 связывает ТРИ писателя зеркала, а не одного
> Замечание §15.4 п. 1 верно, и я перемерил его своей командой:
>
> ```sh
> git grep -n 'INTO kacho_iam.resource_mirror' -- '*.go' | grep -v _test
> # resource_mirror/emitter.go:177   — полоса прокси (предмет приёмки)
> # backfill_adapter.go:224          — SeedSmokeMirrorObject, страж загрузки
> # scalegrid/seed.go:180            — посевщик нагрузочного стенда
> ```
>
> **Ключ — инвариант ТАБЛИЦЫ, а не полосы.** Формулировка «сужается полоса прокси»
> верна для *чтения* §2.2 и **неверна** для ключа: он свяжет всех троих, включая
> тех, кто о приёмке не знает.
>
> Совместимость проверена, а не предположена, по каждому из двух остальных:
>
> | писатель | что кладёт | референт жив |
> |---|---|---|
> | страж загрузки | `smokeMirrorType = "vpc.network"` (`seed/verify_gate.go:66`), литерал в **другом** пакете; рядом уже стоит своя проверка по закрытой таблице (`:321`) | **да** |
> | посевщик нагрузки | точечные типы из своей записи (`scalegrid/seed.go:93`, godoc: «тип в словаре КАТАЛОГА»); прод-импортёров у пакета **ноль** | **да** |
>
> Находки нет — но §9.1 обязан перепрогнать пробы обоих, а не только полосы
> прокси, иначе первый же новый тип у стража загрузки упрётся в ключ, о котором
> его автор не знал.
- **Классификация исходов дренажа не трогается.** `pkg/outbox/drainer` только
  **спрашивается** замком §2.6, а не правится: коды выбираются под существующую
  классификацию, а не классификация под коды.
- **Реконсайлер и дренаж не трогаются.**

---

## 5. Сценарии

### 5.0. Какие дают RED, а какие — характеризующий замок

| вид | смысл | круг 1 | круг 2 | круг 3 |
|---|---|---:|---:|---:|
| **RED** | падает на этой ревизии, зеленеет после работы | 6 | 6 | **4** |
| **характеризующий** | зелёный **и до, и после**; закрепляет то, что работа обязана НЕ сломать | 5 | 5 | **7** |
| **формальный** | о форме артефакта, а не о поведении | 3 | 3 | **3** |
| **всего** | | **14** | **14** | **14** |

**Всего не изменилось; переклассифицированы ДВА, переписаны ТРИ.** Объём приёмки
круг 3 не расширяет: новых сценариев не заведено, снятых нет.

| сценарий | круг 2 | круг 3 | почему |
|---|---|---|---|
| `IAM-MV-04` | RED | **характеризующий** | прогон: `caller=vpc` + `nlb_listener` → **refused сегодня**, полосой А, на **обеих** RPC. «Тогда» производится уже сейчас |
| `IAM-MV-06` | RED | **характеризующий** | тот же прогон, та же причина |
| `IAM-MV-05` | RED | RED, **текст исправлен** | требовал `vpc.totallyInvented` — значения, которого не производит никто: незнакомый тип не приводится и уезжает как `vpc_totally_invented` |
| `IAM-MV-09` | RED | RED, **контроль перестроен** | прежний контроль был неконструируем (Б4): у выдуманного типа строки каталога не бывает `CHECK`-ом. Перестроен на **существующий** тип |
| `IAM-MV-10` | RED | RED, **уровень изменён** | полоса `live` с полосы прокси недостижима (§2.1, три шага снятия). Переведён на уровень, где вход существует |

**Почему переклассификация — не ослабление.** Характеризующий сценарий утверждает
то, что работа обязана **не сломать**, и `IAM-MV-04`/`IAM-MV-06` после правки
утверждают именно это: отказ обязан **остаться**, сменив основание с совпадения
приставки на строку данных. Выдавать его за достижение приёмки нельзя — это и
есть предмет пометки.

**И это ровно та ошибка, которую круг 2 нашёл у меня и починил наполовину.** Он
заметил, что `IAM-MV-04` помечен RED при «Тогда: принято», и переписал «Тогда» на
отказ — оставив RED. Но отказ производится **сегодня**; менялась формулировка, а
проверить исход прогоном никто не догадался ни в одном из двух кругов.

Различие видов названо, потому что «все зелёные после» — не доказательство: замок,
зелёный и до, ничего о работе не утверждает и обязан быть помечен как замок.

### 5.1. Положительные — исполнимые, а не декоративные

**`IAM-MV-01` (характеризующий). Законная регистрация модуля проходит, и повтор — тоже.**
- **Дано:** учётка `kacho-vpc` с `fga_writer` на кластере; каталог несёт
  `('vpc','network','vpc.network')`; строка identity `('vpc','vpc','sva…')`
- **Когда:** `RegisterResource(subject="project:prj1", relation="project", object="vpc_network:net1")`
- **Тогда:** ответ пуст, ошибки нет; в `resource_mirror` строка
  `object_type = 'vpc.network'` (**словарь каталога**, не модели)
- **И — повтор той же тройки с той же версией источника принят так же**, без
  ошибки. Это лок на Б3: чтение §2.2 не вправе прочесть штатный ноль строк
  идемпотентного `ON CONFLICT` как отказ
- **Производитель:** полоса целиком; замок словаря — `services/iam/internal/repo/kacho/pg/scalegrid/seed.go:93`

**`IAM-MV-02` (характеризующий, несущий). Балансировщик регистрирует свой тип.**
- **Дано:** учётка `kacho-nlb`; домен SAN `nlb`, модуль каталога `loadbalancer`;
  identity `('loadbalancer','nlb','sva…')`
- **Когда:** `RegisterResource(subject="project:prj1", relation="project", object="nlb_listener:lsn1")`
- **Тогда:** принято; строка зеркала несёт `loadbalancer.listeners`
- **Зачем:** это **тот самый** вход, который отнимает предикат без приведения
  (§1.4). Сценарий обязан быть зелёным и до, и после; красный после — признак,
  что перевод стоит не с обеих сторон

**`IAM-MV-03` (характеризующий). Публикация репозитория проходит и строки зеркала не создаёт.**
- **Когда:** `RegisterResource(subject="user:*", relation="v_get", object="registry_repository:reg1/app")`
  от учётки `kacho-registry`
- **Тогда:** принято; строки зеркала **не появляется** (чистая выдача,
  `register_resource.go:224`)
- **Зачем (переобосновано кругом 2):** это лок на границу работы. Полоса чистой
  выдачи строки зеркала не пишет, значит ни ключ §2.1, ни чтение §2.2 её касаться
  не вправе. Красный после — признак, что сужение уехало на соседнюю полосу.
  Прежнее обоснование («перенос §2.5 ничего не отнял») снято вместе с §2.5

**`IAM-MV-04` (характеризующий, переклассифицирован кругом 3). Снятие ЧУЖОГО типа отвергается — и обязано остаться отвергаемым, сменив основание.**
- **Дано:** учётка `kacho-vpc`; identity балансировщика существует
- **Когда:** `UnregisterResource(… object="nlb_listener:lsn1")`
- **Тогда:** `PermissionDenied`, **дословно** `permission denied`
- **Контроль:** `UnregisterResource` с тройкой `IAM-MV-01` — **принято**
- **Зачем именно снятие, а не регистрация:** односторонний перевод словаря (§2.3)
  остаётся незамеченным ровно на этой полосе. Красный **после** работы означает,
  что приведение поставили только на регистрацию
- **Сегодня: ОТВЕРГАЕТСЯ**, и это измерено прогоном круга 3, а не прочитано:
  `caller=vpc`, `nlb_listener:lsn1` → `refused`. Отвергает полоса А по приставке
  (`nlb_` ≠ `vpc_`), и она стоит на **обеих** RPC одной функцией
  (`handler.go:192` и `:217` → `validateProxyTuple`). Круги 1 и 2 держали сценарий
  RED; классификация была неверна **в обоих**
- **Что тогда утверждает сценарий:** отказ обязан **пережить** работу, сменив
  основание с совпадения приставки на строку данных (§2.2). Приёмка не вправе
  записывать его себе в достижение

> [!note] Асимметрия §2.2 проверяется НЕ ЗДЕСЬ — у неё на этой полосе нет входа
> Круг 2 держал контролем «снятие снятого типа принимается», подавая
> `compute_disk:d1`. Вход неконструируем, и вот почему: `compute_disk` в карте
> платформы **отсутствует** (снят вместе с ресурсом), поэтому приведение отдаёт
> его дословно, а строка каталога называется `compute.disk` — **разные ключи**;
> точечную же форму отвергает полоса А по приставке (прогон круга 3:
> `compute.disk:d1` → `refused`).
>
> Асимметрия остаётся нормой §2.2 и держателя не теряет — он переезжает на
> **уровень хранилища**, где вход существует: снятые строки `compute.disk` /
> `compute.image` / `compute.snapshot` посеяны предшественником
> (`20260901113757:231`), и вызов пути снятия с точечным ключом `compute.disk`
> проверяется интеграционной пробой репозитория напрямую. Форма та же, что у
> `IAM-MV-10` (ниже), и по той же причине.

### 5.2. Отрицательные — каждый с ИСПОЛНИМЫМ положительным контролем

**`IAM-MV-05` (RED). Тип с законной приставкой, но вне каталога — отвергается полосой РЕФЕРЕНТА.**
- **Дано:** учётка `kacho-vpc`; строки каталога под это значение нет — и не может
  быть: приведение отдаёт `vpc_totally_invented`, а `CHECK (dotted = module ||
  '.' || resource)` требует точки (§1.3)
- **Когда:** `RegisterResource(subject="project:prj1", relation="project", object="vpc_totally_invented:x1")`
- **Тогда:** `InvalidArgument`; сообщение называет **значение ровно в той форме, в
  какой оно уезжало бы в зеркало** — `vpc_totally_invented`, с подчёркиванием, —
  и ничего больше; строки в `resource_mirror` не появляется; в журнале — запись с
  доменом вызывающего, тем же значением и оговоркой
- **Почему с подчёркиванием, а не точкой (правка круга 3):** незнакомый тип
  **не приводится вовсе**. Прогон `authzmap.DottedType("vpc_totally_invented")` →
  `знаком=false`, и `objectType()` отдаёт вход дословно
  (`register_resource.go:208`). Точечной формы `vpc.totallyInvented` **не
  производит никто**; круг 2 требовал её в «Тогда», то есть требовал строки без
  производителя — тот же класс, за который эта приёмка получила Б1
- **Оракула здесь нет:** текст возвращает вызывающему его собственный ввод, а
  чужую приставку полоса А отвергла бы раньше
- **Действия текст НЕ предписывает** — «опечатка» и «тип ещё не заведён» отсюда
  неразличимы, а совет «завести строку каталога» **ронял бы старт iam** (§2.6)
- **И — `drainer.Classify` на этом отказе даёт `ClassPermanent`.** Это не
  украшение: временная классификация заклинила бы партицию очереди модуля (§2.6)
- **Контроль:** `IAM-MV-01` на той же учётке принят
- **Производитель кода и текста:** **заводится этой работой** (§2.6). Существующий
  замок полосы А этого не покрывает — он зовёт `validateProxyTuple` напрямую
- **Сегодня:** вход **ПРИНИМАЕТСЯ** (§0.3). Это и есть RED

**`IAM-MV-06` (характеризующий, переклассифицирован кругом 3). Тип чужого модуля отвергается — и обязан остаться отвергаемым по СТРОКЕ, а не по приставке.**
- **Дано:** учётка `kacho-vpc`; identity `('loadbalancer','nlb','sva…')` существует
- **Когда:** `RegisterResource(… object="nlb_listener:lsn1")`
- **Тогда:** `PermissionDenied`, **дословно** `permission denied` — байт-в-байт с
  полосой А; `drainer.Classify` → `ClassPermanent`
- **Контроль:** `IAM-MV-02` — та же тройка от учётки `kacho-nlb` **принята**
- **Зачем именно пара:** односторонний сценарий зеленеет и на полосе,
  отвергающей всё
- **Зачем текст ОПАКОВЫЙ, в отличие от `IAM-MV-05`:** это решение о доступе, и
  назвать оговорку значило бы ответить, чей это тип (§2.6)
- **Сегодня: ОТВЕРГАЕТСЯ** (прогон круга 3, та же строка, что у `IAM-MV-04`).
  Круги 1 и 2 держали сценарий RED — неверно в обоих
- **Что тогда утверждает сценарий, и почему он несущий.** Отказ обязан пережить
  работу, но его **основание** меняется: сегодня это совпадение приставки, после —
  сверка двух прочитанных строк. Пара с `IAM-MV-02` доказывает, что смена
  основания не отняла законного: у балансировщика домен SAN (`nlb`) и модуль
  каталога (`loadbalancer`) **различны**, и предикат без приведения отнял бы у него
  три типа разом (§1.4)
- **Перемерено кругом 3, и это надо сказать вслух:** на сегодняшнем словаре
  проверка принадлежности **не меняет ни одного вердикта** — приставка совпадает с
  доменом у всех пяти писателей (§16.3). Её ценность в том, что совпадение
  перестаёт быть **условием работы**, а не в том, что она что-то отвергает сегодня

**`IAM-MV-07` (характеризующий). Кластерный объект — отвергается.**
- **Когда:** `RegisterResource(subject="user:mod1", relation="system_admin", object="cluster:cluster_kacho_root")`
- **Тогда:** `PermissionDenied`
- **Зачем замок, а не RED:** измерено в §0.3 — отвергается **сегодня**, двумя
  независимыми оговорками. Сценарий закрепляет то, что работа обязана не сломать,
  и **опровергает** формулировку задачи; выдавать его за достижение приёмки нельзя

**`IAM-MV-08` (характеризующий). Именованный получатель публичного чтения — отвергается.**
- **Когда:** `RegisterResource(subject="user:usr1", relation="v_get", object="registry_repository:reg1/app")`
- **Тогда:** `PermissionDenied`
- **Контроль:** `IAM-MV-03` с `user:*` принят
- **Зачем (переобосновано кругом 2):** пара с `IAM-MV-03` доказывает, что работа не
  тронула полосу чистой выдачи **ни в одну сторону** — ни разрешив лишнего, ни
  отняв законного. Без контроля «отвергается» зеленело бы и на полосе, отвергающей
  всё. Прежнее обоснование через §2.5 снято вместе с §2.5

**`IAM-MV-09` (RED, контроль перестроен кругом 3). Учётка без строки identity — отвергается, а не проходит по приставке.**
- **Дано:** проверенная учётка `kacho-storage` с `fga_writer`; строка identity
  `('storage','storage','sva…')` **удалена фикстурой**
- **Когда:** `RegisterResource(subject="project:prj1", relation="project", object="storage_volume:v1")`
- **Тогда:** `PermissionDenied`, дословный текст; `drainer.Classify` → `ClassPermanent`
- **Контроль:** **та же тройка при восстановленной строке identity — принята.**
  Тип `storage_volume` существует в карте платформы и приводится к
  `storage.volumes` (прогон круга 3), а строка `('storage','volumes',
  'storage.volumes')` посеяна предшественником (`20260901113757:216`) — значит
  положительная половина **исполнима**
- **Сегодня:** вход **ПРИНИМАЕТСЯ** (таблицы identity не существует вовсе). Это и
  есть RED
- **Зачем:** пустой перечень обязан означать «никого». Отсутствие identity, читаемое
  как «сужать нечем», вернуло бы связывание приставкой через заднюю дверь

> [!important] Правка круга 3: прежний контроль был НЕКОНСТРУИРУЕМ (Б4)
> Круг 2 подавал выдуманный модуль `kacho-newmod` с типом `newmod_thing:t1` и
> обещал контролем «та же тройка **после появления строк** identity и
> `catalog_resource` — принята». Такой строки не бывает **конструкцией БД**:
> `newmod_thing` не приводится (прогон: `знаком=false`), а `catalog_resource`
> требует `CHECK (dotted = module || '.' || resource)` — точки в значении нет,
> значит строки нет ни при какой попытке. Отрицание оставалось **без исполнимого
> положительного** — при том что заголовок §5.2 обещает именно исполнимый.
>
> Замена на существующий тип сохраняет предмет («личность вызывающего решает
> строка, а не приставка») и делает обе половины исполнимыми. Цена замены названа:
> сценарий больше **не** утверждает, что новый модуль привозит свои типы строками,
> — этого и не происходит (§1.3, §3.5).

**`IAM-MV-10` (RED, уровень изменён кругом 3). Полоса `live` ключа — снятие типа УПОРЯДОЧЕНО, а не разрешено при живых строках зеркала.**
- **Дано:** живая строка каталога `('vpc','network','vpc.network', live=true)` и
  строка зеркала `object_type = 'vpc.network'` — то есть состояние, которое
  производит `IAM-MV-01`
- **Когда:** `UPDATE kacho_iam.catalog_resource SET live = false, retired_at =
  now(), superseded_by = … WHERE dotted = 'vpc.network'`
- **Тогда:** `23503` — ключ `(object_type, live) → (dotted, live)` не даёт снять
  тип, пока на него ссылается строка зеркала
- **Контроль первый:** та же правка после снятия строки зеркала — **проходит**
- **Контроль второй:** правка живой строки каталога, **не** трогающая `live`
  (`retired_reason` у уже снятого типа) — **проходит**; иначе отрицание зеленело бы
  на ключе, запрещающем любую правку каталога
- **Зачем:** ключ ссылается на `(dotted, live)`, а не на `(dotted)`. Ссылка без
  `live` приняла бы снятый тип, и снятие перестало бы что-либо значить
- **Сегодня:** ключа нет вовсе, `UPDATE` проходит и оставляет строки зеркала без
  референта молча. Это и есть RED

> [!important] Правка круга 3: полоса `live` с полосы прокси НЕДОСТИЖИМА (Б4, моя находка)
> Круг 2 утверждал этот сценарий сквозным вызовом: `RegisterResource(…
> object="compute_disk:d1")` → `InvalidArgument` «по полосе референта». Вход
> существует, но проверяет **не то**: `compute_disk` в карте платформы
> отсутствует (прогон круга 3: `знаком=false`), приводится дословно, и ключ ищет
> `dotted = 'compute_disk'` — строки с таким значением нет, потому что снятая
> называется `compute.disk`. Отказ пришёл бы полосой **незнакомого** типа, то есть
> тем же, что у `IAM-MV-05`, — и о `live` не сказал бы ничего.
>
> **Достижимого входа у полосы `live` с полосы прокси нет вообще**, и это следствие
> §2.1: тип, снятый строкой, обязан быть снят из карты **тем же релизом** (иначе
> паритет роняет старт), а снятый из карты не приводится. Промежуточного состояния
> не существует.
>
> Поэтому сценарий переведён на уровень, где вход **есть** — на само хранилище.
> Он утверждает ровно то свойство ключа, ради которого `live` в него и включён:
> **порядок снятия типа**. Сквозной сценарий на ту же тему был бы утверждением о
> входе, которого не бывает.

### 5.3. Краевые и формальные

**`IAM-MV-11` (RED, миграция). Неполный каталог роняет применение.**
- **Дано:** в `resource_mirror` есть строка с типом без живого референта
- **Когда:** миграция применяется
- **Тогда:** `RAISE EXCEPTION`, текст называет **обе** величины (найдено ·
  ожидалось) и номер `#1031`; ключ **не** навешен
- **Производитель:** образец `20260901113757:620`

**`IAM-MV-12` (формальный). Второго переходника не заведено.**
- **Когда:** гейт §2.10 п. 2 обходит дерево
- **Тогда:** объявление соответствия ровно одно; перепись печатает объём
  осмотренного; на пустом обходе гейт **падает**

**`IAM-MV-13` (формальный). Карта исключений снята вместе с предметом.**
- **Когда:** `git grep -c moduleObjectDomain -- pkg/authz/proxytuple`
- **Тогда:** **ноль**. Пустая карта, оставленная «как точка расширения» после того,
  как расширение переехало в строки, — второе место об одном предмете

**`IAM-MV-14` (формальный, переписан кругом 3). Паритет identity — по признаку, с переписью в ОБЕ стороны.**
- **Когда:** гейт §2.10 п. 1 обходит миграции и выводит набор **писателей
  отношений** — учёток, которым выдаётся `fga_writer`
- **Тогда:** у каждого писателя ровно одна строка identity; `sa_id` совпадает с
  `ServiceAccountIDForService(san_domain)`; `module` — живая строка
  `catalog_module`
- **И зеркально:** строка identity **без** выдачи `fga_writer` — находка
- **Перепись печатает ДВЕ величины:** «писателей N · строк identity M». Одно число
  скрывает случай `N = M = 0`, ради которого гейт и заведён
- **Предпосылка:** на пустом обходе (миграций прочитано ноль либо выдач найдено
  ноль) гейт **падает**
- **Сегодня N = 5** — `vpc` · `compute` · `nlb` · `registry` · `storage`
  (`0009` три, `0044`, `0057`). Число **выводится гейтом**, а не вписывается: оно
  вырастет с первым же новым модулем и устареет молча
- **Почему не `domain.KnownModules()` (шесть):** перечень описывает **грантуемый
  домен правила**, а не вызывающего этой полосы; у `iam` модульной учётки не
  существует, и требовать ей строку значило бы заводить принципала, которого никто
  не предъявляет (§2.2, Б5)

### 5.4. Сценарий → производитель «Тогда»: перечень целиком

Этот раздел и есть предикат проверяемости приёмки. **Круг 2 переписал колонку
«существует сегодня» для каждой строки, называющей код или текст** — именно там
жила дыра Б1.

| сценарий | что утверждает «Тогда» | производитель | существует сегодня |
|---|---|---|---|
| `IAM-MV-01` | приём + `object_type` в словаре каталога | `register_resource.go:203`, `scalegrid/seed.go:93` | **да** |
| `IAM-MV-01` | повтор принят так же (идемпотентность) | `resource_mirror/emitter.go:186`, `:198` | **да** |
| `IAM-MV-02` | приём типа балансировщика | `fga_types.go:498`, `authzmap.DottedType` | **да** |
| `IAM-MV-03` | приём публикации, строки зеркала нет | `register_resource.go:224`, `policy.go:198` | **да** |
| `IAM-MV-04` | отказ снятия чужого типа | `handler.go:217` → `validateProxyTuple` (приставка) | **да** — прогон круга 3; после работы основание меняется на §2.2 |
| асимметрия §2.2 (вне сценариев) | приём снятия снятого типа | интеграционная проба репозитория на `compute.disk`; сквозного входа нет (§5.1, врезка) | **нет; заводится** |
| `IAM-MV-05` | `InvalidArgument` + текст с **дословным** значением `vpc_totally_invented` | перевод отказа полосы референта | **нет; заводится** (§2.6) |
| `IAM-MV-05` | `ClassPermanent` у `drainer.Classify` | `pkg/outbox/drainer/classify.go:82` | **да** (спрашивается, не правится) |
| `IAM-MV-05` | запись в журнал с оговоркой | — | **нет; заводится** (§2.6) |
| `IAM-MV-05` | отсутствие строки зеркала | ключ §2.1 + чтение §2.2 | **нет; заводится** |
| `IAM-MV-06` | `PermissionDenied` + дословный текст на чужом типе | сегодня `policy.go` (приставка); после — чтение §2.2 | **да** — прогон круга 3; предмет сценария в СМЕНЕ основания |
| `IAM-MV-07` | отказ на кластерном объекте | `policy.go:231`, `policy.go:239` | **да** |
| `IAM-MV-08` | отказ именованному получателю | `policy.go:198` | **да** |
| `IAM-MV-09` | отказ учётке без identity | чтение §2.2 (1) | **нет; заводится** |
| `IAM-MV-09` | контроль: та же тройка при живой строке identity | `storage.volumes` посеян `20260901113757:216`; приведение `storage_volume` → `storage.volumes` (прогон) | **да** — контроль исполним |
| `IAM-MV-10` | `23503` при снятии типа с живыми строками зеркала | ключ `(object_type, live) → (dotted, live)` §2.1 | **нет; заводится** |
| `IAM-MV-11` | текст `RAISE` с двумя величинами | образец `20260901113757:620` | **форма да, экземпляр заводится** |
| `IAM-MV-12` | одно объявление соответствия | гейт §2.10 п. 2 | **нет; заводится** |
| `IAM-MV-13` | ноль вхождений карты исключений | `git grep -c` | **да** (предикат), значение изменится |
| `IAM-MV-14` | паритет: писателей N · строк identity M, обе стороны | гейт §2.10 п. 1, набор **выводится** обходом миграций | **нет; заводится** |

**Ни одна строка «Тогда» не осталась без исхода, и ни одна помета «да» не стоит у
кода, рождающегося в БД** — это второе требование §13.7, и оно выполнено
построчно. Всё, что помечено «заводится», входит в эту же под-фазу (§2.11) и
потому не является отсрочкой.

**Правка круга 3 в этой таблице — про колонку «существует сегодня», и она в обе
стороны.** Две строки получили «да» вместо «нет; заводится» (`IAM-MV-04`,
`IAM-MV-06` — отказ производится уже сегодня, полосой А), одна строка добавлена
(контроль `IAM-MV-09`, чтобы исполнимость положительной половины была видна в
таблице, а не только в тексте), одна выведена из числа сценариев в свойство
(асимметрия §2.2 — у неё нет сквозного входа, и держатель у неё интеграционный).
Число сценариев при этом прежнее: **14** (§5.0).

---

## 6. Чем держится каждое требование

| требование | держится | вид |
|---|---|---|
| тип имеет референт в каталоге | внешний ключ `(object_type, live)` — на **всех трёх** писателях зеркала (§4) | **конструкция БД** |
| снятие типа упорядочено: сперва строки зеркала, потом `live=false` | тот же ключ ссылается на `(dotted, live)` | **конструкция БД** |
| снятый тип остаётся снимаемым | ключ не проверяется на `DELETE`; живость не читается на полосе снятия | **конструкция БД + §2.2**; сквозного входа нет — держит интеграционная проба (§5.1, врезка) |
| тип принадлежит модулю вызывающего | два чтения с названной кардинальностью в той же транзакции, `FOR SHARE` | **оператор, одна транзакция** |
| штатный ноль идемпотентной вставки не читается как отказ | отказ рождается **чтением**, а не кардинальностью записи | **форма §2.2** |
| один домен — один модуль | `UNIQUE (san_domain)` | **конструкция БД** |
| identity не называет несуществующую учётку | ключ в `service_accounts(id)` | **конструкция БД** |
| identity не называет несуществующий модуль | ключ в `catalog_module(module)` | **конструкция БД** |
| identity полагается **писателю отношений**, и только ему | самопроверка миграции (§2.7 п. 3) + гейт паритета (§2.10 п. 1), обе стороны | **оператор + гейт дерева**; ключом **невыразимо** — первичный ключ `group_members` тройной (§2.2), и это ослабление названо |
| каталог полон до навешивания ключа | самопроверка миграции | **оператор** |
| перевод делает один переходник | гейт по узлу разбора | **гейт дерева** |
| тройка эмитента принимается приёмной стороной | `internal/repohygiene/proxytupleintent_test.go` | **гейт сборки, существует** |
| текст отказа **полосы А** не называет оговорку | `proxy_tuple_refusal_transport_test.go` | **проба, существует** |
| **текст отказа полосы принадлежности байт-в-байт равен полосе А** | новый замок транспорта | **проба, заводится** |
| **текст отказа полосы референта называет только ввод вызывающего** | тот же замок | **проба, заводится** |
| **оба отказа ПОСТОЯННЫ для дренажа модуля** | тот же замок зовёт `drainer.Classify` и требует `ClassPermanent` | **проба, заводится** |
| правило проксируемой записи остаётся чистым | `git grep -nE 'database/sql\|pgx\|"context"' -- pkg/authz/proxytuple` → пусто | **предикат дерева** |
| «ноль отказов» заметно | счётчик по оговорке | **наблюдаемость** |

**Ни одна строка не держится вниманием, и ни один держатель не держит соседнюю
полосу** — это первое и третье требования §13.7. Если рецензент найдёт строку,
держащуюся вниманием, — это блокирующее: правило, обещающее несуществующую
проверку, есть тот самый класс, который корпус ловит.

**Круг 3 назвал ОДНО ослабление вслух и не спрятал его:** принадлежность строки
identity писателю отношений держится оператором и гейтом, а **не** ключом.
Причина названа в §2.2 (тройной первичный ключ членства; второй уникальный индекс
ради ключа был бы вторым местом об одном предмете), цена названа тоже: расхождение
здесь ловится на **старте и на сборке**, а не в момент вставки строки.

---

## 7. Способность проверок УПАСТЬ — как доказывается

### 7.1. Форма доказательства

По каждой оси — **три** прогона, а не два (`testing.md` §«Гейт на класс», п. 2в):

1. **контроль** — всё цело: молчат **оба** гейта;
2. **инъекция нового** — снято **новое** свойство у элемента, чьё **старое** на
   месте: краснеет **только** новый гейт, и находка называет координату;
3. **инъекция старого** — краснеет **только** существующий гейт.

Третий прогон обязателен: без него молчание существующего контроля неотличимо от
молчания мёртвого.

### 7.2. Что обязана показать инъекция по каждой оси

| ось | инъекция | ожидаемое |
|---|---|---|
| ключ каталога | строка зеркала с выдуманным типом — **через каждого из трёх писателей** (§4), а не только через полосу прокси | отказ БД с именем ключа у всех трёх |
| `live` в ключе | ссылка на `(dotted)` без `live` | `IAM-MV-10` зеленеет ложно ⇒ инъекция обязана это показать. **Подаётся на уровне хранилища**: сквозного входа у полосы `live` нет (§5.2) |
| асимметрия снятия | добавить `AND live` в чтение полосы снятия | интеграционная проба на `compute.disk` краснеет; `IAM-MV-10` молчит |
| приведение словаря | снять `DottedType` из **одной** полосы | `IAM-MV-02` молчит, `IAM-MV-04` краснеет — и это признак, что перевод односторонний |
| identity | снять строку `loadbalancer` | `IAM-MV-02` краснеет **по имени**; `IAM-MV-01` молчит |
| **текст отказа референта** *(круг 3)* | заменить дословное значение на точечное (`vpc.totallyInvented`) | замок §6 краснеет: такой формы не производит `objectType()` ни при каком входе |
| **код полосы референта** | вернуть отказу голый `fmt.Errorf` | замок §6 краснеет и называет **и** код (`Internal` вместо `InvalidArgument`), **и** класс (`ClassTransient` вместо `ClassPermanent`) |
| **код полосы принадлежности** | заменить текст на называющий оговорку | замок §6 краснеет по тексту; байт-идентичность с полосой А нарушена |
| **чистота фундамента** | добавить импорт драйвера в `pkg/authz/proxytuple` | предикат §4 непуст ⇒ находка; существующий гейт тройки эмитентов становится неисполнимым |
| паритет identity, сторона «нет строки» *(переписано кругом 3)* | снять строку identity у одного писателя, **оставив** его выдачу `fga_writer` | гейт §2.10 п. 1 краснеет **по имени модуля**; перепись печатает «писателей 5 · строк 4» |
| паритет identity, сторона «лишняя строка» *(круг 3)* | завести строку identity модулю **без** выдачи `fga_writer` | тот же гейт краснеет зеркальной стороной. Прежняя редакция подавала «седьмую строку с чужим `sa_id`» — при пяти конструируемых из шести объявленных гейт краснел бы уже на **контроле**, то есть на целом дереве |
| **предпосылка гейта паритета** *(круг 3)* | пустой обход (ни одной миграции прочитано) | гейт **падает**, а не объявляет ноль находок |
| второй переходник | завести карту соответствия в SQL | гейт §2.10 п. 2 краснеет **по координате**; тот же текст **в комментарии** — молчит |

Последняя строка — обязательный законный близнец: гейт, краснеющий на собственном
объяснении, отключают первым.

### 7.3. Граница доказательства — названа, а не обойдена

Инъекция подаёт вход **сама**, синтетическим деревом. Значит она доказывает, что
механизм жив, и **не** говорит ничего о том, производится ли предмет ещё
(`testing.md` §«Гейт на класс», п. 9). Для негативного утверждения `IAM-MV-13`
(«карты исключений нет») это существенно: оно замолчит, если полосу когда-нибудь
снимут целиком. Поэтому рядом с ним стоит `IAM-MV-12`, утверждение
**положительное** — «объявление ровно одно», — а оно на исчезнувшем предмете
краснеет, а не молчит.

**И то же — для замка §2.6.** Он утверждает **положительное**: «этот отказ
классифицируется постоянным». Снимут полосу — замок перестанет компилироваться, а
не позеленеет молча: он зовёт производителя отказа по имени.

---

## 8. Предикат готовности ДО посадки

Работа не начинается, пока не выполнено **всё**:

1. **`APPROVED` от `acceptance-reviewer`.** Запрет #1; вердикт машинно читается
   проверкой вердикта приёмок дерева продукта (`check-04` набора документов).
   Координата её здесь не пишется: набор живёт в **воркспейсе**, а инлайн-путь
   читается проверкой свежести как координата ЭТОГО дерева и справедливо не
   находится;
2. **число строк `resource_mirror` без живого референта названо** — прогоном против
   базы, а не оценкой (§0.5, §2.7 п. 1). Ненуль → исход по каждой строке до посадки;
3. **число строк `resource_mirror` со снятым типом названо отдельно** — они
   отличаются от п. 2 исходом: у них есть преемник;
4. **перечень модульных учёток с `fga_writer` перемерен НА ЖИВОЙ БАЗЕ** — включая
   те, что посев завёл под компоненты, которых нет (§3.2). Круг 3 назвал по
   дереву **пять** (§2.2), но состав группы писателей заполняется из
   **действующего основания доступа** (`20260823002000:89`), то есть из строк;
   расхождение дерева со стендом здесь и есть предмет замера. Это же число задаёт
   ожидаемую перепись `IAM-MV-14`;
5. ~~задачи-преемники заведены, их номера стоят в §3~~ — **ВЫПОЛНЕНО кругом 2**:
   `#1828` (§3.1), `#1829` (§3.2), `#1830` (§3.3). Обещание без номера блокировало
   бы приёмку (образец: `Б2` круга 1 приёмки `#1815`); круг 1 честно назвал это
   предусловием, круг 2 его снял заведением.

Пункты 2–4 остаются **неизмеренными** у автора и у рецензентов обоих кругов, и
остаются блокирующим предусловием: прогона хранилища не было ни у одного из нас
(§0.5, §13.6, §15.5). Круг 3 этого не изменил и не выдаёт свои прогоны чистых
функций за замер строк.

---

## 9. Гейты дерева, которые обязаны увидеть новое

### 9.1. Существуют и обязаны быть перепрогнаны

| гейт | почему его касается эта работа |
|---|---|
| `internal/repohygiene/proxytupleintent_test.go` | тройка каждого эмитента прогоняется через `ValidateTuple` **на сборке, без БД**; после выноса §2.5 работа фундамента не касается вовсе, значит гейт обязан остаться зелёным **без правки** — это и есть машинное доказательство, что Б2 закрыт не ценой Б2-бис |
| `services/iam/internal/authzmap/canonical_model_drift_test.go` *(круг 3)* | звено цепи авторитета §1.1: каждый тип каталога существует в модели. Работа его **не трогает и не вправе тронуть** — исход (б) замечания Б4 требовал бы его снятия, и это и есть довод против (б) (§16.1) |
| пробы стража загрузки и посевщика нагрузки *(круг 3)* | ключ §2.1 связывает **три** писателя зеркала, а не один (§4); их пробы обязаны быть перепрогнаны, иначе первый же новый тип у стража упрётся в ключ, о котором его автор не знал |
| `services/iam/internal/check/catalog_seed_parity.go` | каталог получает **строки** identity; паритет литерала и строк обязан их учесть, иначе гейт краснеет на верной работе |
| `services/iam/internal/apps/kacho/seed/catalog_parity.go` | тот же предмет со стороны посева |
| `proxy_tuple_refusal_transport_test.go` | замок **полосы А**; краснеет, если работа тронула её, — а она не трогается (§2.6) |
| гейты формы ключа (`DEFERRABLE INITIALLY IMMEDIATE`) | новый ключ судится ими же |
| `pkg/errors/reason_closed_test.go` | закрытость словаря токенов; работа его не расширяет (§2.6), значит гейт обязан остаться зелёным без правки |

### 9.2. Ведомость приёмок

`check-04` обходит дерево продукта по **стволу**, а не по рабочей копии. Пока эта
приёмка не влита, она читается по индексу копии; после вливания — по `origin/main`.
Строка статуса обязана перечитываться вместе с шапкой при каждом круге: круг,
оставивший чужую строку, — тот самый класс «утверждение пережило свой предмет».

### 9.3. Гейтов, которых нет и которые заводятся

Два гейта дерева (§2.10, сценарии `IAM-MV-12` и `IAM-MV-14`) и **один замок
транспорта** (§2.6, §6). Ни один не откладывается: все входят в под-фазу (§2.11).

---

## 10. Порядок работ после `APPROVED`

1. замер по §8, пп. 2–4 — **до** первой строки кода;
2. падающие сценарии §5.1–5.3 (RED целиком, все **четыре** — `IAM-MV-05`,
   `IAM-MV-09`, `IAM-MV-10`, `IAM-MV-11`; число пересчитано кругом 3, §5.0) плюс
   замок транспорта в его красном виде;
3. миграция: `catalog_module_identity` · колонка `live` и ключ у `resource_mirror` ·
   самопроверка исхода;
4. чтение §2.2 на обеих проксируемых полосах, с асимметрией живости; приведение
   словаря **с обеих сторон**;
5. перевод отказа в два кода (§2.6) + замок, зовущий `drainer.Classify`;
6. снятие карты исключений вместе с предметом;
7. журнал и счётчик отказов;
8. два гейта дерева + инъекция по §7 (три прогона на ось);
9. локальный прогон **до** отправки; конвейер — подтверждение, а не первый читатель.

---

## 11. Что рецензент обязан проверить прежде вердикта

Замером, а не прочтением. Возражение без предиката автор вправе вернуть как
неисполнимое (`git-issues.md` §«Возражение рецензента — ТОЖЕ утверждение»).
**И собственный предикат прогоняется дословно и целиком** — усечение вывода
превратило верную координату в ложную находку у круга 1 (§13.0) и превратило
верное существо Б1 в неверный предикат (§14.1).

1. **Опровержение §0.3 — своим прогоном.** Подать `ValidateTuple` тройку из
   §ПРЕДМЕТ задачи и увидеть отказ. Если у вас принято — расходится дерево, и это
   блокирующее;
2. **`vpc_totally_invented` принимается сегодня** — тем же способом. Если
   отвергается, у приёмки нет предмета;
3. **три написания типа балансировщика** (§1.4). Если хоть два совпали, решение
   §2.2 переусложнено;
4. **ноль проксируемых отношений в `catalog_verb`** (§2.4) — своим предикатом;
5. **`catalog_module_identity` в дереве нет** — `git grep -c … ':!*.md'`;
6. **у `resource_mirror` нет внешних ключей** — по `0019_resource_mirror.sql`;
7. **классификация дренажа** — `pkg/outbox/drainer/classify.go:145`: постоянны
   **ровно** `InvalidArgument` и `PermissionDenied`. Если это не так, §2.6 выбрал
   коды под неверный замер, и это блокирующее;
8. **`drainer.Classify` экспортирована** — иначе держатель §6 неконструируем, и мы
   повторили бы Б1 на новом месте;
9. **чистота фундамента** — `git grep -nE 'database/sql|pgx|"context"' --
   pkg/authz/proxytuple` пуст **и остаётся пустым** после работы (§4). Непуст —
   значит Б2 закрыт ценой Б2-бис;
10. **каждая строка §5.4 с пометкой «да»** — по названной координате, и **отдельно**
    вопрос «называет ли это „Тогда" код или текст». Пометка «да» у такого «Тогда» —
    блокирующее: ровно так родился Б1;
11. **§6 не содержит строки «держится вниманием»** и ни одного держателя, который
    держит **соседнюю** полосу;
12. **три задачи-преемника существуют и не о другом** — `#1828`, `#1829`, `#1830`;
13. **своя граница замера названа** — как названа моя в §0.5.

**Добавлено кругом 3 — и первые три пункта проверяются ОДНИМ прогоном, а не
чтением** (все три опровержения круга 3 получены именно им, и ни одно чтением бы
не нашлось):

14. **`caller=vpc` + `nlb_listener:lsn1` отвергается СЕГОДНЯ** — подать
    `proxytuple.ValidateTuple` и увидеть `refused`. Если у вас принято, расходится
    дерево. Именно это переклассифицировало `IAM-MV-04` и `IAM-MV-06`;
15. **незнакомый тип не приводится** — `authzmap.DottedType("vpc_totally_invented")`
    → `ok=false`. Значит в отказ и в зеркало уезжает форма **с подчёркиванием**;
    точечной не бывает. Требование точечной формы в «Тогда» — блокирующее;
16. **`compute_disk` не приводится тоже**, а строка каталога называется
    `compute.disk` → полоса `live` **недостижима** сквозным вызовом (§5.2). Если у
    вас достижима — назовите вход;
17. **исход (б) замечания Б4 требует снятия landed-гейта** — прочитать шапку
    `authzmap/canonical_model_drift_test.go` и сказать, остаётся ли он в силе при
    каталоге-авторитете. Если остаётся, мой довод §16.1 неверен, и это блокирующее;
18. **набор носителей identity выводится, а не выписан** — §2.10 п. 1 и
    `IAM-MV-14`: гейт обязан **находить** писателей обходом миграций и печатать
    **обе** величины. Число 5 в тексте — ориентир §0.5, и оно помечено как
    ориентир;
19. **§4 называет всех трёх писателей зеркала**, и по каждому сказано, жив ли его
    референт;
20. **§2.6 не предписывает действия, которое ломает старт.** Текст «завести строку
    каталога» — блокирующее: такой строки не бывает `CHECK`-ом, а заведённая руками
    роняет старт.

---

## 12. Ссылки

- задача: `PRO-Robotech/kacho#1031`
- предшественник: `PRO-Robotech/kacho#1030`, миграция
  `services/iam/internal/migrations/20260901113757_rule_segments_have_a_referent.sql`
- задачи-преемники: `PRO-Robotech/kacho#1828` · `#1829` · `#1830` · **`#1092`**
  (портируемость словаря типов, названа кругом 3, §3.5 — заводить не пришлось,
  она уже существует)
- смежная того же класса, **не** закрывается этой приёмкой: `#1824` (роли
  объявлены миграциями iam, а не данными модуля)
- эпик манифеста домена, куда сходятся обе: `#1087`
- соседняя приёмка того же словаря:
  `services/iam/docs/engineering/acceptance/rule-segments-have-a-referent.md`
- приёмка о глаголах системных ролей (образец переписи производителей):
  `services/iam/docs/engineering/acceptance/system-role-segments-resolve.md`
- границы поверхности выдачи:
  `services/iam/docs/engineering/architecture/grant-surface-boundaries.md`
- компонент: `services/iam/docs/engineering/components/21-internal-iam.md`

---
## 13. Ревью, круг 1 — `acceptance-reviewer`

**Вердикт: ❌ CHANGES REQUESTED.** Три блокирующих. Каждое замечание получено
**прогоном**, а не прочтением; предикат назван при каждом, чтобы автор мог его
вернуть как неисполнимое (`git-issues.md` §«Возражение рецензента — ТОЖЕ
утверждение»).

### 13.0. Моя опровергнутая гипотеза — первой

Я счёл предикат строки «якорь права записи» (§0.1) ложным: `git grep -n
'relationWriteObject = clusterRootObject' -- services/iam` вернул мне **только
сам этот документ**, и я готовился записать это находкой класса «предикат считает
собственное объяснение».

**Опровергнуто моим же повтором.** Я оборвал вывод `head -2`, а `services/iam/docs/`
сортируется раньше `services/iam/internal/`, поэтому код в срез не попал. Полный
прогон даёт три строки, третья — `services/iam/internal/authzguard/fgaproxy.go:54`.
Предикат автора **верен**, находки нет.

Урок для этого же ревью: чужой предикат прогоняется **дословно и целиком** —
усечение вывода превращает верную координату в ложную находку.

### 13.1. Что я проверил и ПОДТВЕРДИЛ — чтобы «ноль находок» было отличимо от «ноль прочитанного»

| утверждение приёмки | чем проверено | исход |
|---|---|---|
| §0.3 посылка 1 опровергнута: атака §ПРЕДМЕТ отвергается сегодня | **свой** прогон `ValidateTuple`, 17 троек | **подтверждено дословно** |
| §0.3 посылка 2: `vpc_totally_invented` принимается | тот же прогон | **подтверждено** — `ACCEPTED` |
| §1.4: три написания типа балансировщика различаются | `module_set.go:22` (`loadbalancer`) · `fga_types.go:498` (`nlb_listener`) · прогон: `caller=nlb` принимает `nlb_listener` и **отвергает** `loadbalancer_listener` | **подтверждено**, включая цену ошибки |
| §2.4: `catalog_verb` — 109 строк, ноль проксируемых отношений | предикат автора дословно | **109**; `project`/`parent`/`owner`/`v_get` — **0** |
| §1.3: 27 типов, 20 чужих, 6 модулей | `fga_types.go` перепись по модулям: 9 vpc · 7 iam · 3 compute · 3 loadbalancer · 3 storage · 2 registry | **подтверждено**; независимо корроборировано самопроверкой миграции `:620` (`live_resources <> 27 … live_verbs <> 109 … retired_res <> 3`) |
| §0.2: `catalog_module_identity` в дереве нет | `git grep -c` | **есть только в этом документе** (7) |
| §0.2: у `resource_mirror` ноль внешних ключей | `0019_resource_mirror.sql` | **0** ключей, 3 CHECK + PK |
| §0.2: `h.logger` в `handler.go` — одно попадание, присваивание | `grep -c` + `:146` | **1**, `h.logger = l` |
| §2.1: цель ключа существует | `catalog_resource_dotted_live_uk UNIQUE (dotted, live)` | **существует** |
| §2.2: цели ключей identity существуют | `catalog_module_pkey PRIMARY KEY (module)` | **существует** |
| §2.1: гейт формы ключа (`RESTRICT`+`DEFERRABLE` инертен) | `catalog_seed_parity.go:26-28,267-277` | **подтверждено дословно** |
| §3.1/§3.2: задач-преемников не существует | свой прогон `gh issue list --search`, 100 записей | **подтверждено**: #1032 · #1092 · #1027 · #1827 — все о другом |
| §5.4 строки «да» | построчно `sed -n` по каждой координате | **все резолвятся**, кроме разобранной в Б1 |

**Отдельно проверена ось, которой приёмка не касается, но обязан касаться
рецензент** (`security.md` §«Отношение, выполнимое подстановочным знаком»):
`define fga_writer: [service_account, group#member]` (`fga_model.fga:286`) —
подстановочным знаком **не выполняется**. Гейт, на который решение опирается как
на существующее сужение, действительно сужает. Находки нет.

**Ось «самодельная проверка вместо модели»** (`security.md`) — нарушения не нахожу:
предмет сужения не «может ли этот принципал писать» (это `fga_writer`, в модели,
§2.8 его не трогает), а «о своём ли типе запись». Модель об этом не высказывается
by construction, а словарь и факт лежат в одной схеме — значит место инварианта в
БД (запрет #10), и §2.1 выбирает его верно.

### 13.2. Б1 (блокирующее). Производитель отказа четырёх несущих сценариев назван существующим — и произвести его НЕ МОЖЕТ

**Что стоит в приёмке.** §5.4: «`IAM-MV-05` | `PermissionDenied` + дословный текст
| `handler.go:182`, замок `:37` | **да**». То же в §5.2 («Производитель кода и
текста»), в §2.6 («на проводе — без изменений… замок не трогается») и в §6
(«текст отказа не называет оговорку | `proxy_tuple_refusal_transport_test.go` |
проба, **существует**»).

**Чем опровергнуто — четырьмя прогонами:**

1. `handler.go:180-185` — `validateProxyTuple` зовёт `proxytuple.ValidateTuple` и
   больше ничего. Функция принимает **четыре строки** и возвращает ошибку;
2. пакет **провозглашённо чист**: `git grep -nE 'database/sql|pgx|context' --
   pkg/authz/proxytuple` (без проб) → **пусто**. Он не может прочитать ни
   `catalog_resource`, ни `catalog_module_identity` — ни сегодня, ни после работы;
3. значит отказ `IAM-MV-05/06/09/10` рождается там, где приёмка его и помещает —
   в ключе §2.1 и операторе §2.2, **внутри** `h.registrar.Register(…)`. Его
   транспорт — **другой** производитель: `shared.MapRepoErr` (`handler.go:204`,
   `:226`);
4. что `MapRepoErr` из этого делает — прогнано по дереву: ветки SQLSTATE у него
   **нет** (`git grep -n '23503' -- services/iam --include='*.go'` → ни одного
   не-тестового попадания), а эмитент зеркала оборачивает **голым** `fmt.Errorf`
   (`resource_mirror/emitter.go:190`, `:327`). Несентинельная ошибка проваливается
   в хвост `errors.go:108` → **`codes.Internal "internal error"`**.

**Следствие.** Четыре из шести RED-сценариев утверждают `PermissionDenied` +
дословный текст, а дерево на спроектированной полосе произвело бы `Internal
"internal error"`. Замок `proxy_tuple_refusal_transport_test.go` этого не ловит:
он зовёт `validateProxyTuple` **напрямую** (строка 30) и новой полосы не касается
вовсе — то есть §6 объявляет держателем пробу, которая держит **соседнюю** полосу.

Это ровно тот класс, ради которого §0 и написан, и он тем опаснее, что §5.4
помечает производителя «**да**»: перепись производителей, найдя существующую
функцию с подходящим именем, засчитала её полосе, которой она не принадлежит.

**Что закрывает.** Одно из двух, и выбор — предмет приёмки, а не реализации:
(а) назвать сентинель (`iamerr.ErrPermissionDenied` с текстом ровно `permission
denied`), место обёртки и **новый** замок на полосу `MapRepoErr` — тогда §5.4
получает строку «заводится», а §6 — своего держателя; либо (б) переписать «Тогда»
четырёх сценариев под то, что производится, и назвать код честно. «Оставить как
есть» исходом не является.

### 13.3. Б2 (блокирующее). §2.5 неконструируема в названном месте и роняет гейт, который §9.1 требует зелёным

**Что стоит в приёмке.** §2.5: «список в Go снимается, а `publicReadAllowed`
спрашивает каталог **тем же оператором §2.2**».

**Чем опровергнуто.** `publicReadAllowed` — `pkg/authz/proxytuple/policy.go:198`,
зовётся из `ValidateTuple` (`policy.go:244`). Тот же прогон, что в Б1, показывает:
пакет не имеет ни `context`, ни драйвера. Сверх того — **два** независимых
следствия, каждое самостоятельно достаточное:

1. `handler.go:175` объявляет прямо: «the rule itself is transport-free **by
   design** … so a consumer's domain layer can import it without taking a
   dependency on gRPC» (`architecture.md`, правило зависимостей). Дав ему БД, мы
   отзываем landed-решение, на которое сама приёмка опирается в §2.6;
2. `internal/repohygiene/proxytupleintent_test.go:132` прогоняет тройку **каждого**
   эмитента через `ValidateTuple` **на сборке, без БД**. §9.1 требует, чтобы этот
   гейт остался зелёным, и называет его «машинным доказательством, что живого
   права не отнято». Функция, которой нужен каталог, делает его неисполнимым.

**Отдельно: у этой полосы вообще нет строки зеркала.** Публичная выдача — чистый
кортеж (`register_resource.go:224` `isPureGrant`; `IAM-MV-03` сам утверждает
«строки зеркала **не появляется**»). Значит ни ключ §2.1, ни оператор над
`resource_mirror` её не касаются **by construction**, и «тем же оператором» не
имеет референта. `IAM-MV-03` и `IAM-MV-08` остаются без производителя своего
«Тогда» после переноса.

**Что закрывает.** Назвать место, где публичность спрашивается у каталога, так,
чтобы `ValidateTuple` осталась чистой (например, полоса решает это **в
use-case**, до эмиссии кортежа, а закрытый список в общем фундаменте остаётся
вторым рубежом) — либо признать перенос §2.5 отдельным предметом с номером и снять
его из этой под-фазы. Сегодня §2.11 объявляет его частью production-complete
под-фазы, а он неконструируем.

### 13.4. Б3 (блокирующее). «Ноль строк — отказ» сталкивается с уже действующей идемпотентностью того же оператора

**Что стоит в приёмке.** §2.2: «Запись обеих проксируемых полос принимает форму
`INSERT … SELECT … WHERE EXISTS (…)` … **Ноль вставленных строк — отказ по
кардинальности**, ровно как у образца CAS в `data-integrity.md`».

**Чем опровергнуто — чтением обеих полос, которые предлагается изменить:**

- **регистрация.** `resource_mirror/emitter.go:177-186`: `ON CONFLICT … DO UPDATE
  … WHERE resource_mirror.source_version < EXCLUDED.source_version`. Комментарий
  автора того кода объявляет ноль строк **законным исходом** дословно: «или версия
  не новее (0 rows — a redelivery the monotonic guard already recognised)».
  Эмитент возвращает `Outcome{Applied: false}` — и это **успех**;
- **снятие.** `emitter.go:317-328`: `DELETE … WHERE source_version <= $3`, и
  результат **отбрасывается** (`if _, err := tx.Exec(...)`). Ноль строк —
  молчаливый успех, намеренно: контракт RPC объявляет «Idempotent: absent tuple →
  OK, never NotFound» (`handler.go:210`), а миграция `0019` — «a repeated
  RegisterResource (drainer retry) is a no-op-equivalent».

**Следствие.** Добавив `WHERE EXISTS (…каталог…)` в **эти** операторы, приёмка
делает ноль строк **двузначным**: «повторная доставка дренажа, которую сторожевая
версия правильно погасила» и «тип не в каталоге / модуль не владелец» становятся
неразличимы. Прочесть ноль как отказ — сломать идемпотентность, которую at-least-once
дренаж делает **штатным** режимом (тогда `IAM-MV-01` и `IAM-MV-04` краснеют на
повторе); прочесть как успех — сделать сужение no-op. Третьего оператор не даёт.

Ключ §2.1 этой двузначности **не имеет** и потому остаётся годным: нарушение
даёт `23503` — сигнал, отличимый от нуля строк by construction. Предмет замечания
— **только** §2.2, то есть ровно та половина, которую «внешним ключом невыразимо».

**Что закрывает.** Назвать сигнал отказа, отличимый от штатного нуля: отдельный
`SELECT … FOR SHARE` по identity **в той же транзакции** с явной кардинальностью,
либо `RAISE` из триггера, либо ключ на колонку, которую полоса всё равно пишет.
Выбор — предмет приёмки: от него зависит и код на проводе (Б1).

### 13.5. Не блокирующее — но правится тем же кругом

1. **§0.1, строка «якорь права записи».** Предикат верен (§13.0), но матчит и
   **собственное объяснение**: полный вывод — 3 строки, из них 2 — этот документ.
   Как координата годится, как **счёт** — нет. Если он когда-нибудь станет числом,
   класс сработает (`testing.md` §«Гейт на класс», п. 4). Стоит сузить пометкой
   `':!*.md'`.
2. **§2.3 просит рецензента искать признак, которого §5.4 не проверяет.** «В
   операторе стоит `object_type` без приведения» — верное требование, но ни один
   сценарий §5 его не утверждает: `IAM-MV-02` ловит односторонний перевод только
   если перевод снят в **положительной** полосе. Инъекция §7.2 строку про это
   несёт — стоит поднять её в §5 сценарием, иначе требование держится вниманием
   рецензента, а §6 объявляет, что таких строк нет.
3. **§8, п. 5 — форма исхода верна.** Обещание без номера действительно
   блокировало `#1815`; здесь оно **не** оставлено «как есть», а превращено в
   названное блокирующее предусловие, и отсутствие обеих задач я подтвердил своим
   прогоном трекера. Замечания нет — фиксирую, чтобы следующий круг не завёл его
   заново.

### 13.6. Граница МОЕГО замера — названа, как §0.5 называет авторскую

- **Прогона хранилища у меня не было** (6.0 ГБ при 98 %). Ни один вывод выше не
  требует БД: Б1 установлен по коду отображения ошибок и обёрткам эмитента, Б3 —
  по тексту самих операторов и их комментариям. Числа §8, пп. 2–4 остаются
  неизмеренными **и у автора, и у меня**, и остаются блокирующим предусловием;
- **я не перечислял фактические типы, регистрируемые модулями в рантайме** — по
  той же причине; принимаю авторскую пометку §0.5 о негодности единицы счёта;
- **я не проверял поведение ключа §2.1 на живой базе** (`23503` против ноля строк
  установлен по семантике PostgreSQL и по прецеденту приёмки `#1030`, §0.2 Н2, а
  не своим прогоном).

### 13.7. Что круг 2 обязан показать

По каждому из Б1–Б3 — **тот же** прогон, которым опровергнуто, с другим исходом,
а не похожий (`multi-agent-flow.md` §15). Именно: `git grep -nE
'database/sql|pgx|context' -- pkg/authz/proxytuple` остаётся **пустым** (иначе Б2
закрыт ценой Б2-бис); §5.4 не содержит ни одной строки «да» у сценария, чей отказ
рождается в БД; §6 не содержит держателя, который держит соседнюю полосу.

---

## 14. Ответ автора, круг 2

Вердикт круга 1 остаётся в шапке: его ставит проверяющий, и автор его не снимает.
Ниже — что сделано по каждому блокирующему, чем это перемерено и где я согласился,
а где вернул замечание с предикатом.

### 14.0. Мой замер круга 2 — своими командами, а не переносом

Ни одно число и ни одна координата круга 1 не приняты на веру: рецензент прогонял
свои предикаты, я прогонял свои. Совпало не всё, и расхождения названы ниже
поимённо, а не сглажены.

**Граница замера круга 2 — та же, что у круга 1 и по той же причине:** прогона
хранилища не было (`df -h /` → **6.0 ГБ при 98 %**, контейнеры не поднимались
осознанно). Всё установлено по дереву и по одному прогону чистой функции. Числа
§8, пп. 2–4 остаются неизмеренными — блокирующее предусловие в силе.

### 14.1. Б1 — ПРИНЯТО по существу, ПРЕДИКАТ ОПРОВЕРГНУТ

**Существо замечания верно, и я подтвердил его своей цепочкой** (§2.6): отказ,
рождённый в БД, приезжает `Internal "internal error"`, а не
`PermissionDenied "permission denied"`; существующий замок этого не ловит, потому
что зовёт `validateProxyTuple` напрямую (`proxy_tuple_refusal_transport_test.go:30`
— прочитано мной построчно). §5.4 действительно пометила «да» у производителя,
полосе не принадлежащего. Это моя ошибка, и её механизм назван в §0.4.

**Предикат, которым Б1 это доказывал, — неверен.** Замечание пишет:

> ветки SQLSTATE у него **нет** (`git grep -n '23503' -- services/iam
> --include='*.go'` → ни одного не-тестового попадания)

Я прогнал этот предикат **дословно и целиком** — и он опровергается дважды, на
двух разных уровнях.

**Опровержение первое, по исходу.** Дословный прогон даёт **57 файлов**, и
не-тестовых среди них большинство — включая
`services/iam/internal/repo/kacho/pg/pgmaperr.go:45` (`wrapPgErr`), где `23503`
переводится в `iamerr.ErrFailedPrecondition` с текстом по умолчанию
`"referenced resource not found or still in use"` (`:383`), и
`services/iam/internal/repo/kacho/pg/maperr.go:10`, где это отображение объявлено
шапкой. Перевод SQLSTATE → sentinel в iam **существует и работает**.

**Опровержение второе, по самому предикату — и оно объясняет первое.** У `git
grep` **нет флага `--include`**: это флаг обычного `grep`, а `git grep` сужает
охват **pathspec'ом**. Флаг не отсекается и не ошибается — он просто не
фильтрует, поэтому предикат обходил **весь** `services/iam`, включая
`docs/`, и среди его 57 файлов часть — документы, а не код. То есть число было
негодно **в обе стороны сразу**: единица счёта не та, что заявлена.

Годная форма и её замер — pathspec вместо флага:

```sh
git grep -l '23503' -- 'services/iam/**/*.go' | grep -v _test.go | wc -l   # → 22
```

**22 не-тестовых файла Go** в `services/iam` знают `23503`. Существо опровержения
от смены единицы не меняется — перевод существует, — но число теперь называет то,
что обещает.

**Класс, ради которого это записано, а не просто исправлено.** Предикат с
несуществующим флагом **не падает**: он молча меряет шире объявленного и оттого
выглядит строгим. Тот же предикат в этой же паре ревью сработал у обоих
участников — у рецензента в Б1 и у меня в первой редакции §14.1, где он
матчил **собственное объяснение** этого абзаца. Проверяемый признак для
следующего: прогнав свой предикат, посмотри на **типы найденных файлов**, а не
только на их число; `.md` в выводе поиска по коду означает, что сужение не
сработало.

**Почему вывод при этом уцелел, и это важнее самого расхождения.** Верное
утверждение — не «ветки нет в сервисе», а «**её нет на этой полосе**»:
`wrapPgErr` **неэкспортирована** и живёт в пакете `pg`, а зеркало — в отдельном
пакете `resource_mirror`, который оборачивает голым `fmt.Errorf`
(`emitter.go:190`, `:327` — прочитано мной). Экспортированной формы в дереве нет
(`git grep -rn 'func WrapPgErr\|func MapPgErr' -- 'services/iam/**/*.go' 'pkg/**/*.go'`
→ пусто — предикат в pathspec-форме, а не с несуществующим флагом).

Расхождение записано, а не сглажено, потому что оно **меняет решение**: раз
перевод в дереве есть, соблазн «переиспользовать существующий» велик, и его надо
отвергнуть явно. Я его отвергаю: экспортировать `wrapPgErr` ради одной полосы
значило бы вынести наружу отображение, чей контракт — имена **ограничений iam**,
и завести второго знающего. Перевод пишется **на полосе**, своим коротким
распознавателем, и держится замком §6.

**Что изменено в документе.** Выбран исход **(б') — заказан производитель, и он
внутри этой под-фазы**, а не «в другой задаче»: §2.6 переписана целиком, §5.4
получила строки «заводится» у каждого «Тогда», называющего код, §6 получил три
новых держателя, §0.1 и §0.2 — строки о транспорте.

**Исход (а) круга 1 — «переписать „Тогда" под производимое» — я отверг, и не по
вкусу.** `pkg/outbox/drainer/classify.go:145` объявляет постоянными **ровно два**
кода: `InvalidArgument` и `PermissionDenied`. `Internal` в их числе нет, значит
дренаж модуля прочёл бы этот отказ **временным**, держал бы строку ниже порога
отравления и не выпустил бы её партицию — а снятие регистрации стоит в очереди за
регистрацией. Оставить «как производится» значило бы посадить приёмку, чей отказ
заклинивает очередь модуля. Комментарий того же файла описывает этот исход как
наблюдавшийся; повторять его осознанно нельзя.

**И ту же меркой я поверил свой первый вариант — он был неверен.** Сперва я
выбрал для полосы БД `FailedPrecondition` (как «состояние не позволяет», по
`api-conventions.md`). Замер classify.go показал: `FailedPrecondition`
**тоже временный**. То есть верный по конвенции код дал бы ровно тот же клин.
Записываю это как опровергнутую гипотезу рядом с решением, чтобы следующий не
прошёл по ней снова: **на этой полосе код выбирается по классификации применяющего
дренажа, а не только по семантике ошибки**, и две мерки здесь расходятся.

### 14.2. Б2 — ПРИНЯТО целиком, предмет вынесен задачей `#1830`

**Все три довода замечания подтверждены моим прогоном** (§2.5): пакет чист
(предикат пуст), `handler.go:175` объявляет чистоту landed-решением дословно, и
полоса чистой выдачи строки зеркала не пишет вовсе (`register_resource.go:224`).

**Я рассмотрел первый из двух названных Б2 исходов и отверг его.** «Спрашивать
публичность в use-case, оставив закрытый список вторым рубежом» оставляет литерал
`registry_repository` в общем фундаменте — то есть **не достигает цели §2.5**
(«новый модуль не требует релиза фундамента») и вдобавок заводит два места об
одном предмете. Полумера здесь хуже отсутствия: она выглядит переносом, не будучи
им.

**Взят второй исход: предмет вынесен целиком, задачей `#1830`**, с признаком,
предметом и предикатом снятия в её теле, включая ту сторону, которой в §2.5 не
было названо: перенос делает отказ **ослабляемым строкой**, то есть переворачивает
умолчание, и это требует своей модели угроз.

**Проверяемое следствие, названное §13.7 первым:** после выноса работа фундамента
**не касается ни одной строкой**, поэтому `git grep -nE 'database/sql|pgx|"context"'
-- pkg/authz/proxytuple` остаётся пустым **by construction**, а не по обещанию, и
существующий гейт тройки эмитентов остаётся зелёным **без правки**. Это записано
предикатом в §4 и требованием в §9.1.

### 14.3. Б3 — ПРИНЯТО, форма §2.2 заменена

**Обе половины замечания подтверждены моим чтением тех же двух операторов**
(§2.2, врезка): на регистрации `Outcome{Applied: false}` возвращается **без
ошибки** (`emitter.go:198`), на снятии результат `Exec` **отбрасывается**
(`emitter.go:321`). Ноль строк на обеих полосах — штатный успех повторной
доставки, и прочесть его отказом значило бы сломать идемпотентность, которую
контракт RPC объявляет прямо (`handler.go:210`).

**Взят сигнал, названный самим замечанием:** явное чтение `FOR SHARE` в той же
транзакции с названной кардинальностью. У `SELECT` по уникальному ключу ноль строк
означает ровно «строки нет», и повторная доставка на него не влияет никак.

**Что при этом обязано быть сказано, и чего в замечании не было.** Ключ §2.1 и
чтение §2.2 делят работу так, что **запрет #10 не нарушен**: инвариант держит
ключ, чтение производит сообщение, обе операции — одна транзакция, `FOR SHARE`
закрывает окно до коммита. Landed-образец назван дословно (`pgmaperr.go:331`).

**И моя собственная находка круга 2, которой в Б3 нет: полосы АСИММЕТРИЧНЫ.**
Круг 1 писал «оператор обязан стоять на обеих полосах» и был прав только
наполовину. Проверка **принадлежности** — на обеих. Проверка **живости** типа —
только на регистрации: потребовать её на снятии значило бы запретить отзыв
регистрации у снятого типа, и тогда выдача пережила бы свой предмет — over-grant,
беда крупнее предотвращаемой. Асимметрия записана в §2.2 таблицей, закреплена
вторым контролем `IAM-MV-04` и вынесена отдельной осью инъекции (§7.2).

### 14.4. Не блокирующие круга 1 — по каждому

1. **§0.1, предикат «якорь права записи» матчит собственное объяснение.**
   **Принято**: предикат сужен `':!*.md'` (§0.1). Замечание верно и относится к
   классу, который корпус ловит отдельно.
2. **§2.3 просит рецензента искать признак, которого §5 не утверждает.**
   **Принято**: односторонний перевод словаря теперь утверждает **сценарий**, а не
   только инъекция, — `IAM-MV-04` берёт полосу **снятия**, то есть ту, где
   односторонний перевод и остаётся незамеченным (§2.3, §5.1).
3. **§8 п. 5 — замечания нет, зафиксировано.** Круг 2 снял предусловие заведением
   трёх задач; п. 5 переписан с номерами (§8).

### 14.5. Моя собственная находка круга 2: `IAM-MV-04` был помечен RED неверно

Круг 1 объявил сценарий RED при «Тогда: **принято**» — то есть при исходе,
зелёном и сегодня. Мисклассификация **завышает счёт RED**, а §5.0 сама
предупреждает, что «все зелёные после» доказательством не является: замок,
зелёный и до, о работе не утверждает ничего.

Сценарий переписан на то, что его действительно делает красным (снятие **чужого**
типа сегодня принимается), а прежнее «Тогда» стало его **контролем**. Счёт видов
от этого не изменился — 6 · 5 · 3 и до, и после, — и это сказано числом в §5.0,
чтобы совпадение не читалось как «ничего не менялось».

### 14.6. Что круг 2 предъявляет по §13.7 — построчно

| требование §13.7 | чем предъявлено |
|---|---|
| `git grep -nE 'database/sql\|pgx\|"context"' -- pkg/authz/proxytuple` остаётся **пустым** | §2.5: предмет вынесен в `#1830`, фундамент не трогается ни строкой; предикат стоит в §4 и в §9.1 как требование к гейту |
| §5.4 не содержит ни одной строки «да» у сценария, чей отказ рождается в БД | §5.4 переписана: у `IAM-MV-04/05/06/09/10` все строки кода и текста помечены «нет; заводится». Пометка «да» осталась только у `drainer.Classify` — и он не производит отказ, а **классифицирует** его |
| §6 не содержит держателя, который держит соседнюю полосу | §6: замок полосы А назван **полосой А** явно; для полос референта и принадлежности заведены три отдельные строки, все «проба, заводится» |

### 14.7. Граница ответа — что круг 2 НЕ закрыл

- **Пункты 2–4 §8 по-прежнему не измерены** — прогона хранилища не было ни у
  автора, ни у рецензента. Это блокирующее предусловие, а не остаток: работа не
  начинается, пока числа не названы.
- **Поведение нового ключа на живой базе не проверено** — `23503` против ноля
  строк установлен по семантике PostgreSQL и по прецеденту `#1030`, а не своим
  прогоном. То же ограничение назвал круг 1 (§13.6), и оно не снято.
- **`drainer.Classify` не вызывалась мной на живом отказе** — классификация
  прочитана по коду классификатора. Замок §6, который эту связь держит, вызова
  требует, и он входит в под-фазу; но **сегодня** это утверждение о коде, а не
  наблюдение.

---

## 15. Ревью, круг 2 — `acceptance-reviewer`

**Вердикт: ❌ CHANGES REQUESTED.** Б1 · Б2 · Б3 круга 1 **закрыты по существу** —
не переформулированы, а решены; вынос §2.5 задачей `#1830` и смена формы §2.2
проверены мной прогоном, а не прочтением. **Открыты два новых блокирующих**, Б4 и
Б5. Оба — того же класса, что Б2 круга 1 («требование к тому, у чего нет
конструируемого экземпляра»), и оба лежат в **новой** половине решения, которой в
круге 1 ещё не было в этой форме.

Круг ради круга не заводится: ни одно из двух не закрывается фразой. Б4 меняет
ответы §2.12 по двум осям из пяти, Б5 делает миграцию §2.7 неприменимой.

### 15.0. Мои опровергнутые гипотезы — первыми (их ДВЕ, вторая в §15.5)

Проверяя §2.4 своим предикатом, я получил `project -> 4` против объявленного нуля
и уже держал это находкой класса «число не воспроизводится».

**Опровергнуто моим же повтором.** Мой предикат считал вхождение слова в **строку
целиком**, а `project` — это ещё и имя **ресурса** iam (`('iam','project','get')`
и три соседних). Предмет спора — колонка **глагола**, и она третья. Корректная
форма и её замер:

```sh
awk '/INSERT INTO kacho_iam.catalog_verb/,/;/' <миграция> \
  | grep -oE "^ *\('[a-zA-Z]+', *'[a-zA-Z]+', *'[a-zA-Z]+'\)" \
  | sed -E "s/.*, *'([a-zA-Z]+)'\)/\1/" | sort | uniq -c
#   27 list · 27 get · 26 update · 26 delete · 1 removetargets · 1 create · 1 addtargets
```

Строк **109**, глаголов `project`/`parent`/`owner`/`v_get` — **ноль**. Автор прав
дословно; находки нет. Урок ровно тот, который корпус называет отдельно: единица
счёта у предиката была не та, что у утверждения, и ошибка выглядела как замер.

### 15.1. Что я проверил и ПОДТВЕРДИЛ

Чтобы «ноль находок» было отличимо от «ноль прочитанного».

| утверждение круга 2 | чем проверено мной | исход |
|---|---|---|
| §0.3: атака §ПРЕДМЕТ задачи отвергается сегодня | **свой** прогон `ValidateTuple`, 15 троек | **подтверждено**: `system_admin`/`owner` @ `cluster:…` и @ `account:…` — refused |
| §0.3: `vpc_totally_invented` принимается | тот же прогон | **ACCEPTED** — у приёмки есть предмет |
| §1.4: цена ошибки словаря | тот же прогон, обе стороны | `caller=nlb` + `loadbalancer_listener` → refused; `caller=loadbalancer` + `nlb_listener` → refused. Предикат без приведения отнял бы живое право — **подтверждено с обеих сторон** |
| §2.6: постоянных кодов ровно два | `classify.go:145` дословно | `st.Code() == codes.InvalidArgument \|\| st.Code() == codes.PermissionDenied` — **подтверждено** |
| §2.6: `Internal`/`FailedPrecondition` временны | `classify.go:142-144` + `Classify` целиком | **подтверждено**; комментарий того же файла описывает наблюдавшийся клин партиции дословно |
| §2.6: применяющая сторона модуля судит так же | `services/compute/internal/clients/iam_register_applier.go:170` | `case codes.InvalidArgument, codes.PermissionDenied: ErrPermanent` — **подтверждено на втором производителе** |
| §2.6/Б1: цепочка даёт `Internal "internal error"` | `MapRepoErr` прочитана целиком (`errors.go:47`—`:108`) | ветки SQLSTATE **нет**, хвост — фиксированный `Internal`. **Подтверждено** |
| §14.1: `wrapPgErr` недостижима с полосы | `pgmaperr.go` — `package pg`; зеркало — пакет `pg/resource_mirror` | **подтверждено**; экспортированной формы в дереве нет |
| §14.1: `git grep --include` не фильтрует | **свой** прогон обеих форм | флаг: **57** файлов (37 go · 9 md · 3 py · 8 sql); pathspec: **22**. Оба числа автора воспроизведены **дословно** |
| §13.7 п. 1: фундамент остаётся чистым | `git grep -nE 'database/sql\|pgx\|"context"' -- pkg/authz/proxytuple` | **пусто**. Б2 закрыт не ценой Б2-бис |
| §13.7 п. 2: §5.4 без «да» у отказа из БД | построчно | пометки «да» стоят только у приёма, у `drainer.Classify` (он классифицирует, не производит) и у предиката `IAM-MV-13`. **Выполнено** |
| §13.7 п. 3: §6 без чужого держателя | построчно | замок полосы А назван **полосой А** явно; три новых держателя — «проба, заводится». **Выполнено** |
| §2.1: цель ключа существует | миграция `:189` | `catalog_resource_dotted_live_uk UNIQUE (dotted, live)` — **существует** |
| §2.1: landed-образец ключа | миграция `:572`, `:594` | `role_verb_live_true CHECK (live)` + `role_verb_type_fk … (dotted, live) … DEFERRABLE INITIALLY IMMEDIATE` — **существует дословно, по обеим названным координатам** |
| §0.2: у `resource_mirror` ноль внешних ключей | `0019_resource_mirror.sql` | `grep -cE 'FOREIGN KEY\|REFERENCES'` → **0**; PK + 3 CHECK |
| §0.2: `catalog_module_identity` в дереве нет | `git grep -c … ':!*.md'` | **пусто** |
| §2.7: самопроверка миграции — образец | миграция `:620` | `RAISE EXCEPTION` с обеими величинами — **существует** |
| §5.4/§0.1: **все** координаты | построчно `sed -n "${l}p"` по 34 координатам | **все резолвятся** и говорят то, что им приписано. Промахов содержания — ноль |
| §8 п. 5: три задачи-преемника | `gh issue view 1828/1829/1830` | **существуют**, OPEN, `P2 · size:M · area:iam · release:modules`, тело тремя частями, предмет у каждой **свой** |

**Ось «самодельная проверка вместо модели»** (`security.md`) — нарушения не нахожу,
и по той же причине, что круг 1: модель отвечает на «вправе ли этот принципал
писать» (`fga_writer`, §2.8 его не трогает), а предмет сужения — «о своём ли типе
запись», о чём модель не высказывается by construction. Место инварианта — БД
(запрет #10), и §2.1 выбирает его верно.

**Ось «отношение, выполнимое подстановочным знаком»** — перепроверена мной на
новой половине: сужение §2.2 опирается на **равенство двух прочитанных строк**, а
не на отношение, поэтому подстановочным знаком невыполнимо by construction.

### 15.2. Б4 (блокирующее). Референт полосы — членство в карте Go, а не в каталоге; поэтому контроль `IAM-MV-09` неконструируем, а портируемость заявлена шире измеренного

**Что стоит в приёмке.** §2.1: «Тип объекта, приведённый к словарю каталога, обязан
иметь референт в `catalog_resource(dotted, live)`». §2.2: «Новый модуль привозит
**строку** identity и **строки** `catalog_resource`; ни `moduleObjectDomain`, ни
закрытый набор Go править не требуется». §5.2, `IAM-MV-09`, контроль: «та же тройка
после появления строк identity и `catalog_resource` — **принята**».

**Чем опровергнуто — четырьмя прогонами, и каждый самостоятельно достаточен:**

1. **приведение — голый поиск по карте Go, без запасного пути.**
   ```sh
   awk '/func DottedType/,/^}/' services/iam/internal/authzmap/fga_types.go
   # d, ok := dottedByFGAType[fgaType]; return d, ok
   sed -n '141,147p' services/iam/internal/authzmap/fga_types.go
   # dottedByFGAType — обратный индекс objectTypes, литерала Go
   ```
   Тип вне карты приводится **дословно**, с подчёркиванием: `register_resource.go:208`
   — «unknown types are kept verbatim». То есть `newmod_thing` остаётся
   `newmod_thing`;
2. **точечная форма каталога обязана нести точку — конструкцией БД.**
   ```sh
   grep -n 'catalog_resource_dotted_form\|module_undotted' <миграция #1030>
   # CHECK (dotted = module || '.' || resource)
   # CHECK (module NOT LIKE '%.%')
   ```
   Значит строки `catalog_resource` с `dotted = 'newmod_thing'` **не может
   существовать никогда**. Чтение §2.2 (2) даёт ноль строк, ключ §2.1 даёт `23503`
   — оба **by construction**, а не по забывчивости посева;
3. **каталог ВЫВОДИТСЯ из той же карты Go.**
   ```sh
   awk '/func CatalogSeedResources/,/^}/' services/iam/internal/authzmap/fga_types.go
   # entries := Catalog()  → Dotted: e.Module + "." + e.Resource
   ```
   Каталог не является независимым авторитетом: он есть проекция литерала;
4. **лишняя строка каталога РОНЯЕТ СТАРТ, а не расширяет словарь.**
   ```sh
   grep -n 'func (c CatalogParityCensus) Diverged' -A2 \
     services/iam/internal/apps/kacho/seed/catalog_parity.go
   # return len(c.MissingRows) > 0 || len(c.ExtraRows) > 0
   grep -n 'AssertCatalogParity' services/iam/cmd/kacho-iam/serve.go   # :1323
   ```
   Сверка **двусторонняя** (`ExtraRows`), а её отказ **фатален** (`return
   fmt.Errorf`, и комментарий рядом объявляет фатальность намеренной).

**Три следствия, каждое самостоятельно блокирующее:**

- **контроль `IAM-MV-09` неконструируем в названном месте.** «После появления
  строк identity и `catalog_resource`» тройка `newmod_thing:t1` **не станет
  принятой** ни при какой строке: приведение даёт подчёркнутую форму, а
  подчёркнутого `dotted` в каталоге не бывает. Отрицание остаётся **без
  исполнимого положительного** — при том что заголовок §5.2 обещает именно
  исполнимый. Это ровно класс Б2 круга 1, на новом месте;
- **дискриминатор полосы назван неверно.** §2.1 объявляет референтом членство в
  каталоге; фактический референт — членство в `authzmap.objectTypes`, а каталог
  лишь переносит его в БД. Это **не обесценивает** работу (перенос в БД и есть
  запрет #10 и настоящая ценность), но норма обязана называть то, что делает:
  иначе следующий прочтёт «каталог — авторитет» и построит на этом;
- **портируемость §1.3/§2.2 заявлена шире измеренного.** Не только новый **модуль**
  — новый **тип существующего модуля** тоже требует релиза Go: `vpc_newthing`
  приводится дословно, каталог его принять не может, а строка, добавленная руками,
  роняет старт. §1.3 пишет «Выбора между „модуль неработоспособен без релиза iam"
  и „модуль выписывает себе кластерные права" здесь нет»; решение **выбирает
  первый рог**, и молча.

**И самое острое, ради чего это блокирующее, а не важное.** Полоса **не различает**
опечатку `vpc_totally_invented` (`IAM-MV-05`, предмет приёмки) и законный новый
`vpc_newthing`: обе приводятся дословно, обе промахиваются мимо каталога, обе
получают один и тот же `InvalidArgument`. Текст отказа §2.6 обещает разработчику
«что делать дальше — завести строку каталога», а заведение строки **роняет старт
iam**. Отказ, называющий действие, которое ломает продукт, хуже опакового.

**Что закрывает — одно из двух, и выбор предмет приёмки, а не реализации:**

(а) **назвать цену и вынести словарь**, как §2.5 вынесена в `#1830`: §1.1/§2.1
переписать на «референт в карте платформы, энфорсимый ключом», §1.3/§2.2 —
назвать, что новый тип впредь требует релиза Go, §2.12 пересчитать по осям
«благо продукта» и «перспективность», контроль `IAM-MV-09` перевести на
**существующий** тип (например `storage.volumes` после появления строки identity
`storage`), а портируемость словаря завести задачей-преемником с признаком и
предикатом;

(б) **внести словарь в под-фазу**: сделать каталог авторитетом приведения (чтение
даёт `dotted` по строке, карта Go остаётся посевом), и тогда `IAM-MV-09` в
нынешнем виде конструируем. Цена названа честно: это трогает `authzmap` — тот
самый единственный переходник §2.3, — и требует своей модели угроз, потому что
приведение становится **данными**.

«Оставить как есть» исходом не является: сегодня §2.2 обещает возможность, у
которой нет исполнимого входа (`api-conventions.md` §«Неисполнимая возможность»).

### 15.3. Б5 (блокирующее). Строка identity специфицирована по набору, в котором есть модуль, у которого её не может быть никогда — и §2.7 делает миграцию неприменимой

**Что стоит в приёмке.** §2.2: третья колонка `sa_id` — «ключ в
`service_accounts(id)`: строка не вправе называть несуществующую учётку». §2.10
п. 1: «у **каждого** модуля закрытого набора (`domain.KnownModules()`, **шесть**
имён) есть ровно одна строка identity, её `sa_id` совпадает с
`ServiceAccountIDForService(san_domain)`». §5.3, `IAM-MV-14`: «перепись печатает
**шесть**». §2.7 п. 3: «модули каталога **без** строки identity» — ненуль роняет
применение миграции.

**Чем опровергнуто — двумя прогонами:**

```sh
sed -n '22p' services/iam/internal/domain/module_set.go
# {"iam", "vpc", "compute", "loadbalancer", "registry", "storage"}   ← шесть

git grep -ohE "'kacho-[a-z-]+'" -- services/iam/internal/migrations | sort -u
# kacho-api-gateway · kacho-bootstrap-admin · kacho-bootstrap-grant · kacho-compute
# kacho-iam-bootstrap · kacho-nlb · kacho-registry · kacho-root · kacho-storage
# kacho-system · kacho-vpc · kacho-vpc-operator

git grep -c "'kacho-iam'" -- services/iam/internal/migrations    # → пусто
```

**Модульной учётки `kacho-iam` не существует ни в одной миграции** — и не должна:
`iam` собственную проксируемую RPC не зовёт, домена SAN у него в этой роли нет, а
`ServiceAccountIDForService("iam")` называл бы `sva`+md5(`kacho-iam`), строки под
который нет. Значит для модуля `iam` строка identity **неконструируема ключом
`service_accounts(id)`**, который §2.2 сама и требует.

**Три следствия:**

- **миграция §2.7 не применится НИКОГДА.** Её п. 3 считает модули каталога без
  identity; `catalog_module` несёт шесть живых строк, включая `iam`; счётчик
  вечно равен единице → `RAISE` → под-фаза не садится вовсе. Это строже, чем
  ложное требование: это неприменимый артефакт;
- **§2.10 п. 1 и `IAM-MV-14` неконструируемы.** Строк может быть **пять**
  (`vpc`, `compute`, `loadbalancer`→`nlb`, `registry`, `storage` — у всех пяти
  учётка в миграциях есть), а перепись обещает шесть;
- **инъекция §7.2 «завести седьмую строку с чужим `sa_id`» проверяет не ту
  сторону**: при пяти конструируемых из шести объявленных гейт краснеет уже на
  **контроле**, то есть на целом дереве, — а `testing.md` требует, чтобы контроль
  молчал.

**Почему это не описка в числе.** Набор выбран **не тот**: identity описывает
**вызывающего проксируемой полосы**, а `domain.KnownModules()` описывает
**грантуемый домен правила** — два разных предмета, случайно почти совпавших.
Разошлись они ровно на `iam`, и разойдутся снова на любом домене, который
грантуем, но проксируемой записи не делает (`geo` — ближайший кандидат: он
объявлен листом и журнала не служит).

**Что закрывает.** Назвать набор **по его признаку**, а не по соседнему перечню:
identity полагается модулю, у которого есть модульная учётка и домен SAN, — и
предикат этого признака поставить в §2.10 вместо `domain.KnownModules()`.
Соответственно правятся §2.7 п. 3, §2.10 п. 1, число `IAM-MV-14`, строка §6 «у
каждого модуля есть identity» и ось инъекции §7.2. Заводить строку `kacho-iam`
ради счёта — **не исход**: это принципал, которого никто не предъявляет, то есть
расширение поверхности ради зелёного гейта.

### 15.4. Не блокирующие — правятся тем же кругом

1. **Ключ §2.1 связывает ТРИ писателя `resource_mirror`, а §4 и §9.1 называют
   один.** Предикат и мой замер:
   ```sh
   git grep -n 'INTO kacho_iam.resource_mirror' -- '*.go' | grep -v _test
   # emitter.go:177 (полоса прокси) · backfill_adapter.go:224 · scalegrid/seed.go:180
   ```
   Сегодня **все три совместимы**, и я это измерил, а не предположил: страж
   загрузки пишет `vpc.network` (`seed/verify_gate.go:66`, и там уже стоит своя
   проверка по закрытой таблице `:321`), посевщик нагрузки — `iam.project` и
   `registry.registries`, обе строки каталога живы. Прод-импортёров у `scalegrid`
   ноль (только интеграционные пробы). Находки нет — но ключ есть инвариант
   **таблицы**, а не полосы, и §4 обязан назвать двух остальных писателей, иначе
   следующий прочтёт «сужается полоса прокси» и заведёт четвёртого.
2. **Замечания нет — фиксирую, чтобы круг 3 не завёл его заново.** Я держал
   находкой промах координат `:572`/`:594` (§2.1) и **опроверг её своим же
   повтором** — разбор в §15.5, п. «вторая опровергнутая гипотеза». Обе
   координаты точны построчно.
3. **Находка по дороге, не предмет этой приёмки, задачей мной НЕ заведена** (вне
   моей области): шапка `services/iam/../clients/iam_register_applier.go`
   объявляет «`codes.InvalidArgument` → `ErrPermanent`; любой другой →
   transient», а код на `:170` делает `ErrPermanent` **и** для
   `codes.PermissionDenied`. Комментарий пережил свою правку; §2.6 опирается на
   **код**, а не на шапку, поэтому решение приёмки верно — но следующий читатель
   этой шапки выберет код по ней.

### 15.5. Граница МОЕГО замера — и вторая опровергнутая гипотеза

**Вторая опровергнутая гипотеза, и она того же класса, что первая.** Я объявил
промахнувшимися две координаты §2.1 — `role_verb_live_true` на `:572` и образец
ключа на `:594`, — прочитав их окнами `sed -n "$((l-3)),$((l+4))p"` и посчитав
смещение внутри окна **на глаз**. Повтор с печатью номеров строк:

```sh
sed -n '570,600p' <миграция> | cat -n   # с пересчётом на абсолютные
# 572| ADD CONSTRAINT role_verb_live_true CHECK (live);
# 594| ADD CONSTRAINT role_verb_type_fk
```

**Обе координаты автора точны.** Ошибка моя, и её механизм тот же, что у §15.0 и
у §13.0 круга 1: инструмент чтения врал, а не предмет. Три раза подряд в этой
паре ревью находка рождалась **из способа смотреть**, а не из дерева, — и все три
раза её ловил только дословный повтор. Признак для круга 3: координату проверяют
командой, **печатающей номер строки**, а не окном со счётом в уме.

**Граница замера.**

- **Прогона хранилища у меня не было**: `df -h /` → **6.0 ГБ при 98 %**,
  контейнеры не поднимались осознанно (порог задания — 3 ГБ; при 98 % подъём
  отобрал бы место у соседних полос). Ни Б4, ни Б5 базы не требуют: оба
  установлены по литералам Go, ограничениям `CHECK` и коду стража старта;
- **чистую функцию я прогонял, поднятый стенд — нет.** Временная проба заведена в
  `pkg/authz/proxytuple`, прогнана, **снята**; `git status --porcelain` пуст —
  проверено после снятия;
- **`drainer.Classify` я на живом отказе не вызывал** — прочитал предикат и
  прогнал второй производитель той же классификации (`classifyApplyErr` компьюта).
  Это два независимых чтения кода, а не наблюдение;
- **числа §8, пп. 2–4 остаются неизмеренными** — и у автора, и у обоих
  рецензентов. Блокирующее предусловие в силе и Б4/Б5 его не заменяют;
- **сплошной адъюдикации всех 34 координат по СМЫСЛУ я не делал** — я проверил,
  что каждая резолвится и говорит то, что ей приписано; я не перечитывал каждый
  окружающий блок целиком.

### 15.6. Что круг 3 обязан показать

По Б4 и Б5 — **тот же** прогон, которым опровергнуто, с другим исходом, а не
похожий:

1. `IAM-MV-09` (либо его преемник) несёт положительный контроль, для которого
   существует строка `catalog_resource`, **проходящая** `CHECK (dotted = module ||
   '.' || resource)`, и приведение до неё доходит — либо контроль переведён на
   существующий тип, а портируемость словаря вынесена задачей с номером;
2. §1.3, §2.2 и §2.12 называют цену «новый тип требует релиза Go» **или**
   объявляют, что она снята, — и во втором случае §2.3 переписан вместе с ней;
3. набор носителей identity назван **признаком**, а не перечнем
   `domain.KnownModules()`; §2.7 п. 3, §2.10 п. 1, число `IAM-MV-14` и §7.2
   приведены к нему;
4. §4 называет **всех трёх** писателей `resource_mirror`, которых связывает ключ.

Остальное — §1, §2.1, §2.3, §2.4, §2.6, §2.7 пп. 1–2, §2.8, §2.9, §5.1, §5.2
(кроме контроля `IAM-MV-09`), §6 и §7 — **проверено и возражений не имеет**;
переписывать их круг 3 не обязан.

---

## 16. Ответ автора, круг 3

**Б4 и Б5 — ПРИНЯТЫ ЦЕЛИКОМ.** Ни одно не закрыто фразой: каждое перемерено моими
командами, и по каждому исправлен **предмет**, а не формулировка. Одно замечание
из не блокирующих (§15.4 п. 3) **опровергнуто** — разбор в §16.4.

Правило круга: **сначала прогон, потом вывод.** Оба своих опровержения круг 3
получил подачей входа, а не чтением, — и оба чтение подтверждало.

### 16.0. Мои опровергнутые посылки — первыми, их ТРИ

Все три — мои собственные, все три пережили два круга ревью, и все три нашлись
одним прогоном чистых функций (пробы заведены, прогнаны, **сняты**;
`git status --porcelain` после снятия пуст).

**Прогон 1 — `proxytuple.ValidateTuple`, восемь троек:**

```
caller=vpc       nlb_listener:lsn1            project  -> refused
caller=vpc       vpc_network:net1             project  -> ACCEPTED
caller=nlb       nlb_listener:lsn1            project  -> ACCEPTED
caller=vpc       vpc_totally_invented:x1      project  -> ACCEPTED
caller=compute   compute_disk:d1              project  -> ACCEPTED
caller=compute   compute.disk:d1              project  -> refused
caller=storage   storage_volume:v1            project  -> ACCEPTED
caller=nlb       vpc_network:net1             project  -> refused
```

**Прогон 2 — `authzmap.DottedType`, семь типов:**

```
vpc_network            знаком=true  -> в зеркало ложится "vpc.network"
nlb_listener           знаком=true  -> в зеркало ложится "loadbalancer.listeners"
vpc_totally_invented   знаком=false -> в зеркало ложится "vpc_totally_invented"
compute_disk           знаком=false -> в зеркало ложится "compute_disk"
compute_instance       знаком=true  -> в зеркало ложится "compute.instance"
storage_volume         знаком=true  -> в зеркало ложится "storage.volumes"
newmod_thing           знаком=false -> в зеркало ложится "newmod_thing"
живых пар каталога: 27
```

**1. Два сценария помечены RED при исходе, зелёном сегодня.** `IAM-MV-04` и
`IAM-MV-06` подают чужой тип от `kacho-vpc`. Строка первая прогона 1: **refused**.
Отвергает полоса А по приставке, и стоит она на **обеих** RPC одной функцией —
`handler.go:192` и `:217` вызывают `validateProxyTuple` идентично. Оба
переклассифицированы в характеризующие (§5.0). Особенно неприятно то, что круг 2
нашёл ровно эту ошибку у `IAM-MV-04`, переписал «Тогда» — и **оставил RED**, не
подав вход.

**2. Текст отказа требовал значения, которого не производит никто.** `IAM-MV-05`
ждал `vpc.totallyInvented`. Прогон 2, строка третья: незнакомый тип **не
приводится** и уезжает дословно — `vpc_totally_invented`. Точечной формы у него не
бывает ни при каком входе. Это тот же класс, за который приёмка получила Б1 круга
1, — и я воспроизвёл его сам, тремя строками ниже собственного разбора Б1.

**3. Полоса `live` ключа сегодня недостижима.** `IAM-MV-10` утверждал отказ на
снятом типе сквозным вызовом. Прогон 2, строка четвёртая: `compute_disk` тоже не
приводится, а строка каталога называется `compute.disk` — разные ключи; точечную
форму отвергает полоса А (прогон 1, строка шестая). Достижимого входа у `live` с
полосы прокси **нет вообще**, и это следствие §2.1: тип, снятый строкой, обязан
быть снят из карты тем же релизом. Сценарий переведён на уровень хранилища.

**Общее у трёх и признак для следующего круга:** все три — о том, что
**производится**, а не о том, что задумано. Чтение подтверждало каждую.

### 16.1. Б4 — ПРИНЯТО. Исход (а), и он не отступление: исход (б) НЕСОСТОЯТЕЛЕН

**Четыре прогона проверяющего воспроизведены мной, все четыре подтверждены**, с
одной поправкой координаты: `CatalogSeedResources` живёт в
`services/iam/internal/authzmap/catalog_seed.go:50`, а не в `fga_types.go`.
Существо от этого не меняется — каталог выводится из карты.

**Выбор исхода. Проверяющий дал два: (а) назвать цену и вынести словарь, (б)
внести словарь в под-фазу, сделав каталог авторитетом приведения. Я выбираю (а) —
и утверждаю, что (б) не просто дорог, а несостоятелен.** Довод мой, замером:

Цепь авторитета типа имеет **три** звена, а не два (§1.1), и верхнее — **модель
прав**. Гейт `authzmap/canonical_model_drift_test.go` называет предмет каждого
звена дословно: модель — «то, **ЧТО умеет решаться**»; каталог `authzmap` — «то,
что сервис умеет **НАЗВАТЬ**». Направление сверки там **одно и объявлено
намеренным**: каждый тип каталога обязан существовать в модели.

Сделать каталог авторитетом приведения значит разрешить строку, называющую тип,
которого модель не объявляет. Шапка того же гейта называет исход такой строки
своими словами: «обещание полосы, которой нет». То есть исход (б) требует
**снятия landed-гейта**, закрывшего этот класс, — а снимать защиту, чтобы
расширить доступ, запрещено порядком (`security.md` §«Авторизация живёт в
МОДЕЛИ»: сначала сузь модель, потом снимай лишнее).

Второй, самостоятельный довод — размер: `authzmap` импортируют **21** не-тестовый
файл в **12** пакетах (предикат:
`git grep -l '"…/services/iam/internal/authzmap"' -- '*.go' | grep -v _test`).
Превращение чистой карты в читателя БД трогает путь запроса каждого из них. Это не
под-фаза, и «production-complete в своих границах» такую границу не оправдывает.

**Что исправлено по трём следствиям Б4 — по каждому:**

| следствие Б4 | правка |
|---|---|
| контроль `IAM-MV-09` неконструируем | контроль перестроен на **существующий** тип: `storage_volume` → `storage.volumes`, строка каталога посеяна `20260901113757:216`. Обе половины исполнимы (§5.2) |
| дискриминатор полосы назван неверно | §1.1 переписан: референт — **карта платформы, спроецированная в строки**; §2.1 несёт врезку «что ключ утверждает, а что нет» |
| портируемость заявлена шире измеренного | §1.3 несёт таблицу цены с предикатом на строку; §2.2 снял «ни закрытый набор Go править не требуется»; §2.12 пересчитан по двум осям; предмет вынесен **существующей** задачей `#1092` (§3.5) |

**И самое острое замечание Б4 — про отказ, называющий действие, которое ломает
продукт, — принято дословно.** §2.6 больше не предписывает «завести строку
каталога». Текст называет **значение, которого сервис не узнал**, и ничего не
советует: опечатка и незаведённый тип отсюда неразличимы, и выбирать за
разработчика приёмка не вправе. Ось «разработчик» ban #18 переформулирована
честно (§2.12).

**Задачу-преемника я НЕ завожу — она существует.** `#1092` («таблицы типов
ПОРОЖДАЮТСЯ из манифеста вместо гейта дрейфа», `P1`, `size:L`, `release:modules`,
`Ref: #1087`) описывает ровно этот предмет и формулирует предикат теми же словами,
какими здесь названа цена: «после него **„добавить тип" перестаёт быть правкой
Go**». Заводить вторую задачу об одном предмете было бы тем самым классом, который
корпус ловит.

### 16.2. Б5 — ПРИНЯТО. Набор носителей identity назван ПРИЗНАКОМ

**Оба прогона проверяющего воспроизведены, оба подтверждены:**
`domain.KnownModules()` — шесть имён, включая `iam`; модульной учётки `kacho-iam`
в миграциях **ноль** (мой предикат считал вхождения по каждому имени, а не
`sort -u`: `kacho-iam-bootstrap` есть, `kacho-iam` — нет).

**Признак назван и он не перечень:** строка identity полагается модулю, чья учётка
**состоит в группе писателей отношений**, то есть держит `fga_writer` на кластере
и потому вообще доходит до этой полосы. Он читается **данными** — тем же составом,
из которого собран сам гейт записи (`20260823002000:89`, группа заполняется «из
действующего основания доступа»).

**Почему два перечня разошлись и разойдутся снова:** `domain.KnownModules()`
описывает **грантуемый домен правила роли**, строка identity — **вызывающего
проксируемой полосы**. Совпали они случайно; расходятся на всяком домене, который
грантуем и записи не делает.

**Сегодня писателей пять** — `vpc`, `compute`, `nlb`, `registry`, `storage`
(выдачи `fga_writer` в `0009` три, `0044`, `0057`; `kacho-api-gateway` и
`kacho-vpc-operator` учётки имеют, а выдачи — нет, и комментарий `0009` говорит
это дословно). Число снято **по дереву**; на стенде состав может отличаться, и
граница названа в §0.5.

**Правки по каждому следствию Б5:**

| следствие Б5 | правка |
|---|---|
| миграция не применилась бы никогда | §2.7 п. 3 считает **членов группы писателей** без identity, а не модули каталога |
| §2.10 п. 1 и `IAM-MV-14` неконструируемы | набор **выводится** обходом миграций; перепись печатает **обе** величины; ожидание — пять, и оно помечено ориентиром |
| инъекция §7.2 проверяет не ту сторону | ось разбита на две — «нет строки у писателя» и «есть строка без выдачи», плюс отдельная ось предпосылки (пустой обход роняет гейт) |

**Заводить `kacho-iam` ради счёта — не исход**, и это записано в §2.2: принципал,
которого никто не предъявляет, есть расширение поверхности ради зелёного гейта.

**Ослабление названо вслух** (§6): признак держат оператор и гейт, а **не** ключ —
первичный ключ `group_members` тройной, и внешний ключ «член именно этой группы»
без второго уникального индекса невыразим, а второй индекс был бы вторым местом об
одном предмете.

### 16.3. Что я перемерил СВЕРХ замечаний — и одно число здесь неприятное

**Проверка принадлежности сегодня не меняет ни одного вердикта.** Я сверил
приставку типа с доменом SAN по всем пяти писателям:

| писатель | домен SAN | приставка типов | модуль каталога | совпадает |
|---|---|---|---|---|
| vpc | `vpc` | `vpc_` | `vpc` | да |
| compute | `compute` | `compute_` | `compute` | да |
| nlb | `nlb` | `nlb_` | **`loadbalancer`** | приставка — да, модуль — нет |
| registry | `registry` | `registry_` | `registry` | да |
| storage | `storage` | `storage_` | `storage` | да |

Приставка совпадает с доменом у **всех пяти**, поэтому входов, которые сегодня
отвергаются и после работы принимались бы, — **ноль**, и входов, принимаемых
сегодня и отвергаемых полосой **принадлежности**, — тоже **ноль**. Единственное
сужение приносит полоса **референта** (незнакомый тип).

Я говорю это прямо, потому что иначе §2.2 читается как защита, которая сегодня
что-то ловит. Она ничего не ловит — она снимает **условие**: сегодня полоса
работает потому, что имена совпали, а совпадение никем не решалось. Строка `nlb`
показывает, насколько это тонко: домен и модуль там уже различны, и держится всё
на том, что сравнивают **приставку**, а не модуль.

**Второе перемеренное:** три писателя `resource_mirror`, а не один (§4), и оба
соседних референта живы — `vpc.network` у стража загрузки (`verify_gate.go:66`) и
точечные типы у посевщика нагрузки. Находки нет, но §4 и §9.1 обязаны их назвать.

### 16.4. Замечание §15.4 п. 3 — ОПРОВЕРГНУТО прогоном, и это четвёртый случай того же класса

Проверяющий назвал находкой по дороге: шапка `classifyApplyErr` в
`services/compute/internal/clients/iam_register_applier.go` якобы объявляет
«`codes.InvalidArgument` → `ErrPermanent`; любой другой → transient», тогда как код
на `:170` делает `ErrPermanent` и для `PermissionDenied`.

Прочитал шапку **целиком**, с первой её строки:

```
// classifyApplyErr maps a gRPC status to the drainer disposition. InvalidArgument
// (malformed tuple — retry is pointless) and PermissionDenied are permanent poison;
// every other code … is transient → drainer retries with backoff
```

**Шапка называет оба кода**, дословно и в первом же предложении. Комментарий свою
правку не пережил, расхождения нет, задачу заводить не под что — и я её не завожу.

**Класс тот же, что рецензент называл у себя дважды** (§15.0, §15.5) и что я нашёл
у себя трижды (§16.0): находка родилась **из способа смотреть** — из окна, не
включившего начало шапки. За два круга ревью это **четвёртый** случай в этой паре,
и все четыре поймал дословный повтор.

Отсюда признак для круга 4, дешевле любого из них: **шапку читают от её первой
строки, а не окном вокруг сигнатуры.**

### 16.5. Граница ответа — что круг 3 НЕ закрыл

- **Предусловие §8, пп. 2–4 остаётся блокирующим.** Прогона хранилища не было ни у
  автора, ни у обоих рецензентов; круг 3 добавил прогоны **чистых функций** и за
  замер строк их не выдаёт;
- **число писателей отношений (пять) снято по дереву, а не по базе.** Состав
  группы на стенде заполняется из строк, и расхождение — предмет п. 4 предусловия;
- **`drainer.Classify` на живом отказе я по-прежнему не вызывал** — классификация
  установлена по коду классификатора и по второму производителю той же
  классификации в компьюте. Замок §6 прогона требует и входит в под-фазу;
- **сплошной адъюдикации всех координат §0.1 и §5.4 круг 3 не делал заново** — я
  проверял те, которых касался, и добавленные мной; координаты, не тронутые кругом
  3, стоят как их проверил круг 2 (§15.1);
- **исход (б) замечания Б4 я отверг доводом, а не прогоном.** Довод проверяем
  чтением шапки одного гейта (§11, п. 17), но экспериментом — «сделать каталог
  авторитетом и посмотреть, что сломается» — я его не подтверждал: такой опыт есть
  сама работа.

### 16.6. Что круг 3 предъявляет по §15.6 — построчно

| требование §15.6 | где закрыто |
|---|---|
| 1. `IAM-MV-09` несёт исполнимый положительный контроль **либо** переведён на существующий тип, а портируемость вынесена задачей с номером | **оба**: контроль на `storage_volume`/`storage.volumes` (§5.2), портируемость — `#1092` (§3.5) |
| 2. §1.3, §2.2, §2.12 называют цену «новый тип требует релиза» либо объявляют её снятой | цена **названа** таблицей с предикатом на строку (§1.3), §2.2 снял противоположное утверждение, §2.12 пересчитан по осям «благо продукта» и «перспективность» |
| 3. набор носителей identity назван признаком; §2.7 п. 3, §2.10 п. 1, `IAM-MV-14`, §7.2 приведены к нему | §2.2 (признак и предикат), §2.7 п. 3, §2.10 п. 1, `IAM-MV-14`, §7.2 — все четыре |
| 4. §4 называет всех трёх писателей `resource_mirror` | §4, врезка с предикатом и таблицей референтов; §9.1 добавил их пробы |

**Сверх требуемого** круг 3 предъявляет три собственные находки (§16.0), одно
опровержённое замечание (§16.4) и одно неприятное число о собственном решении
(§16.3).

---

## 17. Ревью, круг 3 — `acceptance-reviewer`

**Вердикт: ❌ CHANGES REQUESTED.** Два блокирующих — **Б6** и **Б7**, — и оба
лежат на одной оси: **кому полагается строка `catalog_module_identity`**. Это та
самая ось, которую круг 3 правил по Б5; правка признака (§2.2) верна, а два его
следствия — держатель (§6) и гейт (§2.10 п. 1) — за ней не поехали.

Каждое замечание получено **прогоном**, и каждое перемерено **второй формой**
прежде, чем стать находкой: в этой паре ревью четыре находки подряд родились из
способа смотреть, а не из дерева, и пятая — моя, ниже.

### 17.0. Моя опровергнутая гипотеза — первой, и она пятая в этой паре

Я готовился записать блокирующим §2.4: автор утверждает «109 строк каталога, **ноль**
из проксируемых отношений», а мой предикат дал `project → 4`.

```sh
sed -n '/INSERT INTO kacho_iam.catalog_verb/,/;$/p' <миграция> | grep -c "'project'"
# 4
```

**Опровергнуто моим же повтором с другой единицей счёта.** Я считал вхождения
строки `'project'` **в кортеже целиком**, то есть попадал в колонку **ресурса**
(`('iam','project','get')` и три её соседа), а предмет утверждения — колонка
**глагола**. Перемер по третьему элементу кортежа:

```sh
sed -n '/INSERT INTO kacho_iam.catalog_verb/,/;$/p' <миграция> \
  | grep -oE "\('[a-zA-Z]+', *'[a-zA-Z]+', *'[a-zA-Z]+'\)" \
  | sed -E "s/.*, *'([a-zA-Z]+)'\)/\1/" | sort | uniq -c | sort -rn
#   27 list · 27 get · 26 update · 26 delete · 1 removetargets · 1 create · 1 addtargets   (=109)
# из них project|parent|owner|v_get → 0
```

Раскладка сошлась с §2.4 **дословно**, включая три однострочных глагола. Находки
нет; неверна была моя единица счёта. Это ровно тот класс, о котором предупреждает
разбор автора в §16.0, и он стоил бы блокирующего, будь замер один.

### 17.1. Что я проверил и ПОДТВЕРДИЛ — чтобы «ноль находок» было отличимо от «ноль прочитанного»

| утверждение | чем проверено мной | исход |
|---|---|---|
| §16.0 прогон 1: `caller=vpc` + `nlb_listener` отвергается **сегодня** | чтение `ValidateTuple` целиком (`policy.go:222`—`254`): связывание домена — `strings.HasPrefix(objType, objectDomainForCaller(caller)+"_")`; `nlb_` ≠ `vpc_` | **подтверждено**; переклассификация `IAM-MV-04`/`IAM-MV-06` верна |
| та же функция стоит на **обеих** RPC | `handler.go:192` и `:217` — одна строка, одна функция | **подтверждено** |
| текст полосы А — **дословно** `permission denied` | `handler.go:182`: `status.Error(codes.PermissionDenied, "permission denied")` | **подтверждено**; производитель именно этот |
| §16.0 прогон 2: незнакомый тип не приводится | `DottedType` = голый поиск по `dottedByFGAType`; `register_resource.go:203-213` отдаёт вход дословно | **подтверждено**; точечной формы у него не бывает |
| `compute_disk` не приводится, а строка каталога — `compute.disk` | карта `fga_types.go:480-526` (в ней `compute.disk` отсутствует) + посев снятых `20260901113757:233` | **подтверждено**; полоса `live` с полосы прокси недостижима |
| §2.4: 109 строк, ноль проксируемых | §17.0, второй формой | **подтверждено дословно** |
| §1.3: 27 типов, 20 чужих (9 vpc · 3 compute · 3 loadbalancer · 3 storage · 2 registry) | перепись карты по приставке точечного имени | **подтверждено построчно** |
| §0.2: `catalog_module_identity` в дереве нет | `git grep -c … ':!*.md'` → пусто | **подтверждено** |
| §0.2: у `resource_mirror` ноль внешних ключей | `0019_resource_mirror.sql:67-70` — PK + три CHECK | **подтверждено** |
| §4: писателей зеркала **три** | `git grep -n 'INTO kacho_iam.resource_mirror' -- '*.go' \| grep -v _test` → 3 | **подтверждено**; референты обоих соседей живы (`smokeMirrorType = "vpc.network"`, `verify_gate.go:66`; единственный вызывающий — `:324`) |
| §2.1: цель ключа существует | `catalog_resource_dotted_live_uk UNIQUE (dotted, live)` | **подтверждено** |
| §2.1: образец ключа и `CHECK (live)` | `:572` и `:594` — командой, печатающей номер строки | **подтверждено построчно** |
| §11 п. 7: постоянны **ровно** два кода | `classify.go:150`: `st.Code() == codes.InvalidArgument \|\| st.Code() == codes.PermissionDenied` | **подтверждено** |
| §11 п. 8: `drainer.Classify` экспортирована | `classify.go:82` | **подтверждено** |
| §2.6: байт-идентичность текста **конструируема** | `MapRepoErr` (`errors.go:47`) несёт пропуск статуса и ветку `ErrPermissionDenied` → `StripSentinel`; вырожденная обёртка отдаёт текст самого sentinel'а (`errors.go:162-163`) | **подтверждено**: производитель у «Тогда» будет |
| §4: чистота фундамента | `git grep -nE 'database/sql\|pgx\|"context"' -- pkg/authz/proxytuple` → пусто | **подтверждено** |
| §16.1, довод 2: `authzmap` импортирует **21** файл в **12** пакетах | свой предикат по пути импорта | **подтверждено дословно** |
| §16.4: шапка `classifyApplyErr` называет **оба** кода | прочитал от первой строки: «InvalidArgument … **and PermissionDenied** are permanent poison» | **подтверждено**; замечание §15.4 п. 3 действительно опровергнуто |
| §3, §12: четыре задачи-преемника существуют и **не о другом** | `gh issue view` по каждой: `#1828` · `#1829` · `#1830` · `#1092`, плюс `#1824` и эпик `#1087` | **подтверждено**, все OPEN, все `release:modules` |
| §5.2 `IAM-MV-09`: контроль исполним | `storage_volume` → `storage.volumes` есть в карте; строка `('storage','volumes','storage.volumes')` посеяна `20260901113757:216` | **подтверждено** |
| `IAM-MV-10` «Когда» конструируемо | `live` — обычная колонка под `CHECK (live = (retired_at IS NULL))`, не генерируемая; `UPDATE … SET live=false, retired_at=now(), superseded_by=…` проходит обе проверки | **подтверждено** — вход существует |
| ведомость приёмок читает вердикт | `docs-gate` при `KACHO_MONOREPO=<эта копия>`: **6/6, провалено 0, без предмета 0**, код **0**; `_lib.verdict` → `('CHANGES REQUESTED', …)` | **подтверждено** |

**Отдельно проверена ось, которой приёмка не касается** (`security.md` §«Отношение,
выполнимое подстановочным знаком»): `cluster#fga_writer: [service_account,
group#member]` (`fga_model.fga:286`, тип `cluster` объявлен на `:190`) —
подстановочным знаком не выполняется. Гейт, на который решение опирается,
действительно сужает. Находки нет — но из **этой же** строки вырастают Б6 и Б7.

### 17.2. Б6 (блокирующее). Единственное названное ослабление §6 обосновано УТВЕРЖДЕНИЕМ О БД, которое дерево опровергает — и опровергает своей же идиомой

**Что стоит в приёмке.** §2.2: «**Членство в группе ключом НЕ выражается, и это
сказано прямо.** Первичный ключ `group_members` — тройка `(group_id, member_type,
member_id)`, поэтому внешний ключ из `catalog_module_identity(sa_id)` на „члена
именно этой группы" **невыразим без второго уникального индекса**, который был бы
вторым местом об одном предмете». То же — строкой §6 («ключом **невыразимо**») и
абзацем под таблицей §6 («круг 3 назвал ОДНО ослабление вслух»).

**Чем опровергнуто — тремя измерениями, каждое самостоятельно достаточно:**

1. **тройной первичный ключ есть годная ЦЕЛЬ составного внешнего ключа, а не
   препятствие ему.**
   ```sh
   grep -n 'group_members_pkey' services/iam/internal/migrations/0001_initial.sql
   # 1023: ADD CONSTRAINT group_members_pkey PRIMARY KEY (group_id, member_type, member_id);
   ```
   Ссылка `FOREIGN KEY (a, b, c) REFERENCES group_members (group_id, member_type,
   member_id)` попадает **ровно** в него. Второй уникальный индекс понадобился бы
   для **более узкого** ключа — например `(group_id, member_id)` в обход
   `member_type`; обходить его не надо, он константа `'service_account'`
   (`20260823002000:91`, а словарь из двух значений держит
   `group_members_type_check`, `0001_initial.sql:482`);
2. **трёхколоночные внешние ключи в этой схеме уже есть — два.**
   ```sh
   grep -rnE 'FOREIGN KEY \([a-z_]+, *[a-z_]+, *[a-z_]+\)' services/iam/internal/migrations
   # 732001_binding_subject_carries_its_scope.sql:196  FOREIGN KEY (binding_id, resource_type, resource_id)
   # 20260901113757_rule_segments_have_a_referent.sql:581  FOREIGN KEY (module, resource, live)
   ```
   Второй — из **той самой** миграции-предшественника, на которую опирается §2.1;
3. **идиома «колонка-константа ради составного ключа» landed, и её комментарий
   называет предмет дословно.**
   ```sh
   sed -n '571,576p' services/iam/internal/migrations/20260901113757_rule_segments_have_a_referent.sql
   # ADD CONSTRAINT role_verb_live_true CHECK (live);
   # COMMENT ON COLUMN kacho_iam.role_verb.live IS
   #   'Константа true. Колонка существует ради ключа role_verb_type_fk: сослаться
   #    на «эту строку каталога И она жива» без неё нечем.';
   ```
   §2.1 применяет эту идиому у `resource_mirror` **на соседней странице** — и §2.2
   объявляет её неприменимой, не назвав, чем этот случай отличается.

**Почему это блокирующее, а не важное — три довода, и третий несущий:**

- **запрет #10 делает выбор нормативным**, а не вкусовым: инвариант внутри одной
  БД выражается конструкцией БД. Приёмка выбрала более слабого держателя,
  сославшись на невозможность, которой нет;
- **§6 сам объявляет этот исход блокирующим**: «Если рецензент найдёт строку,
  держащуюся вниманием, — это блокирующее». Строка держится **оператором и гейтом
  дерева**, и обоснование выбора ложно;
- **предмет таблицы — личность вызывающего.** `catalog_module_identity` отвечает
  на вопрос «кто это пришёл», а под целью эпика (`#1087`: модуль привозит своё
  данными, сайдкар применяет) строка этой таблицы — **ровно то, что привозит
  модуль**. Без ключа единственный держатель — гейт **нашего** дерева, который
  строку, применённую сайдкаром в чужом кластере, не видит **by construction**.
  То есть контроль объявлен там, где вход не приходит.

**Что закрывает — одно из двух, и выбор ваш (запрет #18), а не мой:**

(а) **выразить ключом**: `catalog_module_identity` несёт `writers_group_id` и
`member_kind`, обе с `DEFAULT`+`CHECK` на константу (идиома §2.1), и
`FOREIGN KEY (writers_group_id, member_kind, sa_id) → group_members`. Тогда строка
identity **не заводится** для того, кто в группе не состоит, — на вставке, а не на
старте; §6 получает вид «конструкция БД», а §2.7 п. 3 и §2.10 п. 1 остаются
**вторым** рубежом, а не единственным. Цена: снятие модуля из группы упорядочено —
сперва строка identity (это ровно то же упорядочение, что даёт ключ §2.1, и его
надо назвать в §2.8);

(б) **оставить оператор и гейт — но назвать ИСТИННОЕ основание.** Оно есть, и я его
нашёл, проверяя вашу же ссылку: `cluster#fga_writer: [service_account,
group#member]` (`fga_model.fga:286`) допускает **прямую** выдачу учётке, в обход
группы. Ключ на `group_members` запретил бы форму выдачи, которую **модель
разрешает**, — и запретил бы её молча, отказом на вставке строки identity. Это
защитимый довод; «невыразимо» — нет.

«Оставить как есть» исходом не является: сегодня §2.2 объясняет выбор
утверждением о БД, которое опровергается тремя командами по дереву.

### 17.3. Б7 (блокирующее). Гейт §2.10 п. 1 выводит набор из ОТСТАВНОГО маршрута выдачи — поэтому «вырастет с первым же новым модулем» неверно, а зеркальная сторона краснеет на верной работе

**Что стоит в приёмке.** §2.2 называет признак носителя строки identity — «модуль,
чья учётка **состоит в группе писателей отношений**» — и читает его **данными**
(`SELECT member_id FROM group_members WHERE group_id = 'grp'||…`). §2.7 п. 3 считает
**членов группы** без identity. Но §2.10 п. 1 и `IAM-MV-14` выводят набор **иначе**:
«у каждой модульной учётки, которой **миграции выдают `fga_writer`**, есть ровно
одна строка identity… гейт ищет **выдачи `fga_writer`** в очередь намерений». И
§5.3 обещает: «Число **выводится гейтом**, а не вписывается: **оно вырастет с первым
же новым модулем** и устареет молча».

**Чем опровергнуто — тремя измерениями:**

1. **выдачи `fga_writer` живут в трёх миграциях, и все три — на ОТСТАВНОМ якоре.**
   ```sh
   git grep -l "'fga_writer'" -- services/iam/internal/migrations
   # 0009_sec_c_module_sa_least_priv.sql · 0044_registry_sa_least_priv.sql
   # 0057_storage_sa_least_priv.sql · 20260823002000_relation_write_moves_onto_the_cluster.sql
   ```
   Первые три эмитят `…#fga_writer@iam_fgaproxy:system`, а модель называет этот тип
   надгробием дословно: «ИСТОРИЧЕСКИЙ ЯКОРЬ… **ЖИВЫХ фактов на нём НЕТ**»
   (`fga_model.fga:929-931`);
2. **новый модуль приходит ЧЕРЕЗ ГРУППУ, и это решено, а не сложилось.** Комментарий
   `cluster#fga_writer` говорит это своими словами: «Почему `group#member`, а не
   перечисление учёток. Получателей пятеро, и **состав меняется с каждым новым
   доменом**, который начнёт регистрировать свои ресурсы (data-integrity.md B18)»
   (`fga_model.fga:277-280`). B18 требует того же: право на многих выдаётся группе,
   и «снять одного из группы — одна строка членства»;
3. **группу трогает ровно одна миграция.**
   ```sh
   git grep -ln "module.relation_writers" -- services/iam/internal/migrations   # → 1
   ```
   Значит шестой модуль добавляется **строкой членства**, а новой выдачи
   `fga_writer` не появляется **никогда**: выдача уже сделана — группе.

**Три следствия, каждое самостоятельно блокирующее:**

- **обещание §5.3 ложно.** N гейта прибит к трём отставным миграциям и не вырастет;
  вырастет M. Утверждение «оно вырастет с первым же новым модулем» — то самое
  «утверждение шире измеренного», за которое приёмка получила Б4;
- **зеркальная сторона гейта краснеет на ВЕРНОЙ работе.** §2.10 п. 1 объявляет
  находкой «строку identity **без** выдачи `fga_writer`». У шестого модуля строка
  есть, выдачи нет — гейт красен на дереве, где всё сделано правильно. Гейт,
  краснеющий на верном коде, отключают первым (`testing.md` §«Гейт на класс»);
- **у одного свойства ТРИ разных предиката, и это не названо.** Признак (§2.2) —
  строки `group_members`; самопроверка миграции (§2.7 п. 3) — те же строки; гейт
  дерева (§2.10 п. 1) — выдачи в миграциях. Сегодня они совпадают **не by
  construction**, а потому что `20260823002000:89` собрал группу из фактов тех же
  трёх миграций. §0.5 называет границу **замера** («мерил по миграциям, а не по
  базе»), но не называет того, что предикаты **разные**.

**Что закрывает.** Гейт дерева не читает БД **by construction**, поэтому «тем же
предикатом» он не сможет никогда — и это надо не спрятать, а **назвать и выбрать
источник, который растёт вместе с предметом**. Наименьшая правка: гейт выводит
набор из **миграций, добавляющих членов в группу писателей** (источник, который
шестой модуль обязан тронуть), а не из выдач `fga_writer`; §5.3 снимает обещание
про рост либо привязывает его к новому источнику; §2.10 п. 1, `IAM-MV-14` и обе оси
§7.2 приводятся к нему; §0.5 (или §6) называет, что признак и его гейт — **разные
предикаты**, совпадающие сегодня по одной миграции.

### 17.4. Не блокирующие — правятся тем же кругом

1. **В1. §16.1 назвал не тот гейт — и вывод от этого только крепче.** Довод против
   исхода (б) верен по существу, но `TestCatalogTypesExistInTheCanonicalModel`
   снимать бы **не пришлось**: он читает **литерал Go** (`catalog := authzmap.Catalog()`,
   `canonical_model_drift_test.go:63`), поэтому при каталоге-авторитете он остался бы
   **зелёным и стал бы вакуумным** — строк, пришедших мимо литерала, он не видит вовсе.
   Снимать пришлось бы **другое**: двусторонний паритет
   (`Diverged() = MissingRows>0 || ExtraRows>0`, `catalog_parity.go:58-60`), чей отказ
   фатален для старта (`serve.go:1333-1334`). Замените имя — довод станет сильнее:
   исход (б) требует не только снятия рубежа, но и обращения landed-гейта в вакуумный.
2. **В2. §4: два объекта из трёх названы отсутствующими в каталоге, а они там есть.**
   «выдачи iam на `account` / `project` / `cluster`, **которых в каталоге ресурсов
   нет**» — `account` приводится в `iam.account`, `project` в `iam.project`
   (`fga_types.go:520-521`), и обе строки посеяны живыми (`20260901113757:203` и `:205`).
   Вывод §4 («чтение стоит на полосе прокси, а не в общем эмитенте») **верен**, но по
   другой причине: у `iam` нет строки identity, поэтому чтение (1) отвергло бы его
   собственные выдачи. Без правки следующий читатель построит на ложной посылке.
3. **В3. Второй контроль `IAM-MV-10` не различает того, ради чего заведён.** Он
   объявлен как «правка **живой** строки каталога, не трогающая `live`
   (`retired_reason` у уже **снятого** типа)» — формулировка противоречит себе, а по
   существу правится строка, на которую **никто не ссылается**. Заявленная цель —
   «иначе отрицание зеленело бы на ключе, запрещающем любую правку каталога» — такой
   правкой не проверяется: чрезмерно широкий ключ запрещал бы правку **ссылаемой**
   строки, а контроль её не трогает. Контроль обязан править `retired_reason` у
   **живой `vpc.network`, на которую ссылается строка зеркала** (это состояние
   производит `IAM-MV-01`).
4. **В4. `IAM-MV-07` — единственный отрицательный без названного контроля**, при том
   что заголовок §5.2 обещает исполнимый положительный **каждому**. Закрывается
   строкой «Контроль: `IAM-MV-01`».
5. **В5. Таблица-дискриминатор §2.2 говорит регистрационным языком за обе полосы.**
   Строка «(2) ноль строк | типа нет в каталоге **живым**» верна для регистрации; на
   полосе снятия чтение (2) идёт **без** предиката `live`, и ноль строк означает там
   «типа нет в каталоге **вовсе**». Одна оговорка в таблице.

### 17.5. Граница МОЕГО замера — названа, а не обойдена

- **прогона хранилища у меня не было**: `df -h /` → **5.2 ГБ при 98 %**, контейнеры
  не поднимались осознанно. Это «не выполнилось», а не красный вердикт. Ни Б6, ни Б7
  базы не требуют: оба установлены по DDL, по литералам, по модели и по комментариям
  дерева;
- **Go-прогона у меня не было тоже.** Три находки круга 3 (§16.0) я воспроизвёл
  **чтением исполняемого кода** — `ValidateTuple` целиком, `DottedType`,
  `objectType()`, — а не подачей входа. Для связывания домена этого достаточно
  (условие одно и синтаксически однозначно), но я **не** выдаю это за прогон:
  автор подавал вход, я читал ветвь;
- **предусловие §8, пп. 2–4 остаётся блокирующим** и кругом 3 не закрыто — числа
  строк зеркала без референта и состав группы писателей **на живой базе** не мерил
  никто из четверых;
- **сплошной адъюдикации всех координат §0.1 я не делал** — проверил все, что
  называют код или текст (§5.4), и все, которых касаются Б6, Б7 и В1–В5;
- **три СВОИ координаты я поставил неверно и поймал их дословным прогоном
  собственных цитат** — уже после того, как написал раздел: `policy.go:215` вместо
  `:222`, `classify.go:151` вместо `:150`, `20260901113757:198-199` вместо `:203`
  и `:205` (плюс две мелких). Механизм тот же, что у четырёх находок этой пары:
  номер брался из окна, в котором я смотрел, а не из команды, печатающей номер.
  Признак круга 3 («координату проверяют командой, печатающей номер строки»)
  связывает и рецензента; я применил его к себе последним действием, а надо было
  первым;
- **числа §17.1, взятые из чужих замеров, я пересчитывал своей командой**; там, где
  сходилось, помечено «подтверждено», там, где мерил впервые, названа команда.

### 17.6. Что круг 4 обязан показать

По Б6 и Б7 — **тот же** замер, которым опровергнуто, с другим исходом:

1. §2.2 и §6 либо несут ключ (три колонки, две пинятся константой — идиома §2.1),
   либо называют **истинное** основание отказа от него; «невыразимо тройным
   первичным ключом» из документа уходит в любом случае;
2. §2.10 п. 1, `IAM-MV-14` и обе оси §7.2 выводят набор из источника, **который
   растёт вместе с предметом**; обещание §5.3 про рост либо снято, либо привязано
   к новому источнику;
3. §0.5 либо §6 называет прямо, что признак (строки БД) и его гейт (обход дерева) —
   **разные предикаты**, совпадающие сегодня по одной миграции;
4. В1–В5 правятся тем же кругом.

Остальное — §0, §1, §2.1, §2.3, §2.4, §2.5, §2.6, §2.7 пп. 1–2, §2.8, §2.9, §2.11,
§2.12, §3, §5 (кроме контроля `IAM-MV-10` и `IAM-MV-07`), §7 (кроме двух осей
identity), §8–§12 и §16 — **проверено и возражений не имеет**; переписывать их круг
4 не обязан.

**И одно, что стоит сказать вслух.** Три находки круга 3 (§16.0) и опровержение
§16.4 — верны все четыре, и все четыре я перемерил. Два блокирующих ниже — не
откат этой работы, а её продолжение на **одну ось глубже**: круг 3 верно заменил
перечень признаком, и обе находки о том, что за признаком не поехали его держатель
и его гейт.
