# Copyright (c) PRO-Robotech
# SPDX-License-Identifier: BUSL-1.1

{{/*
Two Services so api-gateway (public clients) and peer services (internal
listener) get separate DNS records — workspace CLAUDE.md "Запреты" #6:
Internal.* must never be reachable from the external TLS endpoint.
*/}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kacho-nlb.fullname" . }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "kacho-nlb.labels" . | nindent 4 }}
    kacho.io/listener: public
spec:
  type: ClusterIP
  selector:
    {{- include "kacho-nlb.selectorLabels" . | nindent 4 }}
  ports:
    - name: grpc
      port: {{ .Values.ports.grpc }}
      targetPort: grpc
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kacho-nlb.fullname" . }}-internal
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "kacho-nlb.labels" . | nindent 4 }}
    kacho.io/listener: internal
spec:
  type: ClusterIP
  selector:
    {{- include "kacho-nlb.selectorLabels" . | nindent 4 }}
  ports:
    # Порт диагностики стоит на ВНУТРЕННЕМ Service, а не рядом с арендаторской
    # gRPC-поверхностью. Сегодня оба Service — ClusterIP, и наружу это не
    # выходит; но соседство инфра-диагностики с поверхностью арендатора обязано
    # быть исключено ПОСТРОЕНИЕМ, а не типом Service, который завтра поменяют
    # (`security.md` §Internal-vs-external).
    - name: metrics
      port: {{ .Values.ports.metrics }}
      targetPort: metrics
      protocol: TCP
    - name: igrpc
      port: {{ .Values.ports.internalGrpc }}
      targetPort: igrpc
      protocol: TCP
