// Словарь описаний полей — единый источник для таблиц запроса/ответа во всех
// API-страницах (DRY: одно описание поля переиспользуется на всех ресурсах).
export const DICTIONARY = {
  id: { short: 'Идентификатор ресурса — TEXT: префикс + crockford-base32 (output-only, генерируется сервером, immutable)' },
  projectId: { short: 'Идентификатор проекта kaname, которому принадлежит ресурс (обязателен при создании, immutable)' },
  name: { short: 'Имя ресурса: DNS label по RFC 1123 — ^[a-z0-9]([-a-z0-9]{0,61}[a-z0-9])?$ (строчные латинские буквы, цифры и дефис; 1..63; цифра первым знаком допустима). Уникально в паре (projectId, name). Пустая строка — законный вход Create: сервер проставит имя, производное от id; в Update отвергается' },
  description: { short: 'Описание ресурса (UTF-8, ≤256)' },
  labels: { short: 'Пользовательские метки key→value (≤64 пар) для поиска ресурса' },
  createdAt: { short: 'Время создания (output-only; truncate до секунд)' },
  updatedAt: { short: 'Время последнего изменения (output-only; truncate до секунд)' },
  updateMask: { short: 'FieldMask: список изменяемых полей. Неизвестное поле / immutable → InvalidArgument; пустой mask = full-PATCH' },
  status: { short: 'Статус ресурса (output-only enum)' },
  filter: { short: 'Строка фильтра (конвенция Kachō; поддерживается name="<value>")' },
  pageSize: { short: 'Размер страницы (0 → 50, max 1000)' },
  pageToken: { short: 'Opaque cursor (base64 от {created_at, id}); невалидный → InvalidArgument' },
  zoneId: { short: 'Идентификатор зоны размещения; существование валидируется через kacho-geo ZoneService.Get (TEXT-ссылка, без FK)' },
  regionId: { short: 'Идентификатор региона размещения; существование валидируется через kacho-geo RegionService.Get (TEXT-ссылка, без FK)' },
  diskTypeId: { short: 'Идентификатор класса диска — slug каталога (FK внутри своей БД, ON DELETE RESTRICT). В updateMask не входит никогда: меняется отдельным глаголом :changeDiskType' },
  sizeBytes: { short: 'Размер в байтах. У тома > 0, в границах класса и изменяется только в сторону увеличения' },
  minDiskBytes: { short: 'Минимальный размер тома, в который образ помещается (output-only; выводится из размера источника, у зарегистрированного образа приходит от регистрации)' },
  sourceSnapshotId: { short: 'Идентификатор снимка-источника (FK внутри своей БД, ON DELETE SET NULL); immutable' },
  sourceVolumeId: { short: 'Идентификатор тома-источника (FK внутри своей БД, ON DELETE SET NULL); immutable' },
  sourceImageId: { short: 'Идентификатор образа-источника загрузочного тома (FK внутри своей БД, ON DELETE SET NULL — происхождение, не живая зависимость); immutable' },
  format: { short: 'Формат образа (output-only). Текущее значение каталога — STANDARD' },
  zoneIds: { short: 'Список зон, где класс диска предлагается; пустой список — ограничения нет' },
  attachments: { short: 'Привязки тома к машинам — авторитетное состояние (output-only, публичная проекция)' },
  usedBy: { short: 'Обобщённая проекция «кто использует ресурс» (output-only; тот же источник истины, что и attachments; на вход Create/Update не принимается)' },
  instanceId: { short: 'Идентификатор машины kacho-compute — TEXT-ссылка через границу сервиса, без FK' },
  instanceName: { short: 'Имя машины на момент привязки (снимок значения; storage не перечитывает его у compute)' },
  deviceName: { short: 'Имя гостевого устройства, уникальное в пределах машины. Пустое значение — сервер подбирает первое свободное' },
  isBoot: { short: 'Том выступает постоянным корневым слоем машины. Не более одного такого тома на машину' },
  mode: { short: 'Режим доступа привязки: READ_WRITE или READ_ONLY' },
  autoDelete: { short: 'Удалять том вместе с машиной' },
  attachedAt: { short: 'Время привязки (output-only; truncate до секунд — усечение применяется и к вложенной записи)' },

  // ── политика класса ──────────────────────────────────────────────────────
  tier: { short: 'Ярус класса — закрытый словарь CAPACITY | BALANCED | FAST | SINGLE | IO_MAX. Необъявленный ярус законен, значение вне словаря отвергается на входе. Ярус не обещает чисел и не является шкалой' },
  lifecycle: { short: 'Состояние обращения класса: принимает ли он НОВЫЕ тома. Существующие тома живут при любом значении. Меняется отдельным глаголом :setLifecycle' },
  capabilities: { short: 'Что класс умеет (output-only). Выводится ПЕРЕСЕЧЕНИЕМ действующих ревизий привязки и на вход Create/Update не принимается — иначе у одного факта было бы два источника' },
  limits: { short: 'Границы размера тома, объявленные классом; энфорсятся на Volume.Create/Update. Ноль в поле означает «класс не сужает с этой стороны»' },
  minSizeBytes: { short: 'Наименьший размер тома в байтах; 0 — класс не сужает снизу' },
  maxSizeBytes: { short: 'Наибольший размер тома в байтах; 0 — класс не сужает сверху' },
  sizeStepBytes: { short: 'Шаг размера в байтах: размер тома обязан быть ему кратен; 0 — кратность не требуется' },

  // ── наблюдение за бэкендом ───────────────────────────────────────────────
  statusReason: { short: 'Почему ресурс оказался в своём состоянии (output-only). Закрытый словарь НАШИХ полос: текст бэкенда и имена физики наружу не выходят. В штатном состоянии — STATUS_REASON_UNSPECIFIED' },
  usedBytes: { short: 'Фактически занятые байты, наблюдённые у бэкенда (output-only). Поле ОТСУТСТВУЕТ, если бэкенд потребление не сообщил, — оно не равно нулю: «не сказали» и «пусто» разные факты' },
  observedState: { short: 'Что сверщик УВИДЕЛ у бэкенда в последний раз — отдельно от желаемого состояния, иначе дрейф был бы ненаходим' },
  observedAt: { short: 'Когда сверщик смотрел на объект в последний раз (output-only; пусто — не смотрел ни разу)' },
  backendObject: { short: 'Имя объекта у бэкенда — координата, по которой ресурс находится в хранилище. Инфра-чувствительно: только внутренний листенер' },
  bindingId: { short: 'Идентификатор неизменяемой ревизии привязки, под которой создан ресурс. Инфра-чувствительно: только внутренний листенер' },

  // ── снимок и образ ───────────────────────────────────────────────────────
  snapshotZoneId: { short: 'Зона снимка (output-only, immutable): снимается с зоны исходного тома на Create. Собственный якорь, а не ссылка на том, — ссылка на источник обнуляется при его удалении' },
  seededBy: { short: 'Кто засеян этим ресурсом: тома, созданные из него (output-only). Нужен ДО удаления, а не после отказа' },
  targetZoneId: { short: 'Зона, в которой создаётся копия. Обязательна и называется явно — умолчания «та же зона» у переноса нет' },
  targetRegionId: { short: 'Регион, в котором создаётся копия образа. Обязателен, валидируется через kacho-geo' },
  copyProjectId: { short: 'Проект, в котором создаётся копия, — он же объект вопроса о правах (editor@project). Обязателен и обязан совпадать с проектом источника: расхождение отвечает тем же текстом, что и несуществующий источник' },

  // ── реестр бэкендов (внутренний) ─────────────────────────────────────────
  backendKind: { short: 'Род бэкенда — закрытый перечень, а не свободная строка: за каждым значением стоит адаптер в нашем коде, поэтому род без исполнителя зарегистрировать нельзя. Immutable после Create' },
  backendZoneIds: { short: 'Зоны, которые бэкенд обслуживает. Ревизия привязки создаётся только на зону из этого списка' },
  endpoint: { short: 'Непрозрачная координата бэкенда вида <схема>://<путь>. Ни адресов узлов, ни имён пулов в API не кладётся — схему разрешает конфигурация процесса' },
  credentialsRef: { short: 'ССЫЛКА на учётные данные вида <схема>://<путь>, никогда само значение. Секрет не проходит через API и не хранится в БД' },
  backendStatus: { short: 'Полоса действия бэкенда: ACTIVE (принимает новые привязки) | DRAINING (только существующие) | DISABLED (ни тех, ни других)' },

  // ── ревизия привязки (внутренняя) ────────────────────────────────────────
  backendId: { short: 'Идентификатор бэкенда, исполняющего эту ревизию (FK внутри своей БД, ON DELETE RESTRICT)' },
  revision: { short: 'Номер ревизии в пределах пары (класс, зона), с 1. Присваивает сервис — номер, названный клиентом, был бы потерянным обновлением' },
  locator: { short: 'Где у бэкенда лежат объекты этой ревизии: пул и шаблон единицы изоляции арендатора. Инфра-чувствительно целиком' },
  bindingCapabilities: { short: 'Что бэкенд умеет под этой ревизией. Источник истины для публичных способностей класса и для отказов публичных путей' },
  qos: { short: 'Обещание производительности ФОРМУЛОЙ: baseline + за_ГиБ × размер, ограниченное max. Блок отсутствует целиком, если бэкенд QoS не энфорсит' },
  bindingStatus: { short: 'Полоса действия ревизии: ACTIVE (ровно одна на пару «класс, зона») | SUPERSEDED (новые ресурсы не ссылаются, существующие ссылаются и дальше)' },
} as const

export type DictKey = keyof typeof DICTIONARY
