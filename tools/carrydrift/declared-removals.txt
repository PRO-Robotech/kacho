# Осознанные СНЯТИЯ содержимого ствола при переносе — перечень С ПРЕДМЕТОМ.
#
# ЗАЧЕМ ЭТОТ ФАЙЛ. Гейт `drift.sh` стережёт молчаливый откат: ветка, отставшая от
# ствола, возвращает содержимое базы на файлах, которых не касалась, и конфликта
# при этом нет. Но у снятия есть и законный случай: ствол и ветка независимо
# завели ОДИН предмет разными файлами, и в результате обязан остаться один.
# Тогда файл ствола снимается осознанно — и это решение, а не откат.
#
# Различие между ними не выводится из графа: по путям обе картины одинаковы.
# Поэтому решение называется здесь ЯВНО, по одному файлу на запись, с причиной.
#
# КАК ЭТО ЧИТАЕТСЯ ГЕЙТОМ:
#   находка есть, строки нет   → ОТКАЗ. Молчаливое снятие не проезжает.
#   находка есть, строка есть  → пропуск, и он печатается: снятие названо решением.
#   строка есть, находки нет   → ОТКАЗ. Записи больше нечего исключать.
#
# Последнее — не придирка. Запись, пережившая свой предмет, прикроет следующее
# снятие, и прикроет молча: гейт останется зелёным ровно на том, ради чего писался.
# Поэтому запись УДАЛЯЕТСЯ тем же изменением, которое поднимает базу сравнения.
#
# Форма записи: <путь> <TAB или пробелы> <причина одной строкой>
# Пропуском засчитывается ТОЛЬКО снятие (результат не несёт файла). Если файл на
# месте, но с другим содержимым, — это не снятие, и запись его не прикрывает.

terraform/internal/provider/machine_type_data_source.go	дубль источника kacho_compute_machine_type: ствол и ветка независимо завели один TypeName, оставлена версия ветки — она покрывает поля стволовой целиком (id/name/description/family/status/memory_mib/available_zones/created_at) и несёт шесть своих
terraform/internal/provider/machine_type_data_source_test.go	пробы снятого дубля источника
terraform/examples/compute/main.tf	пример ствола писан под снятую версию ресурса машины: приспособлен к оставшейся (вложенный источник загрузки, список спецификаций интерфейса, явный выбор достижимости) и к списочному источнику каталога размеров
terraform/internal/provider/registry_census_test.go	гейт ствола считал фабрикой конструктор: параметризованная функция в реестр не вносится by construction (реестр принимает функцию без аргументов), поэтому различитель формы добавлен — доказано инъекцией в обе стороны
terraform/internal/provider/instance_resource.go	дубль ресурса kacho_compute_instance: ствол и ветка независимо завели ОДИН TypeName (`_compute_instance`), сосуществовать они не могут. Оставлена версия ветки — 1697 строк против 424, и она покрывает вход стволовой целиком: плоская пара boot_source_type/boot_source_id выражена вложенным boot_source с теми же двумя полями, остальные поля стволовой формы присутствуют
terraform/internal/provider/instance_resource_test.go	пробы снятого дубля ресурса машины
